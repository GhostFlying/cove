# Cove current handoff

Updated: 2026-09-27. Verify live GitHub/checkout state before resuming. Accepted behavior remains in the design documents; exact task evidence belongs to linked Issues/PRs.

## Authorized stage

The user explicitly approved M0 implementation on 2026-09-26 after reviewing [the merged M0 DAG](milestones/m0.md). The entry blocker is removed; do not ask again for routine implementation, testing or gated PR merges within that scope. M0 exit and M1 entry still require user review/permission. Architecture, product scope and acceptance changes still go to the user with evidence/options.

Planning PR [#27](https://github.com/GhostFlying/cove/pull/27) merged six atomic commits by rebase. Baseline main `a902ee0bae0e6539afab6555c0585ff6745261d5` passed macOS/Linux checks; tree equality, independent validation/review, corrected review findings and commit mappings are recorded in PR #27 and [Issue #5](https://github.com/GhostFlying/cove/issues/5). Planning Issues #5–#8 are closed and their clean worktrees were removed.

## Current dispatch

Current P3/N1 checkpoint, 2026-09-27: the user approved explicit external subscription identity, fresh IDs for new attach and the same complete ID for recovery within a live subscription. The R1 protocol source at original `c33f7f1` passed independent source review and bounded compiled verification; its protocol and fixture Git trees were replayed byte-identically onto accepted main `77de10a` in the separate R1 integration branch. Shared plans now describe the ordered recovery-result barrier and local attempt/view fences; runtime W2/P2/P3 producer and consumer barriers remain unimplemented. The user's release policy maintains no experimental-version compatibility line before Cove's first formal versioned release, while same-version and formal-release obligations remain. N1's maintained node-pty patch is in draft [PR #42](https://github.com/GhostFlying/cove/pull/42), latest author head `8b43915`; first-source descriptor churn failed, corrected source `e1a8c08` passed author checks, and independent corrected-source attribution/review plus hosted CI still gate acceptance. Neither R1 nor N1 is merged at this checkpoint.

Latest checkpoint, 2026-09-26: V1 PR38 is merged at main `489f3466d55009b2730a8fa70b86c54e44abbb3c`; all40 commit mappings match and final-main CI36240528166 passed macOS/Linux30suites270tests with both artifacts verified. Issue20 is closed. The detailed pre-merge evidence below remains historical. The user has now selected **a minimal maintained node-pty patch for W1**; see the accepted section in [native input decision](tasks/m0-worker-native-input-decision.md). This removes the maintenance-route decision blocker, while concrete patch planning and subsequent implementation/validation remain. P3 direction is subsequently clarified above; exact revised contract and compatibility details still need implementation-plan review. No milestone transition occurred.

T2 [PR #37](https://github.com/GhostFlying/cove/pull/37) is complete at main `caa2c65cf618aa9ba77df4739884cd25e8d1118b`. All eight atomic source-to-main tree/message/single-parent mappings match final head `df0da356996ae32a875dbb1b235ca240ced9a8c8`, with equal tree `9414d88d8fd6b6833ccc5c9412271b81936ac9c8`. Final PR run36235494574 and [main run36235691451](https://github.com/GhostFlying/cove/actions/runs/36235691451) passed macOS/Linux, 27 suites / 247 tests each; both artifact sets were checked against exact SHA/pins/lock, inventory and inherited native/browser/recovery evidence. Final independent verification SHA256 `ab4adbb55b473e966c19ed9c767429e56a6d2a7d7e3ebe57de025f1368b367b5`; independent Sol-high review `9182dc9b240c1df5600791977137e6fe1a92629e50296bfb3467d3ef11ee8fea`. Issue #15 is closed; W1 planning is released.

The first local full T2 check failed 246/247 on an unchanged Q1 browser-close timeout. One isolated and one complete same-source reproduction passed, with owned browser/listener cleanup verified. The original failed artifacts remain preserved; its cause is unknown and is not declared fixed. First-source reset, queue-accounting and fault-drain defects were repaired and independently verified separately. See PR #37 and [engine results](tasks/m0-terminal-engine-results.md).

V1 [PR #38](https://github.com/GhostFlying/cove/pull/38) now has approved final source at integrated `6aea1735aca685830631f7cf21b9edb15db7403e` (author `e5377119a69d850938ccbb17ef091aa70c868b01`). Independent Sol review SHA256 `b86dedfca19378b8e46d66f5fa4ae62874fb34beaf827c9f8dcc7e91b581376e` closes public stylesheet, immediate fatal retirement, renewed focus intent, post-disposal subscriptions and callback reentry findings. Genuine input remains observable during baseline installation; only P3 decides server admission. Author final scoped lifecycle6 and static checks pass; prior18-case author results belong to older source, not this final correction. Results-only author `b79f271` maps to root `1ab3a02`.

Independent Sol verification at exact `6aea173` passed four frozen public-browser checks and one pinned full gate: 30 suites / 267 tests, numeric exit0. Report SHA256 `990852b6accb1949bf21fa0b079bafae02a5b01b5279e3dc450eea02a432e862`; four independent browser PID/ports were absent and the verifier checkout was clean. The checks cover fresh focus before repeated genuine input after simulated grant invalidation, rejection of all three post-disposal registrations, synchronous replacement without old-input relabeling, and real keyboard input while a baseline parse is held. These are adapter observations, not a real server grant/PTY workflow. An initial unpinned launcher failed before tests and is preserved separately; only one pinned full gate ran at this source.

Preserved earlier evidence includes the actual stale-input failure at `62d8b15` (report `cc28ae17fd8220d7f0aaa26d50e6398677e24e2309a50125f843d27023034434`), the intermediate guard regression at `59173fc` (report `645ef1cc71338c1471ee373ec998c9c15ccf01004cec713e79840d7e304ab8d0`), and the author's initial TS2367 build error before correcting closure-state narrowing. The earlier CSS/fatal report is `670b0c4a02a243f0d60fac63e046457b3dcbc61c97148fa62db98c3ce7689c2d`; its wrapper exit capture limitation and independent driver corrections remain disclosed. Exact earlier hosted run36237890369 passed both OSes267/267, with artifacts and synthetic fee3bf9 exactcaa2/aa2463parents/equaltree7678b350 checked; it does not qualify the later source.

A later shared-tooling review at `7f96fbf` found that the package test script omitted V1 and the evidence writer incorrectly fixed total view cases at18. Separate Sol registrar `/root/m0_t1_impl` safely preserved W1 branch `a5f874f` and temporarily took `p/luchengxuan/m0-20-view-tooling` in its existing checkout. Atomic author plan/fixes `f4f8043` / `4e8ef00` / `4f90a0d` map to root `66c1052` / `d51681d` / `701a4e51e6cf011aea28a6a8d8dbaab71f002377`, tree `8e05a830e7c4fd1d3d104494f47e22a9a5970fb4`. Package tests now select both projects; evidence uses validated per-suite counts, retaining exact suite identities and all-passed/minimum checks while allowing new cases. Production source, browser tests, pins and lock remain identical to independently tested `6aea173`.

Independent source review `0de3b9ccd850176fd8d7f05fdc84a5ea8b64dcdd34c565112d6b68d671e07567` approves the tooling fix. Author pinned full check passes30suites270tests, numericexit0; added tooling tests account for the increase from267. Independent tooling report `bf175dfd0f581e982c3d2ea126c947b5d4d5521caee441df40af68246461b7e1` confirms actual package5files33tests, positive7/7/7 view cases and missing/short/pending/duplicate-suite/identity/count negative controls. An initial verifier7-to6 mutation was correctly admitted at the floor and then corrected to5; both outcomes are preserved. Repeated human assertion labels within a passing suite are diagnostic, not a newly required uniqueness constraint. The independent full267 production gate remains separate; no independent full270 rerun is claimed. Earlier final-head hosted run36239527196 passed both OSes267/267, artifacts and syntheticb44fd3b exactcaa2/7f96parents/equaltree5515de04 checked; that precedes this tooling correction.

**V1 closure:** [PR #38](https://github.com/GhostFlying/cove/pull/38) rebase-merged at `489f3466d55009b2730a8fa70b86c54e44abbb3c`; all 40 mappings, repaired review threads, final-main macOS/Linux [CI `36240528166`](https://github.com/GhostFlying/cove/actions/runs/36240528166) (30 suites / 270 tests on each OS) and both artifact sets were verified. [Issue #20](https://github.com/GhostFlying/cove/issues/20) is closed. The detailed pre-merge evidence above remains historical; exact maps and failures remain in [execution record](tasks/m0-execution.md), [view results](tasks/m0-terminal-view-results.md), [registration results](tasks/m0-terminal-view-registration-results.md) and PR #38.

| Task                 | Current owner and next gate                                                                                                                                                                                                                                                                                                           |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V1 #20               | Complete. External warmpool TraeX GPT-5.6 Sol high session `01a0dd11-2749-7580-9d9c-8117185ecace` and its clean checkout remain parked at author `b79f271`; no warm hit is claimed.                                                                                                                                                   |
| W1 #16               | QI is included in accepted main `77de10a`; N1's corrected native source is draft PR #42 and still held on independent resource attribution/review/CI. The earlier `a5f874f` qualification branch is preserved as history.                                                                                                             |
| P3 #19               | Original R1 functional `c33f7f1` is independently reviewed and verified at the compiled protocol boundary. Its six author commits are replayed on `p/luchengxuan/m0-19-routed-protocol-integration` from accepted main `77de10a`; combined checks, new review and hosted CI are pending. No runtime subscription barrier has shipped. |
| Independent reviewer | Separate R1 integration review/test remains required; the original author source review and compiled test do not certify the rebased combination.                                                                                                                                                                                     |
| Dispatch/integration | Root schedules only. `/root/m0_t1_impl`, GPT-6 Sol high, owns the R1 integration checkout and primary N1 registration; the branches and installations remain separate.                                                                                                                                                                |

W1's Astra plan is [worker execution plan](tasks/m0-worker-execution-plan.md), source plan SHA256 `2fd979be4f78bacbecd3fdaaae279d20ff3499712c69b20e28fdca595299480c`, independent Sol review `c89d926b79311576798255bd0470ab328b2cd85b4b021d52c48b5ea5a84fb14c`. Only its first native-qualification slice was allocated. At the earlier checkpoint, author qualification commits were unintegrated at `a5f874f06ff02f5ad5771d26a2618fad7f3e8233`, including nonce-bound/fail-closed process cleanup and asynchronous non-Buffer fault handling. Separate source review `2409aa48c398c6b57e328f90173f08e85a9dd00417070401edc401b6d2cdafda` closes those harness findings without tests. Author corrected scoped37 and historical full271 at9864cd2 remain distinct. Independent bounded harness report `546cb58af1417ddc1f86df0afb4e518a6d1939fbd008e7f8977788a4af1b2b58` now passes worker3 plus separate public raw-byte/nonce/fault/disposal controls, with recorded child PIDs absent. Initial CRLF oracle corrections are preserved; private PID identity controls remain authored-runtime and source-review evidence. No independent full271 or Linux/devbox qualification is claimed.

The W1 blocker is independently established: unpatched node-pty1.1.0 retained a full1MiB of copied input after sixteen64KiB void writes to a live nonreading raw child, with no public settlement/error contract. Pending versus allocated bytes differ; EIO queue clearing after exit is not delivery. Independent report `65669cc5c66e7e9877bb56f21130595f618cf19f0a046efce78488629450f2` and [native input decision](tasks/m0-worker-native-input-decision.md) retain exact source/measurement/Orca comparison. A narrow maintained patch is recommended; guarded private writer/lifecycle integration or alternate-backend research remain options. The user subsequently selected the narrow maintained patch; its accepted scope is recorded in the latest checkpoint and decision document. Alternative private overrides/backend changes are not selected, and patch correctness is not yet qualified.

P3's earlier external-event routing alternatives are superseded by the reviewed [routed stable-recovery plan](tasks/m0-routed-stable-recovery-contract-plan.md). R1 contract source is independently reviewed and compiled-tested; its accepted-main integration still needs combined testing, review and hosted CI. Actual Codex/TraeX/Claude Code workflows remain H1/C3. Devbox SSH remains blocked and must not be retried until the user restores access; C5/X1 still require 100 sustained real PTYs. M0 exit/M1 entry remain unapproved.

## Prior task records

C1 [PR #29](https://github.com/GhostFlying/cove/pull/29) is merged. Current completed baseline is `72c15b30c20d8d8228c4988517786c109197a6c5`; [final main CI](https://github.com/GhostFlying/cove/actions/runs/36163798332) passed both OSes. Independently tested/reviewed source tree equals main; exact maps, counterfactual tests and final artifact readback are in PR #29 and Issue #9. C1 gates the actual 10 tooling tests; it makes no app/PTY claim. Its clean implementation/verification worktrees were removed.

[M0 execution record](tasks/m0-execution.md) tracks allocation and the milestone DAG owns prerequisite order. All 19 implementation tasks are authorized in scope; remaining tasks still wait for their individual dependencies.

B0 [PR #30](https://github.com/GhostFlying/cove/pull/30) is now merged at `69eb239ae7ca0394b09c6546079e03f9be69df6d`. Accepted source `95b51e3f353e6b1c1d54325db0f2d6b859bd1b34` has the identical tree. [Final main CI](https://github.com/GhostFlying/cove/actions/runs/36173098846) passed macOS/Linux, 6 suites / 25 tests each; both artifacts were downloaded and checked against the main SHA, exact toolchain/lock, native and browser evidence. The PR retains all 12 rebase mappings, independent reports, prior failures and resolutions. B0 supplies private compiled probes and candidate dependencies; recovery/query/reflow, production runtime and capacity remain unproven.

P1a [PR #31](https://github.com/GhostFlying/cove/pull/31) is merged at `eb3a0d50f553f0959a7606da584421ba683c4f1b`; accepted source `a73ca296b0bc5842145df124b0eeee1c280ad9bd` is tree-identical. [Final main CI](https://github.com/GhostFlying/cove/actions/runs/36177751993) passed macOS/Linux, 9 suites / 47 tests each; both artifacts were downloaded and checked. PR #31 retains all seven commit mappings and independent evidence. The provisional package supplies only bounded terminal/pipe bytes and identity/metadata validation, not production recovery or query behavior.

| Task                           | Owner and scope                                                                                    | State                                                                                                          |
| ------------------------------ | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Q1 #12                         | Separate Sol-high implementation, testing and review                                               | PR #32 and cleanup #34 merged; final-main artifacts verified                                                   |
| T1 #13                         | Separate Sol-high implementation, testing and review                                               | PR #33 merged at `81c0fa6`; final-main macOS/Linux artifacts verified, 136/136 each                            |
| P1b #14 plan                   | `/root/m0_p1b_plan`, GPT-6 Astra high; read-only source/Orca comparison                            | Plan committed at `421afcf`; separate Sol-high plan review approved dispatch                                   |
| P1b implementation             | `/root/m0_t1_impl`, GPT-6 Sol high; reuses clean prior T1 checkout                                 | `p/luchengxuan/m0-14-supported-protocol`; sole protocol/shared registration writer transferred from T1 to P1b  |
| Independent P1b testing/review | Existing separate Sol-high roles available                                                         | Independent final full gate passed 200/200 at `efa90f2`; separate Sol-high review approved source and fixtures |
| Integration                    | Coordinator primary checkout, `p/luchengxuan/m0-14-contract-integration`; coordination/GitHub only | Base final-main `81c0fa6`; preserve prior T1 branches/evidence                                                 |

P1a independent local TraeX GPT-5.6 Sol high through warmpool (session `01a0d9db-3585-77d2-be99-9ed1c406ba08`) verified the frozen source: 103 schema assertions, 1,670 final codec/composition/bounds assertions and full 47-test gate. Separate GPT-6 Sol high `/root/m0_p1a_review` approved exact source after a comment-only invariant clarification. Final report `/tmp/cove-m0-p1a-verification/final-report.md` has SHA256 `f537948949d733b003c014cb70507fbc305e002e130839dd1c42c7cef24b8f2e`; durable summary is in PR #31 / Issue #11. No warm-hit claim; agent review is not a second-account GitHub approval.

Q1 PR [#32](https://github.com/GhostFlying/cove/pull/32) rebase-merged all 16 atomic integration commits at `a111a7ca9821ef903ca108d69304600866c683a0`. Final source `70902c9c4a16b26a656eaffa3e096b6fda81218d` has the same tree (`bd908ac3a483b7f637e415825ce1e8319648c74d`), and every intermediate tree/message and single-parent mapping was verified. The PR retains exact maps and independent evidence. Required final PR CI run `36183614752` passed macOS/Linux, 58/58 each; both artifact sets were downloaded and checked. Final-main [run `36185518371`](https://github.com/GhostFlying/cove/actions/runs/36185518371) passed macOS/Linux, 58/58 each. Both downloaded artifacts match exact main SHA, pins/lock, native/browser evidence and complete Q1 inventory; Q1 is complete.

Independent Q1 Sol-high verifier `/root/m0_q1_verify` covered 42 query phase occurrences, 75 byte cuts, real browser keyboard/paste/mouse, mixed OSC, adapter missing-surface, malformed baseline, overlap, lifecycle and cleanup. All 21 owned browser PIDs/ports were closed; final full macOS gate passed 58/58. Report `/tmp/cove-m0-probes-verification/q1/70902c9/final-macos-report.md` SHA256 `cc698ee6e3a9aafdcc8dd22dd7d99ef0a29bd9c4efca09b07e2f5a917ac227a8`. Instrumented compiled-body and isolated adapter-transpilation challenges are distinguished from published-export/real-browser observations. Hosted Linux CI is separate evidence; no independent Linux execution is claimed or required for Q1. Separate Sol-high reviewer `/root/m0_q1_review` approved the exact candidate after the author fixed late-browser-error, preappend baseline-budget and UTF-8 result-budget findings; report SHA256 `c1c37add828886e91983f320ed0c998016c7a9cdda50db9383fddc264fc430dd`. This is not a second-account GitHub approval.

Q1 cleanup PR [#34](https://github.com/GhostFlying/cove/pull/34) is complete. The original draft T1 macOS run `36186368871` exposed a Q1 page-disposal timeout; its failure evidence remains. The fix divides the existing seven-second cleanup envelope between page disposal, graceful browser close, actual kill fallback and listener/evidence cleanup. It preserves B0's original limits and surfaces cleanup errors. Source `abdbe5bd176a61a622c200556179c7ac639bf6c8` maps to main `8e340c3be63c603815f371032d51e2250562b6df`, with both atomic commit trees/messages verified. Final-main [run `36189313254`](https://github.com/GhostFlying/cove/actions/runs/36189313254) passed macOS/Linux, 58/58 each; downloaded artifacts were checked. Independent delayed/hung real-browser cleanup cases and separate Sol-high review passed. Report hashes: independent `0312c59744440323733d64eab75af2c7ebf67df82427efcacd15e9bc8198f552`; review `2ead5788e90d4b46953299aaff34adf8870647796550b51420701fac1d1bdb36`. This fixes cleanup behavior without claiming to identify why the hosted disposal was slow.

T1's final finite [source-derived plan](tasks/m0-terminal-source-derived-plan.md) and one permitted construction correction are exhausted. Author source `38271bdac901dafd84a44f7f85d63d470b2fb4d1` integrates as `ab32c5557952aa7e7dfc50823c59fdfd1469a040` after rebasing onto cleanup main. The generator receives settled source and explicit budgets only; fixture hints, setup streams, literal repairs and receiver-difference search are excluded. It now derives the final-print witness and handles the 67-byte combining cluster and print→resize control. Author evidence includes 20 fresh checkpoint restorations across four 131,073-byte streams and a 120×40 / 1,000-history control. Payload accounting is not a measured peak-heap result. Required combined geometry cases still fail; green expected-mismatch assertions are not acceptance.

Independent Sol-high testing of `ab32c555` admitted all eight pre-frozen hidden variants plus two controls and exercised 23 fresh suffix branches. Five cases / 11 branches mismatch: four geometry variants and the combined geometry-plus-join variant, including missing hidden-normal R/T/K/X cells and an alternate foreground difference. Three join-state variants plus the two controls match (12 branches). Purity, determinism and supplied output bounds pass. The independent report is `/tmp/cove-m0-probes-verification/t1/source-derived/ab32c555-report.md`, SHA256 `9d9272aa2ac8edc28961f5eb376af7135f9aa59042cd80690b12ca67190463c4`. This is a failed candidate, not a proof that every public VT construction is impossible. [Draft CI `36189648260`](https://github.com/GhostFlying/cove/actions/runs/36189648260) passed 98/98 tests, 17 suites on each OS; its diagnostic G failures remain failures of recovery.

The reviewer found nonfinite caller limits could bypass budget comparisons. Author validation-only source `03c4889fa2953530b994cf85e1ae21894c85c230` integrates as `bbab3bd5db7951db6d9e55ca013eda92c05b87c0`, validating finite positive safe integers and fixed ceilings before source access. It does not change the frozen construction. Author scoped checks pass; Independent delta verification passed at exact `bbab3bd`: 65 malformed values rejected before any source read, valid/default and boundary controls passed. Report SHA256 `6de086081861080d3239effc7cf27c87d683b8f02d489bc1d04fb638e606810f`. [Code-head CI `36190498635`](https://github.com/GhostFlying/cove/actions/runs/36190498635) passed macOS/Linux 98/98, 17 suites; downloaded artifacts and synthetic merge `bae29f28b9b3dffa334511ebb75ce994ff62820f` parents/tree were verified. The geometry matrix was not rerun in the independent validation delta; its exact tested head remains `ab32c555`. Separate Sol-high review approved only the admission fix; report SHA256 `418ad5ddff468a26091af6aa13da52bcfedcb9d0b2d21d91708befba750bb329`. Missing R1/R2/R6/R8 coverage and resource qualification remain recorded in the spike; **T1 is not accepted and P1b remains blocked**.

The historical strict results above retain their original verdicts; the following practical contract is the active gate.

The user **approved pragmatic Orca-like first-release recovery on 2026-09-26**, then confirmed initial agent targets **Codex / TraeX / Claude Code**. [Terminal architecture §3.2.1](terminal-architecture.md#321-首发恢复精度与验收2026-09-26-用户已确认) is the current acceptance authority. The maintained receiver-installation API and inverse-reflow generator are paused. Saved SGR/charset and complex retained off-grid/physical-row equivalence become documented non-gating diagnostics; prior failures remain failures of that stronger profile. Budgeted normal+alternate, ordinary visible/current/input state and common continuation remain required, as do parser/UTF-8, bounded resources, query isolation and exact ordering. True authoritative resize may rely on application repaint; no fake resize for same-grid recovery and no observer-driven authority resize. Do not copy Orca mobile's initial normal-prefix omission.

Astra-high `/root/m0_p1a_plan` supplied the [replacement executable T1 plan](tasks/m0-terminal-pragmatic-plan.md), committed as `04520a9` after accepted scope `9c46ee5`. The plan is in the repository before implementation. T1's deterministic practical-profile gate precedes P1b. Small real Codex/TraeX/Claude Code workflows are qualified after H1/C3 supplies real PTY plus browser views, not falsely claimed by synthetic replay or made a circular prerequisite of T1. Native mobile/desktop product validation remains later. Separate Sol-high `/root/m0_q1_verify` prepared strategy `/tmp/cove-m0-probes-verification/pragmatic-strategy.md` (SHA256 `9dc955cb0ad985f18d18b009b39a7c93e835aecce58cf786b93abf05007eadaa`) and observed Codex 0.155.1, TraeX 0.205.1 internal, Claude Code 2.1.220 through safe version/help calls only; `/root/m0_q1_review` approved the corrected decision/plan at `04520a9` without tests/edits (report SHA256 `ed73671c7b93a11656d0beb40c9993dc10b1bc7248625f2aa30dfbfc50e4810d`); the later implementation review is recorded below. T1 implementer used `p/luchengxuan/m0-13-pragmatic-recovery` from `04520a9`; pre-code execution entry source `a99dc76392404e58c72269cb708b7417b5232913` is integrated as `33ddaed`. Old author branch remains at `03c4889`; source/shared registration ownership remains unchanged. The latest decision authorizes implementation within this revised scope, not the installation API or a milestone transition.

The pragmatic candidate is now frozen: source `21addf1` mapped to root `d3e7214` (an authorized message-only Conventional Commit correction preserved the tree), source fix `cdef56f` to `72e8ad1`, explicit lint assertions `2f985b3` to `8614af0`, and final negative/bounds/inventory tests `d0dc78c923259e6150dfd647bd1d65c2b10bb3e0` to `61e1e1d5d5dee48d31985cbd6c8c0fcb1fa79d46`. The new compiled exports are `createLogicalGridCheckpoint` and `observeLogicalGrid`. Visible packed-color/overline/cursor-visibility and default blank-row REP defects were repaired rather than waived. Static test discovery was made explicit without lowering the gate. Author final macOS `pnpm check` passed 18 suites / 136 tests, including 37 practical tests and 180 distinct case IDs. Separate Sol-high review at `61e1e1d` closed the practical-path R8 negative/bounds gap; report SHA256 `ad6398150f41a5012842b110b13d57f159836a660078d2699788bb450cfdf46f`. This source approval is not a live-agent claim.

Independent practical strategy was finalized before candidate delivery: strategy SHA256 `df04c4c0837ca3391f7567d60b3832f452fc60652cde87ec31af290b5f856624`, fixtures `09271241e7d8f0a079ab9765f01e0d0284f31be1eb9cb94f739c5313c53b31df`, sentinels `db5d5d73bc0e16322651d2ada07754e012151c661057471d4babca193351010c`. The initial candidate's visible-color failures remain in independent report `b77cfb15bcd7816b7bbd69693fb1b6261f6882ad610dda5adf0deafb157dd202`. On corrected algorithm `8614af0`, all 38 fresh branches (16 families / 19 variants in two partitions), 20 fresh replacements and 12 focused controls passed. Two verifier assumptions were corrected and preserved in earlier driver outputs: pinned default emoji width was one, and the actual-resize receiver needed the source's new width. Frozen descriptors were unchanged. Final independent report at `61e1e1d` has SHA256 `a6de9ab4faa7910b320c904069b1343be5cdee08fe102e8ac64c11db75751bb3`. The implementation is byte-identical to `8614af0`; the verifier checked that equality, then passed scoped 75 tests and full 18 suites / 136 tests at the final head. No independent Linux execution or actual-agent turn is claimed. Code-head [CI `36227032318`](https://github.com/GhostFlying/cove/actions/runs/36227032318) passed macOS/Linux 136/136. Both downloaded artifacts match pins/lock, setup exits, complete inventory and all eight T1 reports, including 180 practical required IDs. Synthetic merge `d2f6e97eb0ca02341700648f618dc95c926a8615` has exact parents main `8e340c3` / head `61e1e1d` and identical tree `bd38d109fcbd76a2ca262f6b36d571b3e29a83ef`. Prior lint and static-discovery failures remain in runs `36226595320` / `36226656705`; the final pass did not lower the inventory. The practical T1 candidate meets this probe gate; P1b awaits PR #33 integration and final-main CI.

T1 PR [#33](https://github.com/GhostFlying/cove/pull/33) rebase-merged all 28 atomic commits at `81c0fa6c2b0660d8fa56259160281091a3eafe0d`. Final source `f127aca8b4be8f77b01147d2e1954aaa4c43c75f` has identical tree `a5de99a2bff8393ed7004df737becd2224fcdd1e`; every intermediate tree/message and single-parent mapping was verified and recorded in the PR. Independent final documentation-delta verification SHA256 is `1004fd3b55e256454bf10f3d0ae455040805395f5f1e728a751d8f6215a99118`; separate final review SHA256 is `6aabf13f6d417354a8078acd1468bcb3aab29d7c1a6899f6d614e1d8d9333baa`. Final-head run `36227341753` and [final-main run `36227595689`](https://github.com/GhostFlying/cove/actions/runs/36227595689) passed macOS/Linux, 18 suites / 136 tests each. Both final-main artifact sets match the exact SHA/pins/lock, native/browser evidence, full inventories and all eight T1 reports. **T1 is complete under the practical probe contract; P1b is released.** Historical failures and live-agent obligations remain unchanged.

P1b #14 planning was assigned to Astra-high `/root/m0_p1b_plan` from final main `81c0fa6`. The planner read current contracts and pinned Orca before producing the [P1b plan](tasks/m0-supported-protocol-plan.md), committed as `421afcf`. Separate read-only Sol-high plan review approved dispatch (report SHA256 `d1b8895e0706f1d63adde0580101cb8f14818f4bdbc96d3b36bc47ea99a705cc`). Independent twelve-family strategy was reconciled, SHA256 `b5dbe79dfda2e08fe58f9225152d142ca6542698a007b0f6093363b682941f60`. Existing Sol-high `/root/m0_t1_impl` then took P1b implementation and exclusive protocol/shared registration ownership; the prior T1 role finished. The existing implementation and verification checkouts were reused with explicit ownership transfer. Root retained coordinator-only writes. Final candidate acceptance is recorded below; do not resume the old recovery experiments.

P1b [PR #35](https://github.com/GhostFlying/cove/pull/35) initially contained the first coherent identity/error/profile/budget/terminal slice. Author entry `5233eb6` maps to root `2a647de`, source `e51b5a3` to `0f2cc0d`, corrections `7c8635b` / `42c979f` to `35cd1c4` / `9def320`; each source/root tree is identical. Independent checks found a valid nonpacked-chunk rejection and confirmed unsafe error tuples and mismatched control epochs; separate review also found transfer-run and impossible effective-credit gaps. These were fixed without changing the accepted contract. At `35cd1c4`, independent finite cases pass 8/8 and supplemental controls 5/5, with scoped protocol 37/37. The verifier's call-shape update for the newly explicit chunk/end `run` field is disclosed, while frozen bytes/expectations remain unchanged. At `9def320`, nine independent exact/below queue-budget boundaries pass. Reports: first correction SHA256 `0e2bee9005ff2d414862bcbc1573b13851b91295ff3dff00afbdbf08793fd572`, budget delta `25fe4dbe163ca6405b6f4632c36ea791372bf1876ea4c7b7c2f28548baca7c25`, separate source review `38b6d25ef6babf17bbb886f27ad98507e0748057750a649d54533f5fad25b1e7`. These first-slice findings are closed. The subsequent complete source and final verification status are recorded below; this partial evidence alone never accepted P1b.

P1b now has all ten new public contract exports plus the two retained provisional exports. Functional author `9c2e1bc` maps to root `9e15321`; final test-only correction `5fee899` maps to root `efa90f294a65168ea41cabb2648eb5bc17dcf004`. Frozen production code is unchanged by that correction. Pipe identity/epoch/result fixes, safe bootstrap errors, RPC method/instance binding and preallocated create receipts passed independent bounded checks. The accepted Origin clarification requires allowlisting every present Origin and the same secret-bearing bootstrap for every peer; absence is not authorization. No spoofable native/browser flag was added.

The final consumer fixtures cover bounded baseline/progress credit, ordering, optional-field compatibility and refused unnegotiated extensions. Independent review caught and corrected an extra contiguous-sequence restriction: focus/input accept monotonic newer values with gaps; duplicate focus is idempotent, input is never replayed, and ACKs return only recorded bytes. Uncertain input uses `inspect-run`, while uncertain create/stop uses `query-operation`. Fixture manifest SHA256 is `8d60932b86a0ac0ec0a56c202b1004e4e43dbae05ac66b1c838c1385885a39f6`. Author full gate at `9c2e1bc` passed 22 suites / 200 tests; final test-only delta passed scoped 110 tests. Independent report SHA256 `dd59cbb275a9bb3db42f0c6ef227461fbfa9c9bcb4f16acd8b127fd6d5b841c8` records the exact tested source, prior failures and test-driver corrections. Independent final full macOS gate at `efa90f2` passed 22 suites / 200 tests; separate Sol-high review approved source and fixtures (report SHA256 `4493ff082ad50dd106e305e7d951bab3b2d0c6824af8486152f890042f0fe682`). Code-head [CI `36231516307`](https://github.com/GhostFlying/cove/actions/runs/36231516307) passed macOS/Linux 200/200 each. Both artifact sets were downloaded and checked for exact pins/lock, native/browser evidence, full inventory and retained T1 diagnostics. Synthetic merge `acc7e3d25ff446702590dd249e7343c9023e5799` has exact parents main `81c0fa6` / head `efa90f2` and equal tree `586cfc14cd0d9297d7187dc5c1d6aa02ea89cd4f`. Author final results-only commit maps to root `6a24966`; final coordinator-document review and exact final-head/main CI remain before dependency release. See [author results](tasks/m0-supported-protocol-results.md). No production worker/server/controller/view, mixed runtime-build compatibility, actual agent workflow or devbox load acceptance is claimed.

Clean completed Q1 and cleanup checkouts were removed after their owners finished; refs and reports remain. Three task checkouts remain: coordinator, T1 author and independent verifier. Native roles are reused serially within the four-agent limit; five is the global agent/worktree ceiling. Recompute live counts before dispatch. No nested dispatch or delegation. The P1b implementer alone owns shared registration/dependencies until explicit downstream transfer. External TraeX session `01a0da05-4310-7340-a747-2235cbdf9d49` is stopped; no warm-hit claim. Pinned vendor reference via `/tmp/cove-m0-p1a-verification/terminal-reference-path.txt` is read-only research evidence.

Scoped P1a type limitation: pinned Zod's unused URL helper declarations do not compile in an unmodified strict ES-only consumer. Keep genuine Zod schemas/inference; only protocol-local declaration skipping is allowed, while verbatim Cove emitted declarations are checked as TypeScript source with a negative host-global control. Root settings remain strict. Do not hide this limitation or replace the schema API with duplicated handwritten types.

## Remote verification access

The read-only `ssh devbox` environment probe failed GSSAPI authentication. Local Kerberos inventory marked a ticket expired; a single noninteractive renewal failed with no matching TGT. Corp-auth doctor showed internal HTTPS reachable but does not manage this Kerberos root. No remote command ran, no service/config changed, and no secret was copied. The user has been asked asynchronously to restore the usual company SSH/Kerberos login; local implementation and GitHub Linux CI continue. This is an access blocker for later devbox verification, not a product decision or permission to waive the 100-real-PTY gate. Retry after the user reports restored access.

## Fixed execution boundaries

- M0 is a loopback experiment with per-launch credentials, instance-lifetime operation receipts, CLI and two browser terminal test views. Durable task/workspace/SQLite and pairing/remote deployment remain M1/M2.
- Recovery/query input probes precede final profile freeze. P2 owns runtime pool/refresh, W2 supplies worker status/preview, P3 owns controller and V1 display adapter. Shared manifests/lockfile/exports and CI each have one writer.
- F1 freezes a complete reference; E1 must make an actual useful compatible runtime change before independent C4b tests. Metadata-only differences do not qualify.
- C5 and final-main X1 retain 100 real PTYs concurrently producing sustained synthetic output on devbox. Idle PTYs/headless models do not qualify. The new three-agent workflow smoke belongs to H1/C3; 100-real-agent capacity, native mobile and long-duration qualification remain later.
- Required main ruleset `23994191`: linear history, PRs, strict `check (ubuntu-latest)` and `check (macos-latest)`, no force/deletion/bypass; only rebase & merge. Independently test/review each exact head, resolve concrete findings, record mapping and verify final main CI before releasing dependents. Agent review is not second-account GitHub approval.

## Source and durable references

[AGENTS.md](../AGENTS.md), [engineering](engineering-plan.md), [product](design.md), [server](server-architecture.md), [terminal](terminal-architecture.md), [protocol](relay-protocol.md), and [M0 plans](milestones/m0.md) remain authoritative. Write the task-specific implementation plan before coding; use atomic what/why commits and rationale comments. Keep execution results out of commit messages.

Orca research pinned fork `322c1839888f4a462e2d68839deafb1fe616c685`, upstream main `646e9a5b02514795af5139961ccca225dfa01b12`, and stable v1.4.211 `5534462b50c660888487a2108700d4cf284270db`; compare relevant implementation when deciding, without treating upstream as authority or modifying the user's Orca checkout.

Next: independent review and verification assess the corrected QI candidate `a1a6549`; the author macOS gate is 31 suites / 275 tests, not an acceptance verdict. R1 continues protocol-only in its assigned external checkout. Preserve W1 qualification evidence and the completed V1 record. N1 production work waits for QI acceptance and a registrar handoff. Devbox access awaits restored SSH/Kerberos login; the later 100-real-PTY gate remains required. M0 exit/M1 entry remain unapproved.
