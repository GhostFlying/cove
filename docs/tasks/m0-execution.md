# M0 execution coordination

- Authorization: user explicitly approved the merged M0 scope/DAG on 2026-09-26. No further entry confirmation is required within M0; M0 exit/M1 entry remain user decisions.
- Baseline: main `a902ee0bae0e6539afab6555c0585ff6745261d5`, PR #27, final macOS/Linux checks passed.
- Coordinator checkout: primary Cove repository; branch `p/luchengxuan/m0-9-ci-integration` for C1 integration.
- Coordinator writable scope: this execution record, `docs/handoff.md`, milestone and module-plan authorization/status; GitHub tracking. C1 implementation alone owns root tooling/CI/test changes and its task plan.
- DAG: [M0](../milestones/m0.md); 19 tasks, 22 edges. C1 #9 is initially ready; B0 #10 waits for C1 integrated main checks. Do not bypass contract/risk-probe gates to maximize concurrency.
- C1 implementer: `/root/m0_c1_impl`, GPT-6 Sol high, checkout `cove-worktrees/m0-ci-gates`, branch `p/luchengxuan/m0-9-ci-gates`, baseline above. Independent tester and Sol reviewer are assigned after a frozen implementation head; no recursive dispatch.
- Allocation: coordinator plus one active task agent, two task worktrees initially. Maximum five task agents/worktrees globally, with native limit four including coordinator. Local TraeX through warmpool is eligible for independent testing; warm hit is optional, not an admission gate.
- Checks: use exact Node/pnpm pins; root tooling changes require pnpm check; independent negative-path tests and separate Sol review; exact-head required macOS/Linux CI; rebase-only merge; tree/commit mapping and final main CI before dependent work.
- Constraints: preserve selected architecture and accepted devbox 100 synthetic real-PTY gate; no keeper, SQLite/task/product GUI or later milestone scope in C1. No existing services are restarted. Implementation/test logs contain only task-owned synthetic data.

## Entry checkpoint

The 2026-09-26 approval removes the common M0-entry blocker from implementation Issues #9–#26 and #28. Their individual dependency blockers remain. C1 is dispatched; all other implementation tasks wait for their prerequisite main SHAs. Product/architecture/scope/acceptance changes go to the user with evidence/options while independent ready work continues.

## C1 completion and B0 release

C1 PR #29 merged at `72c15b30c20d8d8228c4988517786c109197a6c5`; final main CI run 36163798332 passed macOS/Linux. Independent local TraeX GPT-5.6 Sol high via warmpool proved the final 10-test gate and counterfactual failures; a separate GPT-6 Sol high reviewer approved the exact source head. Full evidence and rebase mappings are in Issue #9 / PR #29. Clean delivered implementation/verification worktrees were removed; no application or devbox workload was run.

B0 #10 now starts from that baseline. Astra-high `/root/m0_b0_plan` owns only `docs/tasks/m0-package-plan.md` in `cove-worktrees/m0-package-plan`, branch `p/luchengxuan/m0-10-package-plan`. The coordinator owns this record and handoff on `p/luchengxuan/m0-10-package-integration`. Current allocation is coordinator plus one planner, two worktrees; recompute live counts before dispatch. Planning pins needed stable candidates and minimal executable probe boundaries; no code/config/install changes until the concrete scope is committed. The approved M0 DAG remains unchanged.

## B0 implementation dispatch

Astra-high plan `4d4e10142d3fd18a0df9e6436cc7e369a2717411` was integrated as `b1de6cb`; planning worktree was clean and removed. `/root/m0_b0_impl`, GPT-6 Sol high, owns the plan's explicitly listed manifests/lock/build/CI/probe/development-document paths in `cove-worktrees/m0-package-probes`, branch `p/luchengxuan/m0-10-package-probes`, base `b1de6cb`. Root owns only coordination records in the integration checkout. Independent testing and review are assigned after a frozen head; no concurrent root dependency writer. Two actual experimental packages are authorized, no production exports/placeholders. Native/browser smoke evidence remains pending.

B0 independent verification preparation uses local TraeX session `01a0d989-82a7-7b13-b6f7-2195da7e733a` through `warmpool run -- traex`; session metadata confirms `GPT-5.6-Sol` / high in the separate detached `cove-worktrees/m0-package-verify` checkout at `b1de6cb`. Preparation only reads committed inputs and writes a temporary acceptance matrix; actual tests wait for a frozen implementation head. No warm-hit claim. This adds one task agent/checkout: current allocation coordinator, implementer, independent verifier, three checkouts. Separate Sol-high code review remains to assign.

## B0 completion and P1a release

B0 PR #30 rebase-merged 12 atomic commits onto C1 main. Accepted source `95b51e3f353e6b1c1d54325db0f2d6b859bd1b34` equals final main `69eb239ae7ca0394b09c6546079e03f9be69df6d` by tree comparison; full mapping is in PR #30. Final main run 36173098846 passed macOS/Linux, 25/25 each; both artifacts were downloaded and checked for main SHA, exact pins/lock, setup exits, inventory, real native child exit/replay and Chromium revision/version/input.

Independent TraeX GPT-5.6 Sol high through warmpool (session `01a0d989-82a7-7b13-b6f7-2195da7e733a`) verified frozen final source and bounded counterexamples. Separate GPT-6 Sol high `/root/m0_b0_gate_audit` approved the exact source after a fresh B0 acceptance audit. Evidence preserves the original external-profile browser failure, native resize race and later hosted timeout-message mismatch; each resolution was tested/reviewed without dropping acceptance. All three GitHub review threads have evidence replies and are resolved. Details and source-to-main mapping live in PR #30 / Issue #10; final local report is `/tmp/cove-m0-b0-verification/final-accepted-report.md`. B0 makes no production recovery/query/reflow, capacity, mobile or remote claim.

Clean B0 implementation/verification worktrees were removed after task processes completed, while branches and reports remain. P1a #11 is released from its B0 dependency. Astra-high `/root/m0_p1a_plan` owns only `docs/tasks/m0-provisional-protocol-plan.md` in `cove-worktrees/m0-provisional-protocol-plan`, branch `p/luchengxuan/m0-11-protocol-plan`, base `69eb239`. Coordinator owns handoff/execution on `p/luchengxuan/m0-11-protocol-integration`. Current allocation is coordinator plus planner and two checkouts; implementation, independent tester and Sol reviewer await the plan. Root/lock/build/CI ownership will be explicitly handed to one writer, not left with stale B0 assignments. Q1/T1 still wait for P1a integrated main checks. No milestone transition is implied.

## P1a implementation dispatch

Astra-high plan `ef8fd5715257c3ce772ed44af141ab121220d239` integrated as `fd72b4130c2b8f5012b9946df7e4eb0e91c60cb1`; planning checkout was clean and removed. Coordinator checked scope and formatting. `/root/m0_p1a_impl`, GPT-6 Sol high, now owns `packages/protocol/**` and exactly the root/lock/test-registration/development paths in the plan, in `cove-worktrees/m0-provisional-protocol`, branch `p/luchengxuan/m0-11-protocol`, base `fd72b41`. This explicitly transfers former B0/C1 package/CI registration responsibility to the single P1a writer; there is no concurrent config writer. Coordinator retains only handoff/execution/GitHub ownership. Prefer two buildable atomic commits for useful schema/package registration and codec/composition. Independent testing/review follow frozen source. Allocation: coordinator plus implementer, two checkouts; no recursive dispatch. No production protocol/profile freeze or Q1/T1 release yet.

P1a independent verification preparation is assigned to local TraeX session `01a0d9db-3585-77d2-be99-9ed1c406ba08` through warmpool; session metadata confirms GPT-5.6 Sol / high in detached `cove-worktrees/m0-protocol-verify` at `fd72b41`. It reads the plan and prepares only `/tmp/cove-m0-p1a-verification/strategy.md`; actual installation/testing waits for a frozen head. No warm-hit claim. This brings allocation to coordinator, implementer and external verifier, three checkouts. Independent Sol review remains to dispatch. Preparation is not implementation acceptance.

## P1a completion and parallel Q1/T1 planning

P1a PR #31 rebase-merged seven commits. Accepted source `a73ca296b0bc5842145df124b0eeee1c280ad9bd` equals final main `eb3a0d50f553f0959a7606da584421ba683c4f1b`; mapping is in PR #31. Final main run 36177751993 passed macOS/Linux, 9 suites / 47 tests each, with both artifacts downloaded/read back. Independent TraeX GPT-5.6 Sol high session `01a0d9db-3585-77d2-be99-9ed1c406ba08` proved schema and final codec boundaries; separate Sol-high `/root/m0_p1a_review` approved the exact head. Final report/hash and limitations are in handoff / PR #31. Verifier fixture/orchestration mistakes are preserved and distinguished from product failures. Clean delivered worktrees were removed after processes completed; source refs and evidence remain.

Q1 #12 and T1 #13 are now ready from P1a. They run planning concurrently from `eb3a0d5`: `/root/m0_q1_plan` (GPT-6 Astra high) in `cove-worktrees/m0-client-query-plan`, branch `p/luchengxuan/m0-12-query-plan`, owns only `docs/tasks/m0-client-query-plan.md`; `/root/m0_t1_plan` (GPT-6 Astra high) in `cove-worktrees/m0-terminal-recovery-plan`, branch `p/luchengxuan/m0-13-recovery-plan`, owns only `docs/tasks/m0-terminal-recovery-plan.md`. Coordinator primary branch `p/luchengxuan/m0-12-probe-planning` owns coordination only. Planning does not mutate code/config/installations or run probes; pinned vendor copies with hash provenance are a read-only research aid. Actual allocation: coordinator plus two planners, three checkouts. No nested workers.

After both plans, reconcile query vocabulary and explicitly assign one writer for overlapping manifests/lock/build/inventory; package experiment writers stay disjoint. P1b remains blocked on required client query/input and server recovery/continuation evidence; failures cannot be waived as unsupported/unavailable. Ordinary implementation choices need no repeated approval; architecture/scope/acceptance changes go to the user with concrete cases. M0 exit/M1 entry remain unapproved.

## Q1 implementation release

Q1 Astra-high plan `159a8a6d061b2ef7903e249d67d4944a8bb866eb` integrated as `1468a47`. Clean planner checkout removed. `/root/m0_q1_impl`, GPT-6 Sol high, owns `cove-worktrees/m0-client-query`, branch `p/luchengxuan/m0-12-query-input`, base `1468a47`. Writable scope is the Q1 plan's browser probes/client fixtures/plan/results and its explicit manifest, lock, Node project reference, required-suite inventory, affected tooling tests and development documentation. This agent alone writes shared configuration until a recorded handoff; no absent T1 suite/export is registered. T1 planner still owns only its plan document. Coordinator retains handoff/execution/GitHub. Three active agents/checkouts including coordinator; no nested dispatch. Independent test/review follow frozen implementation. Query corpus names are reconciled with T1; P1a seq remains event identity, not byte offset.

## T1 parallel implementation release

T1 Astra-high plan `ed011fa974d2ccce224f983be48c57db3a6b9f8b` is integrated. `/root/m0_t1_impl`, GPT-6 Sol high, owns `cove-worktrees/m0-terminal-recovery`, branch `p/luchengxuan/m0-13-recovery`, at the integrated plan. Initially writable: engine probes/fixtures and T1 plan/results only. Q1 remains sole shared configuration writer. T1 can explore the candidate and author disjoint code immediately; its coherent registered commits wait for explicit transfer of shared ownership after Q1's first useful registration commit. Then incorporate the Q1 dependency revision before editing shared files, preserve its registrations, and record new base. No absent suites, empty exports or overlapping config edits. Clean T1 planning checkout was removed. Allocation: coordinator plus Q1/T1 implementers, three worktrees; independent testing/review not yet dispatched.

## Independent probe verification preparation

Native independent-verifier dispatch returned an agent-limit error; no native verifier started. The user-authorized local TraeX path supplies independent preparation instead: session `01a0da05-4310-7340-a747-2235cbdf9d49` through `warmpool run -- traex`, metadata verified as GPT-5.6-Sol/high, detached `cove-worktrees/m0-probes-verify` at `f9094a4`. It writes only `/tmp/cove-m0-probes-verification/strategy.md`, without installation/testing or implementation inspection until a frozen head is assigned. No warm-hit claim. Four active task agents/checkouts including coordinator and external verifier; Q1/T1 remain separate implementers. A separate Sol-high reviewer will be assigned after implementation, without concurrent over-allocation.

## Shared registration transfer from Q1 to T1

Q1 first coherent probe/registration source `1c7c65ee63a1b99a8d058e99be74366433d4634e` is frozen and integrated. Author reports macOS browser 11/11 and full 54/54; this is not independent acceptance. Q1 now writes only browser probes/client fixtures/Q1 plan/results. T1 explicitly receives exclusive ownership of `pnpm-lock.yaml`, `scripts/ci-test-gate.mjs`, affected gate/package-boundary tooling tests and `docs/development.md`, plus its engine manifest/project reference. It incorporates the immutable Q1 commit after checking local changes before registering actual T1 code. No concurrent shared writer; findings requiring root changes need a recorded handoff.

Independent preparation finished cleanly at `f9094a4` with no install/probe execution. Strategy `/tmp/cove-m0-probes-verification/strategy.md` SHA256 `1290f433411723646b2aa2ab63653359e198bf0cc984cd1a1ef222bc573e395f`; external session is idle until frozen-head execution. Current active allocation: coordinator plus two implementers; four checkouts, including idle verifier.

## Remote access observation and current recovery work

T1 incorporated Q1 source `1c7c65e` as `435615c`; its first coherent implementation source is `28e8c3d`, author full macOS gate 58/58. It remains incomplete/unaccepted; parser/query/bounds and continuous-printable preceding-state recovery are being expanded. This T1 implementation is not integrated into Q1 PR #32.

A read-only devbox environment probe failed before any remote command because GSSAPI authentication was rejected. Local Kerberos output marked expiry; `kinit -R` failed noninteractively with no matching TGT. No services/settings changed. Corp-auth diagnostics were used; Kerberos is outside its managed provider set. User was asked asynchronously to restore normal SSH login. Local/GitHub Linux work continues; required devbox verification is not waived or claimed.

## Retained-column recovery counterexample investigation

T1 reported a required same-grid continuation gap: alternate buffer initialized at 41 columns retains its off-grid final cell after narrowing to 40; a subsequent DCH can shift that cell back into view. A fresh 40-column stock VT baseline loses it, so a later-resize-only fallback is insufficient. Required semantics remain unchanged; no dependency patch or unavailable waiver is authorized.

The implementer has a one-case candidate using public retained cells, receiver initialization at storage width, VT replay and an ordered resize to logical width; this is narrow feasibility evidence, not general recovery acceptance. `/root/m0_p1a_plan` is reassigned to bounded Astra-high read-only options analysis, output `/tmp/cove-m0-probes-planning/retained-columns-options.md`. Independent TraeX session `01a0da05-4310-7340-a747-2235cbdf9d49` resumes only for a bounded original-engine reproduction in its existing `f9094a4` checkout; outputs under `/tmp/cove-m0-probes-verification/retained-columns/`. It may frozen-install its own checkout, but does not inspect uncommitted implementation or run the full Q1/T1 matrix.

Allocation is now coordinator, two implementers, Astra analyst and external verifier: five active task agents, four native including coordinator, four worktrees. No further worker dispatch until a role completes. Routine probe work continues; only evidence-backed accepted-contract changes go to the user.

## Q1 final source and independent review release

Q1 author froze clean source `d538a752c98804d652ecc7ed89ef419753835a79` (code `1c7c65e`, `ff88490`; result document `d538a75`). Root integrated final code/results as `0b22a9b` / `4d620e8`. Author scoped browser suite is 15/15 (Q1 10, B0 5); final full independent/CI evidence remains pending. T1 shared writer is asked for a separate atomic Q1 inventory floor 6-to-10 update; do not mix engine implementation into Q1 PR #32.

Separate GPT-6 Sol high `/root/m0_q1_review` now owns read-only review in the completed Q1 checkout `cove-worktrees/m0-client-query` at `d538a75`, with no source changes, tests or installation. It reviews implementation independently and will receive the final integration SHA for the inventory delta. The prior implementer has completed; fixes are relayed back after review findings, never written by the reviewer. Astra retained-column analysis completed with a bounded receiver geometry recommendation; no architecture change is preapproved. External verifier finishes its original-engine reproduction, then waits for Q1 exact-head execution. Current active allocation is coordinator, T1 implementer, Q1 reviewer and external verifier, four agents/four checkouts.

Q1 isolated inventory update source `50e7137` is integrated without any T1 engine code. External retained-column reproduction finished (report hash `93f72083935faeab70d8661a89559177b284639fc8369b6245d5098e7e664319`); it confirms topology loss and the minimal geometry control only, not full recovery. Native slot is now available, so independent Q1 execution is assigned to `/root/m0_q1_verify`, GPT-6 Sol high, in the existing clean `cove-worktrees/m0-probes-verify` checkout. The external session is stopped; this is an explicit verification ownership transfer, not concurrent use. The native verifier inherits only independent strategy/evidence and will receive a frozen candidate SHA; no implementation authorship. Current active roles: coordinator, T1 implementer, Q1 reviewer and Q1 verifier; four native agents/four worktrees.

## Q1 completion and bounded T1 continuation

Q1 PR #32 rebase-merged 16 atomic commits at `a111a7ca9821ef903ca108d69304600866c683a0`; final source `70902c9` and every rebased intermediate tree/message match. Full source-to-main mapping is retained in PR #32. Independent Sol-high macOS testing covered the committed query/input, baseline, adapter-failure and cleanup cases; separate Sol-high review approved the exact final source after three fixes. Final main run `36185518371` passed both OSes, 58/58 tests each; downloaded artifacts were checked against main SHA, pinned toolchain/lock, native/browser and complete Q1 evidence. Independent-script Linux execution was not claimed or required. Q1 is complete; it does not release P1b while T1 remains open.

T1 author frozen experiment `7d5877e` plus Q1 fix `ae3a07d` passed the full macOS 82-test gate but retained ordinary required failures. Astra-high read-only analysis supplied one bounded J/G continuation; implementer committed plan `3368457` before code. J source `fb8f824` reports 20 real checkpoint replacements and finite suffix cases, but requires fixture-authored witness/location and rejects a 67-byte combining cluster under its 64-byte witness cap; online generation remains unproved. G gets one combined geometry attempt plus one divergence-driven correction, then freezes. No source semantics, patch/pin, private snapshot wire or acceptance changes are preapproved.

Current writer `/root/m0_t1_impl` (Sol high) exclusively owns engine/T1 probes/docs plus previously enumerated shared registrations. `/root/m0_q1_verify` (Sol high) reuses its clean completed Q1 checkout for independent T1 strategy preparation only; execution waits for a frozen T1 head. Root uses `p/luchengxuan/m0-13-recovery-integration` from final Q1 main for coordination. Clean Q1 implementation/review checkout removed after completion, refs/reports retained. Three active roles/three checkouts; no nested agents or delegation. Devbox auth still awaits user login restoration, without waiving the later real-PTY gate.

## Source-derived recovery and cleanup follow-up allocation

The final bounded T1 source `0d3f6869c7d287df947c8160c8324c0c3487ca3e` includes G `b5fe64d`, fixed physical tail storage `e13f793` and private-read package guard `0d3f686`. Author macOS full gate passed 92/92 across 16 suites. Root integration is `bc21b81`; code/config match the author, while coordination/analysis docs differ. Separate Sol-high review identified the storage/guard bugs and the remaining fixture matrix. Independent Sol-high verifier now executes bounded diagnostics in its own clean frozen `bc21b81` checkout; expected mismatch cases and authored witnesses remain unaccepted.

Astra-high analysis supplied one final finite source-only generator experiment: fixed captured-state rules, no fixture hints/setup streams/literal repair/receiver search, at most one generally applicable correction, then freeze. The existing T1 Sol-high writer owns the plan and implementation from exact `0d3f686`, committing plan before code. Independent verifier prepared eight hidden variants plus two mandatory controls before the generator: fixtures SHA256 `408347e6a03b616ab83b86c903931ecff7706b13c9781abb3dab5dfa24c9e0c0`, sentinels `33406dcbdde3c04113c5f100f9388b3faa24fb0c4d6f4265132663ba758cd514`. Generator gets settled source and profile/budgets only. No patch or weaker semantics is approved.

Initial draft PR #33 CI `36186368871` passed Linux but failed macOS on Q1 page disposal timeout; 85/86 tests passed. Logs/artifacts retained; the 500 ms per-page allowance is observed, not evidence of host contention. Separate Sol-high `/root/m0_q1_impl` owns bounded browser-cleanup diagnosis/fix and task docs in new `cove-worktrees/m0-browser-cleanup`, branch `p/luchengxuan/m0-12-cleanup-deadline`, base `a111a7c`. Existing Q1 Astra plan governs; task-specific plan precedes code. It may not change root registrations/lock (T1 remains sole writer), suppress timeouts or silently retry. Independent verification/review follow. Four active roles/four worktrees including root; no nested dispatch.

## Source-derived experiment frozen; maintenance decision boundary

Q1 cleanup PR #34 rebase-merged source `abdbe5bd176a61a622c200556179c7ac639bf6c8` as main `8e340c3be63c603815f371032d51e2250562b6df`. Its two intermediate trees/messages were checked, separate Sol-high testing/review passed, and final-main run `36189313254` passed macOS/Linux 58/58 with both artifact sets read back. The existing seven-second cleanup budget now reserves real browser-kill time; B0 limits and visible error reporting are preserved. The clean cleanup checkout was removed. Root rebased the T1 feature branch onto this main using an exact feature-branch lease; main and author branches were not rewritten. Eleven replayed patches/messages were byte-identical; mapping is `/tmp/cove-m0-probes-verification/t1-cleanup-base-map.json`.

The committed source-derived plan `95709c9`, first algorithm `4dae766` and sole construction correction `38271bd` map to root `ed998a9`, `e862dc8` and `ab32c555`. The algorithm allowance is exhausted. Independent Sol-high verifier tested the frozen eight hidden variants plus two controls, whose precommitted fixture/sentinel hashes remain unchanged: 10 admitted cases, 23 fresh branches, 12 matches and 11 mismatches across five cases. Four geometry variants and the combined geometry/join case lose hidden normal content or alternate style. J-only variants, the 67-byte cluster and print→resize controls pass. Report `/tmp/cove-m0-probes-verification/t1/source-derived/ab32c555-report.md` SHA256 `9d9272aa2ac8edc28961f5eb376af7135f9aa59042cd80690b12ca67190463c4`; the harness received no hidden hints. No full gate was repeated after decisive independent failures. Hosted draft run `36189648260` passed 98/98 diagnostic tests, 17 suites on each OS; expected mismatch assertions do not confer recovery acceptance.

Separate Sol-high review found one limit-admission bug. The existing T1 writer's atomic validation-only fix `03c4889fa2953530b994cf85e1ae21894c85c230` is integrated as `bbab3bd5db7951db6d9e55ca013eda92c05b87c0`. It validates limits before source capture and corrects suite-count prose; no construction rule changed. Independent verifier owns delta tests in its existing checkout; separate reviewer owns read-only delta review. Their reports and final exact-head CI remain pending. Shared-file ownership stays with T1; root owns coordination and integration of Astra's decision analysis.

Astra-high `/root/m0_p1a_plan` prepared a bounded maintained-API alternative, grounded in pinned xterm and Orca stable `5534462b50c660888487a2108700d4cf284270db`. The proposal isolates alternate staging columns during installation on a fresh inert receiver, preserving hidden normal geometry and ordinary live behavior. It requires reproducible browser/headless adaptation and neutral capability negotiation; it is not an arbitrary grid/private-state wire or an already proven recovery solution. Further public inverse-reflow effort and weaker recovery are alternative user choices. Off-grid private diagnostic state must be assessed against observable accepted semantics before adding a metadata API. No option is adopted yet. The coordinator must route this concrete maintenance decision to the user; PR #33 remains draft/unaccepted and P1b remains blocked.

The bounded Astra clarification excludes a second tab-stop API: inspected pinned tab consumers reveal no identified same-grid observation of off-grid keys. They can affect later resize, so omitted keys imply insufficient resize context and require an authoritative new-size baseline, including row-only changes. This is the already accepted fallback, not a waiver of visible tabs or public cell/style failures. The [decision brief](m0-terminal-restoration-api-decision.md) records exact source surfaces, Orca comparison, maintenance ownership, excluded changes and remaining proof obligations. User selection is pending. Separate Sol-high review approved only `bbab3bd`'s admission delta (report hash `418ad5ddff468a26091af6aa13da52bcfedcb9d0b2d21d91708befba750bb329`); independent delta execution and final CI remain pending.

Final validation-only delta passed independent Sol-high execution at exact `bbab3bd5db7951db6d9e55ca013eda92c05b87c0`: 65 invalid values rejected before source reads, valid defaults and tested exact/below limits preserved. Report SHA256 `6de086081861080d3239effc7cf27c87d683b8f02d489bc1d04fb638e606810f`. Verifier's first script had its own pre/post-REP threshold mistake, corrected without candidate changes and disclosed in the report. This delta did not rerun hidden geometry or the full local gate. Code-head hosted run `36190498635` passed both OSes, 98/98 and 17 suites; downloaded environment, setup exits, lock, inventories and all seven T1 reports were checked. Synthetic merge `bae29f28b9b3dffa334511ebb75ce994ff62820f` has exact parents main `8e340c3` and code head `bbab3bd`, and source tree `374bba489589f49bcca14be366a87ad858c10005`. Its four authored G mismatches remain explicit. Earlier construction-head synthetic merge `c83052c1e2bb646553125436c6c49289f9f3127d` also had exact main/head parents and tree equality verified. Subsequent coordinator commits are documentation only. T1 remains unaccepted; user maintenance selection is pending.

## User-approved practical recovery scope

On 2026-09-26 the user approved the coordinator's revised recommendation: follow a pragmatic Orca-like recovery profile, pause installation-API and inverse-reflow work, and retain complete-state counterexamples as explicit non-gating diagnostics. The user separately selected **Codex / TraeX / Claude Code** as initial actual-agent targets. Canonical scope is terminal architecture §3.2.1; parent plans and historical experiment documents link the override. General visible/input continuity, budgeted normal+alternate, server-only replies, ordering/parser safety and boundedness remain gates. No mobile normal-prefix deletion or fake same-grid resize is adopted.

Astra-high `/root/m0_p1a_plan` prepares the revised executable T1 plan in a temporary owned file for coordinator integration; no implementation before that commit. Separate Sol-high verifier prepares independent strategy and read-only safe CLI availability/launch inventory, with no live agent invocation yet. Separate Sol-high reviewer audits the coordinator's decision documentation without editing or running tests. Existing T1 writer waits for the revised plan and remains the sole shared configuration writer. Current checkouts remain coordinator, T1 and verifier; active roles root/planner/verifier/reviewer fit the native four-agent ceiling. No nested allocation. Small actual-agent workflow evidence belongs after H1/C3 dependencies exist; it must not become a circular T1 prerequisite or be claimed from byte replay alone. Native clients and 100-real-agent capacity remain later milestones, while M0 C5/X1 still require 100 simultaneously outputting real PTYs on devbox.

Accepted contract `9c46ee5` and Astra implementation plan `04520a9` are now committed before code. Initial Sol-high decision review found stale active CI and terminal-plan wording; those rows were updated to the practical oracle and explicit three-agent live gate, with deterministic public CI and separately reported credential-dependent live evidence. Reviewer checks the corrected delta and plan. Independent strategy hash is `9dc955cb0ad985f18d18b009b39a7c93e835aecce58cf786b93abf05007eadaa`; read-only version/help inventory observed Codex 0.155.1, TraeX 0.205.1 internal, Claude Code 2.1.220. No agent turns were executed. Issues #13/#21/#22 now describe the approved scope and live-workflow ownership.

Corrected decision/plan review at exact `04520a9a54e195fe1fa9f29da6980534297b4c4b` closed both wording findings and found no dispatch blocker (report SHA256 `ed73671c7b93a11656d0beb40c9993dc10b1bc7248625f2aa30dfbfc50e4810d`). Existing Sol-high T1 writer verified its checkout clean, preserved old branch at `03c4889`, and created `p/luchengxuan/m0-13-pragmatic-recovery` from `04520a9`. Pre-code execution entry `a99dc76392404e58c72269cb708b7417b5232913` integrated as `33ddaed`. Implementation is active; independent tester freezes its practical source/control descriptors before receiving a candidate. Root/planner-review/test roles have not authored implementation.

## Practical T1 source acceptance and integration handoff

The user-selected targets Codex / TraeX / Claude Code remain required at H1/C3/X1 after the actual PTY/browser chain exists. The T1 candidate is now source-approved under `pragmatic-logical-grid-v1`; no real-agent workflow is claimed. Author source `d0dc78c923259e6150dfd647bd1d65c2b10bb3e0` maps to integration `61e1e1d5d5dee48d31985cbd6c8c0fcb1fa79d46`. Earlier author/root maps are `21addf1` → `d3e7214`, `cdef56f` → `72e8ad1`, `2f985b3` → `8614af0`. The first root commit message was corrected with author approval before push; its code tree equals the initial `0116b0a` integration. Final source differs from author only in coordinator handoff/execution documents.

Separate Sol-high implementation, testing and review remain distinct. Independent testing passed 38 fresh practical branches, 20 independently validated checkpoint replacements and 12 focused controls on code-identical `8614af0`, then scoped 75 and full 136 tests at exact `61e1e1d`. Independent report SHA256 `a6de9ab4faa7910b320c904069b1343be5cdee08fe102e8ac64c11db75751bb3` retains the two corrected verifier assumptions and original strict failures. Separate read-only review closed color/overline/cursor-observation defects and the R8 practical-negative gap; report SHA256 `ad6398150f41a5012842b110b13d57f159836a660078d2699788bb450cfdf46f` approves source/matrix with no remaining concrete finding. No dependency patch, source mutation or acceptance waiver was used for these required cases.

Code-head hosted run `36227032318` passed macOS/Linux 18 suites / 136 tests. Both artifact sets were downloaded and checked against environment, lock/pins, setup exits, inventory and all eight T1 reports. Required pragmatic evidence has 180 distinct IDs; historical source-derived G divergences remain diagnostic. Synthetic merge `d2f6e97eb0ca02341700648f618dc95c926a8615` has parents `8e340c3` / `61e1e1d`, with tree `bd38d109fcbd76a2ca262f6b36d571b3e29a83ef` equal to source. Runs `36226595320` / `36226656705` retain earlier lint/discovery failures; static declarations were fixed without weakening suite minima.

T1 author is frozen and idle; verifier checkout remains clean at `61e1e1d`. Root now owns only final evidence/coordination documentation, PR #33 integration and exact-head/final-main checks. This handoff clarifies proportional result provenance through IDs, assertions, bounded logs and shared environment evidence; it changes no required case. Review the documentation-only delta before gated rebase merge. P1b #14 is released only after that merge and final-main checks. Three checkouts remain; no nested worker or live agent is running. Devbox authentication is still an external blocker for its later load gate; M0 exit/M1 entry remain user decisions.

## T1 completion and P1b planning allocation

PR #33 rebase-merged 28 atomic commits at `81c0fa6c2b0660d8fa56259160281091a3eafe0d`. Every original/rebased intermediate tree and full message matches; all have one parent. Source `f127aca` and main tree are identical. Final PR run `36227341753` and final-main run `36227595689` passed both OSes, 136/136 across 18 suites; downloaded final-main artifacts were verified against exact main/pins/lock, native/browser evidence, inventories and all eight T1 reports. Final independent report hash `1004fd3b55e256454bf10f3d0ae455040805395f5f1e728a751d8f6215a99118`; final review hash `6aabf13f6d417354a8078acd1468bcb3aab29d7c1a6899f6d614e1d8d9333baa`. Full mapping is retained in PR #33. T1 #13 is complete under the approved pragmatic contract.

P1b #14 prerequisites P1a/Q1/T1 are integrated and checked. Planner `/root/m0_p1b_plan` is GPT-6 Astra high, reads exact main `81c0fa6` and pinned Orca stable `5534462`, and writes only its temporary task plan for coordinator integration. It must enumerate concrete contract surfaces, accepted limitations, independent acceptance, atomic sequence and downstream DAG/file ownership before implementation. Root branch is now `p/luchengxuan/m0-14-contract-integration` from that main. The T1 author and verifier are idle; their clean checkouts/refs remain available for explicit P1b transfer. Two active roles and three checkouts at this allocation; no nested dispatch, no live agents, no extra worktree. Shared protocol/registration writer transfer will be recorded with the committed P1b plan. The user selected Codex/TraeX/Claude Code; actual chain qualification remains H1/C3/X1, and no milestone transition is implied.

## P1b implementation ownership transfer

Astra-high P1b plan is committed at `421afcf6a439c4048495737f1d79e9aab2cfd479` as `docs/tasks/m0-supported-protocol-plan.md`. Separate Sol-high read-only plan review approved dispatch, report SHA256 `d1b8895e0706f1d63adde0580101cb8f14818f4bdbc96d3b36bc47ea99a705cc`; independent twelve-family strategy reconciled to the plan is SHA256 `b5dbe79dfda2e08fe58f9225152d142ca6542698a007b0f6093363b682941f60`. No concrete user decision or scope blocker remains.

Implementation transfers to existing GPT-6 Sol high `/root/m0_t1_impl`, reusing clean `cove-worktrees/m0-terminal-recovery` on new `p/luchengxuan/m0-14-supported-protocol` from the coordinator-provided integrated allocation SHA. It verifies clean/process ownership, preserves old T1 refs, and records exact base before editing. The plan's writable paths apply; **sole protocol and shared registration ownership is explicitly transferred from completed T1 to P1b**. No parallel dependency/lock/registration writer. The user identity and atomic what/why commits apply. Root owns coordination/GitHub; independent tester `/root/m0_q1_verify` and separate read-only reviewer `/root/m0_q1_review` remain distinct and wait for coherent source. No nested agents, no deferred runtime/client implementation and no live-agent/devbox execution in P1b. Root plus implementer are active at dispatch, three retained checkouts; testers/reviewers activate as frozen evidence becomes available, never beyond native/global limits.

## P1b first coherent slice and bounded corrections

Implementer entry `5233eb6` maps to root `2a647de`; first functional source `e51b5a3` maps to `0f2cc0d`, exposing identity/errors/profile/budgets/terminal while leaving provisional exports intact. Draft PR #35 is attached. Independent pre-candidate sentinels SHA256 `d24c28d4c2af87c8929e268ca9caa52708871f2e72c3c269b80c6349837b9e55` caught valid nonpacked chunking rejected by an unstated maximally-packed requirement. Separate review found arbitrary public error messages/actions, wrong control epoch correlation, stripped outer transfer run, and insufficient effective credit. These are implementation defects, not user-scope decisions. Preserve original failing reports; green authored CI alone did not qualify the slice.

Atomic corrections `7c8635b` / `42c979f` map to `35cd1c4` / `9def320`, with source/root tree equality checked. Independent first-slice repair checks pass 8/8 plus 5/5 supplemental, and scoped protocol tests 37/37. The verifier corrected its call shape to include the now-explicit same frozen run on chunks/end, retaining its initial 6/8 output and unchanged data/expected outcomes. Independent final budget delta passes nine exact/below/default controls at `9def320`; no unchanged whole matrix or full root gate was repeated. Separate read-only review closes all four findings. Report hashes are recorded in the current handoff and PR. Pipe/runtime/bootstrap/RPC/view and full compiled-fixture acceptance remain pending; author owns their continuation, tester/reviewer await the next coherent source. Four roles were active during review, within the native limit; no extra checkouts or nested workers.

## P1b complete candidate and final bounded corrections

The author completed pipe/runtime, bootstrap/view and RPC/receipt contracts in atomic commits; root integrated each frozen source, preserving sole-writer ownership. Independent repaired-slice reports: pipe SHA256 `2670413423336844525ff2bfc78a4f30845ed0b8efb29a0712cc07061dd87a71` (6/6 plus 4/4); bootstrap `89700b86df46af1f7f5ed778e784bcda6915a03d819328dfd4efa34c55c56dbd` (6/6 plus 4/4); RPC receipts `72694e8244d156b9e2e8469de8386468d2a1af7e5087677b62ffcf0cb1f17e32` (5/5, 4/4, 4/4, 3/3). Failed predecessor cases remain in the independent reports. Astra clarified absent-Origin bootstrap without adding a client-supplied trust label. The inherited Zod declaration limitation retains its scoped guard and negative control.

Final functional author `9c2e1bc` / root `9e15321` fixes uncertain input guidance, validates receipt/unknown-outcome examples and completes finite fixture oracles. Author full macOS gate passed 22 suites / 200 tests. Independent review then found the test ledgers incorrectly required consecutive focus/input sequence values. Test-only author `5fee899` / root `efa90f2` corrects them to the committed monotonic contract, preserving duplicate focus idempotence, no input replay and cumulative ACK accounting; scoped protocol/tooling 110/110 passed. Independent report SHA256 `dd59cbb275a9bb3db42f0c6ef227461fbfa9c9bcb4f16acd8b127fd6d5b841c8` records the exact tested source, prior failures and test-driver corrections. Independent tester `/root/m0_q1_verify` passed the final full macOS gate at `efa90f2`, 22 suites / 200 tests, in its clean own checkout; separate reviewer `/root/m0_q1_review` approved the final source and fixtures (SHA256 `4493ff082ad50dd106e305e7d951bab3b2d0c6824af8486152f890042f0fe682`). Root owns only integration, coordination docs and GitHub. Code-head run `36231516307` passed both OSes, 200/200 each, and both artifacts were read back. Synthetic merge `acc7e3d` has exact base/head parents and the same source tree. Final documentation evidence and exact final-head/main gates are still pending; downstream T2/P3/V1 remain held. Three checkouts, no nested workers or delegation, no new user decision.

## P1b completion and parallel terminal/client planning

P1b [PR #35](https://github.com/GhostFlying/cove/pull/35) is complete at main `2e8e2be7cce58399c8313c315cc6649285abfd54`. All 23 rebase mappings preserve intermediate trees/messages and single-parent history; final source `7fb2d09` has equal tree `85cf94327cfb0486f09d85ce1807e024d680a35c`. Independent final provenance report SHA256 `9c4f77318fd6cf45d1a2729d98cf88a4a7e9cbd8ead3732fa0b26c6f05bc5fb9` and separate Sol-high review `8510ce39fe712446281bf1e1c48cb25eb51b84ca739ed7ec0d3edc2171f4a300` approve the final head. [Final-main CI36232108362](https://github.com/GhostFlying/cove/actions/runs/36232108362) passed macOS/Linux, 22 suites / 200 tests each; both artifacts were downloaded and checked against main SHA, pins/lock, setup exits, inventories, native/browser evidence and all inherited T1 diagnostics. P1b is accepted and releases parallel T2/P3/V1 planning. Production runtime, actual-agent workflows and devbox load remain unproven.

Next allocation: three read-only module planners, each GPT-6 Astra high, for T2 #15 (`/root/m0_t2_plan`), P3 #19 (`/root/m0_p3_plan`) and V1 #20 (`/root/m0_v1_plan`). They read exact main `2e8e2be`, applicable design/parent plans and pinned Orca, and write only their own temporary task-plan files for root integration. No shared repo writes, code, dependency changes or nested agents. Plans define finite acceptance, dependency DAG, file ownership and shared-registration requests before implementation. Root retains integration/GitHub/coordination only. Existing completed author/verifier checkouts are available for explicit reuse; planning needs no new checkout. Three planners plus root fit four native slots; total checkouts remain three. Independent Sol-high plan review follows before implementation allocation. M0 exit/M1 entry remain unapproved.

## T2 implementation allocation and external routing decision

T2's executable [plan](m0-terminal-engine-plan.md) is committed at `226d8f4`; independent Sol-high plan review approves dispatch (report SHA256 `e635cab3e1028957b5417865e839f772db5eeaeb09c0e610071433d38b25c55a`). `/root/m0_t1_impl` now transfers from completed P1b to T2 in the existing clean `cove-worktrees/m0-terminal-recovery`, branch `p/luchengxuan/m0-15-terminal-engine`, base `226d8f4`. It owns the T2 source/tests/task plan/results plus the explicitly requested package export/build and root test registrations as sole shared-registration writer. No other implementer changes shared files. `/root/m0_q1_verify` transfers to independent T2 strategy and later testing in its existing own checkout; no candidate execution before a frozen source. `/root/m0_q1_review` remains independent, now reviewing the P3/V1 plans. That allocation used three checkouts and planned four native roles; the later external V1 assignment and current counts supersede it. No nested workers were allocated.

At that allocation, P3/V1 plans had been committed at `72bf975` / `560d6e3` and awaited separate plan review; the completed review and current assignment are recorded below. P3 identified an existing external-event routing ambiguity: run-scoped events alone cannot distinguish same-run subscriptions or old versus replacement delivery. The user has an asynchronous decision pending: recommended external subscription-bound delivery wrapper (internal RunEvent unchanged), versus isolated view connections with reconnect or an explicit old-queue barrier. The wrapper would be an incompatible external protocol change requiring explicit version treatment; no choice is adopted yet. P3 implementation is held. T2 and V1 interfaces are unaffected; independent work continues. Root must record the user choice and accepted exact contract evidence before P3 dispatch.

## Parallel adapter implementation allocation

All three task plans have separate Sol-high review. Final plan-review report SHA256 is `51b4e70df3702b1cf6030053d68703f2d6aae58c453b5c0ea17a7e1b0049d578`: T2 and V1 are approved for allocation; P3 is a sound conditional draft, with implementation still blocked on the user's routing decision and corresponding integrated contract. T2's pre-code author entry is `b5d2706`; independent T2 strategy has now been dispatched after the earlier runtime-slot limit cleared.

V1 implementation was initially allocated to native `/root/m0_v1_impl`, GPT-6 Sol high (this attempt was rejected and is superseded by the external assignment below), in `cove-worktrees/m0-terminal-view`, branch `p/luchengxuan/m0-20-terminal-view`, base `560d6e3`. The app worktree tool only targets this chat's Orca repository and has no Cove repository parameter; artifact inspection found no reusable managed checkout, so this fourth Cove checkout was created with Git. V1 owns only its plan's terminal-web source/tests and task docs, plus terminal-web's own package export/build files; T2 owns engine's distinct package files. Shared root Vitest/inventory/boundary registrations remain exclusively T2-owned until explicit transfer after its source freezes; V1 submits a bounded registration request and may use an external task-owned local test config meanwhile. It must not silently claim unregistered local tests passed the complete CI gate. Independent V1 verification/review will be assigned after a coherent source, separate from its author. No protocol, lock or pin changes are assigned to V1. This initial plan reserved root, T2 implementer, T2 tester and native V1 implementer; the later rejected spawn and external reassignment supersede that native-slot count. Four checkouts remain, with no nested workers.

Native V1 agent creation was rejected by the runtime thread limit. Before any V1 code, allocation changed to the user-authorized local TraeX GPT-5.6 Sol high via warmpool in the same new checkout. Warm Sol sessions were zero; that is not a dispatch blocker. The root verified CLI help/model routing and did not restart the shared daemon. Three native active roles plus one external implementation role remain below the five-agent ceiling. V1 review stays independent GPT-6 Sol. External CLI session `01a0dd11-2749-7580-9d9c-8117185ecace` started through `warmpool run -- traex exec -m gpt-5.6-sol` with explicit high reasoning; its recorded turn context confirms the model, effort and assigned checkout. Task-local logs/progress live under `/tmp/cove-m0-v1-traex`; no warm-hit claim.

## Adapter plans integrated and implementation handoff

P1b PR #35 and the parallel adapter plans PR #36 are complete. Current integrated main is `efab3acc025454ab97b2b839f903f391b0370be2`. Plan PR #36 rebase-merged nine atomic documentation commits; every intermediate tree/message and single-parent map matches final source `40a726f`, with tree `4c60ad5c5623fcf996ae31c4fb8723856911f58c`. Final-main [CI36233626701](https://github.com/GhostFlying/cove/actions/runs/36233626701) passed macOS/Linux, 22 suites / 200 tests each; both artifact sets were checked against SHA/pins/lock, setup exits, inventory and inherited native/browser/recovery evidence. Plan review SHA256 `c0c06d7ed4fae6c102838b0c384d852ca1d869bf2c9c9e8842dcc7da04e49c75`; separate documentation validation `51ab239eaa94219fa708a3b4a5ccc8a74307b845b5d6d292301a917010012f1d`.

The preceding allocation was the initial dispatch. Current role, source and gate state is maintained in [handoff](../handoff.md#current-dispatch); the later repair checkpoints below supersede the initial parked/awaiting-source states.

T2 independent strategy SHA256 is `db407bd105699292b76b9974a5cee0105c4c01827b601b1dca4b4191265e6859`, sentinel corpus `1a638fd670af20314678dcfdb9e8bd9f053e3051c627ae13943afc45f361f55e`. V1 independent strategy is `443e171ccaacb41d251c9f76c6e398c2fd388529613979db20b6d94f69ef0271`, corpus `96cff9515e62ed4dd6248f82d957c93da09585ed1a73466704c0f483686eefc6`. Both were frozen before candidate delivery; no implementation test pass is implied. Source owners must not read the withheld corpora. Task reports live under `/tmp/cove-m0-t2-verification`, `/tmp/cove-m0-v1-verification` and external V1 progress under `/tmp/cove-m0-v1-traex`.

The P3 decision asks whether external terminal events should carry subscription identity (recommended, retaining internal RunEvent but requiring an explicit incompatible external-version boundary), versus isolated per-view connections with reconnect or a server queue barrier. Keep P3 held until the user answers; T2/V1 are independent. Actual Codex/TraeX/Claude Code workflows remain H1/C3. Devbox SSH remains blocked and must not be retried before the user restores access; C5/X1 still require 100 sustained real PTYs. M0 exit/M1 entry remain unapproved.

## T2 first candidate held for bounded repairs

Author `cfe5fa1` is integrated as `4b3d993`; results-only `29d0f3e` as `995f870`, draft PR #37. Code/config/tests match, with only coordination/P3/V1 plan docs differing from the author branch. Author macOS full gate passed 27 suites / 238 tests; hosted995f run36234128375 is green, while preceding4b3 run36234040075 was cancelled by the next push. Neither accepts the source. Independent Sol-high review `a4bb443527674815a8200e53f969e7de2bf8681db79690e4b5af1028ecc4d93f` records three concrete reset/accounting/fault-drain defects and one consolidated finite authored-case gap. Independent finite execution report SHA256 `53079874ea71ceb35a620eec3753b85a3339284a569420eeb2d80d50b9a0b9f1` confirms reset/counter failures, preserves verifier oracle/call-shape corrections, and holds the broad gate. The existing T2 writer owns the bounded repair; reviewer and verifier retain independence. P3 routing choice is still pending and unrelated.

## T2 corrected source and V1 bounded repair

T2 author functional `5f17c50` / results `5b43910` map to root `09d33e0` / `6035b06` in PR #37. Independent Sol-high source review closes the reset, settled-resource accounting and admitted-work fault guard defects plus required finite test gaps (report SHA256 `429c94e574aea1a978dac463f83017003f665c5b5b1eab616adf43190deaa916`). Author full gate and hosted macOS/Linux run36234797302 passed 27 suites / 247 tests. Both hosted artifact sets were checked; synthetic88476cd has exact main efab3ac/head6035b06 parents and source-equal tree8aea9bc. No protocol/pin/old-probe changes occurred.

Independent finite tests pass, but the first full local gate failed246/247 on unchanged Q1 mouse-probe browser-close cleanup. One isolated same-source reproduction passed, with browser exit and listener close verified; original cause remains unknown. Diagnosis report SHA256 `20f20a6dbdffcfdfaa2755c3dd522ec9fa68ae58352fd75556ecf4de60018723` retains the failure and evidence limitations. The one authorized full reproduction passed 27 suites / 247 tests; original failed artifacts are preserved, no source/deadline changed, and eight Q1 browser PIDs/listener ports were absent afterward. Final independent report SHA256 `8ac3d80ec002eaa27ad092a3d49747e61f1402bf15a556d80268b15d1be97cea`. The original timeout cause remains unknown. Final coordination-delta review and exact-head integration gates remain.

V1 PR #38 first source b137c84 maps to root1eb5aa3, results0c92e22 to8941e50. Independent Sol-high review SHA256 `f76f944a738f166d8229471598b214491f2c826abfacf7b51fc8e519bc571d33` holds it for five bounded classes: fatal retirement, complete teardown/timer cancellation, hidden-state replacement, exact finite oracles, and reuse of the accepted browser runner. The same external TraeX session resumes through warmpool; no warm-hit claim. Its scope extends only to a backward-compatible `packages/terminal-web/probes/node/managed-browser.ts` fixture entry; cleanup deadlines/error visibility, original Q1 behavior, root registrations and protocol stay protected. V1 independent candidate execution remains pending, and scoped eighteen-case success is not full CI acceptance. Four task checkouts remain; root continues coordination only.

## T2 completion and W1 plan release

T2 PR #37 rebase-merged eight commits at main `caa2c65cf618aa9ba77df4739884cd25e8d1118b`, each intermediate tree/message and single-parent mapping verified against final source df0da35. Final main run36235691451 passed macOS/Linux27suites247/247; both artifacts were downloaded and checked. Final independent report SHA256 `ab4adbb55b473e966c19ed9c767429e56a6d2a7d7e3ebe57de025f1368b367b5`, separate review `9182dc9b240c1df5600791977137e6fe1a92629e50296bfb3467d3ef11ee8fea`. The prior unexplained local cleanup timeout remains preserved. Issue15 is closed; W1#16 planning is ready, not worker implementation or a milestone transition.

A fresh W1 planner spawn hit the native thread limit; no worker was created. Existing Astra-high `/root/m0_p3_plan` was reused for W1 read-only planning from exact main, with sole output `/tmp/cove-m0-w1-plan.md`; its old P3 implementation remains held for the user routing decision. It must inspect pinned Orca comparatively and preserve exact contract/finite test/ownership boundaries. Root integrates the plan before any implementation.

V1's external author completed the bounded production repair08c299e and final test-onlyb26892d/results0744aa6. Independent source review closes production/runner findings, while the two finite-oracle corrections still require delta review. The existing V1 six-commit root sequence through1863681 was rebased onto caa2c65 without conflict, preserving each patch/message and endingbedac3a. New oracle note/test/results map3d47801/cdb0945/b087ad2. Root will use an exact old-head lease on its own feature branch; author/main branches stay untouched.

The former T2 author `/root/m0_t1_impl` now solely owns V1 root registration in reused `m0-terminal-recovery`, new branch `p/luchengxuan/m0-20-view-registration`, basebedac3a. Plan beforecode is `docs/tasks/m0-terminal-view-registration.md`. Scope is existing root Vitest/inventory/public-boundary machinery for the real three V1 suites/public export; V1 package source/tests/probes belong exclusively to its external author. Independent tester uses its own checkout and frozen strategy, separate Sol reviewer remains read-only. Four checkouts remain; external author is parked, native active roles are root/planner/registrar/tester. P3 and devbox access decisions remain held.
