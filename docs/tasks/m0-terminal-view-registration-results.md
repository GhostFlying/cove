# M0 V1 shared registration results

Status: author registration candidate. Independent V1 review, hosted macOS/Linux CI and coordinator integration remain separate gates.

The sole shared writer used `p/luchengxuan/m0-20-view-registration` in `/Users/luchengxuan/WORKSPACE/cove-worktrees/m0-terminal-recovery`, starting from exact combined V1 source `bedac3a91c1ed083f17f26e256b109fbce2415ae`. The pre-code allocation entry is `0c16f02739b8c02df1bd81fbf42865d1aad77a48`. The coordinator's two subsequent V1 test/doc-only commits `3d47801` and `cdb0945` were copied without conflict as `bb8584d` and `f8bdbbe`; they changed no shared registration path. Owned registration commits are `e27300a7eda2c7c141b15c41753e141bf78b3f9d` and `059e0b227b579f41fdc2b9387bc9a172c907e106`.

The root Vitest project now discovers `packages/terminal-web/tests/**/*.test.mjs` with one worker and a 45-second scenario limit. The CI gate requires `view-input`, `view-recovery` and `view-lifecycle` with at least six discovered and executed cases each; absent, empty and five-case discovery controls fail. The root TypeScript graph builds terminal-web's production and browser-test projects before both Vite fixtures. Package-boundary tests require the exact public `./xterm-view` subpath, emitted declaration and browser assets, and type-check an isolated DOM consumer of the compiled factory. Existing protocol declarations retain their separate ES-only boundary. The existing workflow already runs `pnpm check` on macOS and Linux and uploads `.cache/ci`; neither it nor pins/lock/protocol/terminal-web-owned source was edited.

Validation used pinned Node 26.10.0 and pnpm 12.6.0. `pnpm build` passed. Scoped `pnpm exec vitest run --project tooling --project terminal-web` passed seven files / 51 tests after the test-only V1 delta. `node --check`, scoped Prettier and oxlint, and `git diff --check` passed for the gate change. The full author `pnpm check` at exact source `059e0b227b579f41fdc2b9387bc9a172c907e106`, tree `a7564aeb1ee480f8c494e72c883f5482248ae0ad`, passed formatting, lint, root TypeScript build, both browser bundles, and **30 required suites / 267 tests** on macOS arm64. The V1 suites passed all 18 named cases; Q1's two retained browser suites passed 15. No skipped, pending or cancelled case was accepted.

The successful gate's `.cache/ci/view-evidence.json` was read back alongside `inventory.json` and `vitest-results.json`: it records the exact source commit/tree, OS/arch, Node/pnpm, xterm 6.0.0, Playwright 1.63.0, managed Chromium revision 1243/version 153.0.8010.12, SHA-256 of the lockfile (`4173cbfdbc0c169a7d5d3780461276e6ae77791ab98c2662fe1cc071dbacdc0f`) and profile fixture (`85bdb168027df1e796d106ede7dccf255f565218b7f3fc42ff561969b8c61987`), and 18/18 V1 case names/statuses. `inventory.json` contains 30 required suites, and the Vitest JSON reports 267/267 passed. `view-evidence.json` states the cleanup proof precisely: each passing view case awaited `withManagedBrowser` completion, whose cleanup errors reject; it does not claim a per-case PID/port record. A targeted post-run process scan found no process under this checkout's terminal-web or node_modules path. Existing Q1 cleanup tests independently exercised owned browser/listener disposal and deadline failure paths in the same full run.

This is the author registration and local browser result, not independent acceptance, Linux execution, a live client/worker/server path, real PTY or agent workflow, GPU/performance qualification, or M0 exit. The coordinator owns final source integration, independent V1 checks, exact-head hosted CI and artifact readback. Historical T1 strict diagnostics and all existing suite floors remain intact.

## PR #38 tooling correction

The temporary shared-writer correction started from integrated `7f96fbf361124ba67b1cdfc549ee5829e8168d0b` on clean branch `p/luchengxuan/m0-20-view-tooling`; the parked W1 branch remains at `a5f874f06ff02f5ad5771d26a2618fad7f3e8233`. Pre-code allocation is `f4f8043`, package script/behavior assertion `4e8ef00`, and evidence derivation/counterfactuals `4f90a0d82b86978211239da546234519d15f8648` (tree `8e05a830e7c4fd1d3d104494f47e22a9a5970fb4`). The package's `test` command now runs both the `terminal-web-probes` and `terminal-web` projects. View evidence uses the validated inventory's actual passed counts for the exact three registered production-view files; an additional passing case is accepted, while missing, short, pending or mismatched suite data still fails. No production view, browser corpus, pin, lockfile or W1 source changed.

With pinned Node 26.10.0/pnpm 12.6.0 on macOS arm64, scoped gate tests passed **26/26**, oxlint and Prettier passed, and the actual package script built and ran **five files / 33 tests** (two probe files / 15 cases plus three view files / 18 cases). The one full author `pnpm check` at exact code head `4f90a0d` passed native preparation, formatting, lint, TypeScript and both Vite bundles, then **30 required files / 270 tests**, numeric exit zero. `inventory.json` readback contains exactly three `terminal-web` files with 6/6 passing cases each; `view-evidence.json` records the exact code commit/tree, pinned browser dependencies, 18 case names/statuses and the bounded cleanup basis. The JSON runner totals are 270 passed, zero failed/pending/todo; all recorded toolchain, discovery, Vitest and source-revision stages exited zero. A targeted post-run process scan found no process under this checkout's terminal-web or node_modules path.

Artifact SHA-256 at that code head: `inventory.json` `f0e8acec093f33c34a71edc24a78cd5739a167895e5a0d93b3e008d63e93a1c9`; `view-evidence.json` `9e4e579d569eefd21a472c9508b08212d1990fbbe0267b14980c7c9e6a50a9ef`; `vitest-results.json` `8044226dcd2a7f579b2127d0ce427732f0f004b2ff3674ff980f8bb59f3e740f`; `execution.json` `692b8791f629fc643f7b330ffb74578be9fe6e905c061ec0652f0458d09b04a9`. The first scoped command was refused by pnpm because this checkout's workspace installation predated the branch; a frozen install completed without lockfile edits. The first gate-test attempt then exposed a stale `needs 23` synthetic assertion after raising the required test floor; that assertion was corrected before the passing scoped and full runs. This document-only result commit follows the tested code head; hosted dual-OS CI, independent verification/review and PR integration remain separate coordinator gates.
