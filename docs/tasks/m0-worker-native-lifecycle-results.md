# W1 N1 owned Unix writer lifecycle: author result

Status: latest author source and tests frozen at `7b3cda67a4ed9e6d99e1d1be94888f748b5309d1` (tree `35049214e282039b92df35eec7be0aa5e2ac1b11`) on `p/luchengxuan/m0-16-native-writer-lifecycle`, based on accepted main `77de10a2f91fb3eb0fc42c7fc27dd5270ce56704`. Production patch and lock remain byte-identical to `fbd3ecc`. This is a **macOS author result**, not independent qualification or N2 release. Root owns independent native verification, source review, hosted macOS/Linux checks and final-main acceptance. The earlier evidence below is retained with its original source attribution.

The pinned node-pty 1.1.0 patch is `patches/node-pty@1.1.0.patch`, SHA-256 `b2102836ecb0be8662b611632a5ab51cdbd236ccb7579371c96364fea6f3053d`. The patched Unix fork returns a distinct close-on-exec writer descriptor only for bounded mode; the package retains that descriptor through the last issued `fs.write` callback. The writer closes once before FIFO ticket settlements and publishes an owner-completion result only after all owned synchronous callbacks and accounting unwind. A close error reports `close-uncertain` and cannot be retried against a reused integer fd. The native exit watcher serializes stop against reap for constructor rollback. Stale addons fail the bounded capability check before spawn, and native preparation rejects a prebuilt path. The source build requires the pinned root build tool `node-gyp@12.4.0`; no node-pty version pin or Windows backend changed.

On macOS 26.3 arm64 with Node 26.10.0, pnpm 12.6.0 and Node ABI 147, `pnpm patch-commit` built the addon from patched source. `pnpm native:prepare` loaded this checkout's `build/Release/pty.node`, SHA-256 `f1d656c9cc80d81179af9e9fb546156b6fb971e2d42dbab64b53002a40699fbd`; the macOS spawn helper was executable (mode 755), SHA-256 `5f7adf1b79b8f93b72e4368c76243df949502c39fa9abc64a02be99efb28ff42`. The scoped native owner, real descriptor reuse, CI registration and install tooling checks passed 4 files / 44 tests. The isolated real reuse runner occupied the sole libuv worker, issued a bounded write, closed ReadStream fd 13, reopened that number as a task-owned sentinel, observed distinct writer fd 15 remain open until settlement, then observed a closed writer and owned child exit; the sentinel contained zero bytes. That run reported an EIO settlement with zero written bytes, so it proves descriptor isolation in this setup, not delivery of the attempted payload.

One full `pnpm check` on the frozen source passed formatting, lint, TypeScript/reference and browser builds, then 33/33 required suites and 290/290 tests with zero failed/pending/todo. The local log is `/tmp/cove-m0-n1-author-check.log`, SHA-256 `d91af7879c54cf035a8cae8b05e8a4b5d3ea5fc1d0ec3f96480cdba9786f16f8`; generated JSON/JUnit inventory remains in this checkout's `.cache/ci`.

The historical N0 Node 24 shared-fd counterexample and its artifacts remain unchanged. This result does not generalize the observed zero-byte settlement to all write timing, prove Linux source-build/runtime behavior, verify the complete rollback and long-churn matrix independently, or claim N2's production owner ledger. The approved N1 plan defines those gates; independent verifier and Sol source reviewer are testing this exact candidate. No W1 worker pump, protocol R1 or 100-PTY load test is included.

## Finite macOS parent-descriptor correction

The independent first-candidate verifier held `232934e` on parent-descriptor churn: eight bounded `/bin/true` PTYs changed its process from 12 to 29 open descriptors and eight legacy PTYs from 12 to 37. Its report is `/tmp/cove-m0-w1-n1-native-verify/report.md`, SHA-256 `2ceb1291acc3d95652a609beae71bf96ff50ae524d9100f595a44dddf02d3b26`; those failures remain negative evidence. The macOS `pty_posix_spawn` helper left parent slave and reservation descriptors open and skipped early-error cleanup. The legacy exit watcher also retained its event `kqueue`. Source commit `c208d783b205e65f15b139703b7db594ac7eb301` closes each parent-owned handle after its last use, including the failed-spawn path. Test-only lint correction `e1a8c08` changes no native behavior. The new patch SHA-256 is `9d4dc635852a250c17eec6edc1c18c9a3c7fa802a4193f933de015e773f44325`; the generated lock refers to that exact patch. No Linux fork, Windows backend, or writer ownership rule changed.

The newly registered `native-write-churn` suite runs eight bounded and eight legacy real PTY cycles, checks child exit and closed reader/writer descriptors, and requires the parent's descriptor count to equal its warmed baseline after each group. On macOS it also invokes the native fork with a nonexistent helper eight times and requires no descriptor growth after `posix_spawn` rejects it. Independent verification discovered that `/bin/true` was absent on this macOS host, so the original fixture had not proved successful target execution. Test-only commit `1c57350` now uses executable `/usr/bin/true` on macOS (`/bin/true` on Linux) and requires every child to exit with code and signal zero. The author run on this corrected fixture and installed package measured **13→13** for successful bounded cycles, **13→13** for successful legacy cycles, and **13→13** for failed spawn. In this author process, a measurement-only control without a PTY yielded `readdirSync('/dev/fd')` counts 12, 13, 13…; the independent verifier's no-PTY control remained 12→12, so that observation cannot explain its first-PTY 12→13 change. The first-PTY descriptor difference remains under independent attribution and N1 acceptance is held. The corrected fixture against the retained first-candidate package failed as expected at **15→31** after successful bounded churn (exit 1). The registered test proves strict no-growth over eight additional cycles after its explicit PTY warmup, not that the first cycle has returned to the pre-PTY baseline.

Node 26.10.0/pnpm 12.6.0 with the task-pinned local headers rebuilt the corrected native addon. `pnpm native:prepare` loaded this checkout's patched `build/Release/pty.node`, SHA-256 `ddc2e3cffa1a31a7605098cdb90c60f9c9a787620e2a92878be52f8b07105884`; the helper was executable, mode 755, SHA-256 `0e887faa653d67db9423feffce2d671dbc8bb9d47f24812fa1bc2a6133d54a9c`. Latest successful-target scoped worker/tooling tests passed 8 files / 56 tests, with targeted formatting and lint green. The first broad attempt stopped at `vitest(no-conditional-expect)` on the new test and did not reach runtime; after the equivalent assertion correction, one full `pnpm check` passed formatting, lint, build, and **34/34 required suites, 291/291 tests** with no failed/pending/todo cases. Its log is `/tmp/cove-m0-n1-corrected-check.log`, SHA-256 `ecd9276ccd5d051ca9fce6492dfc0782b894b3a95b745c225a448669febba648`, but that full gate belongs to the earlier `/bin/true` test oracle at `e1a8c08`, not the successful-target correction. The latest source requires new exact-head hosted and independent checks; no repeated local full gate is claimed.

The real failed-helper spawn exercises the native helper's post-open rollback on macOS. It does not inject duplicate-writer-fd allocation failure, native watcher publication failure, or every `posix_spawn` file-action/attribute failure; no narrow runtime fault seam exists for those phases in this patch. They remain source-review and future fault-injection limits, not claimed runtime passes. Independent delta native verification, independent source review and hosted macOS/Linux gates must still evaluate this corrected source before N1 acceptance.

## Final bounded N1 author candidate

`a1cb9d2` binds every issued `fs.write` callback to its own ticket. A stale callback cannot retire a newer issued write after a partial prefix, and an invalid successful count after disposal faults and finishes retirement. `cb55a27` adds the parent watchdog and nonce-bound, non-shortcircuit cleanup to the real native reuse fixture. `61cab46` covers real child-read byte delivery, natural exit, induced read error, close-on-exec, legacy/open descriptor ownership, and three JavaScript constructor rollback phases. The private, fixed 13th native fork argument at `c78cf0b` injects duplicate acquisition, pre-watcher, and post-watcher failures so the native rollback path is exercised at runtime. The ordinary 11/12-argument fork paths and production public API do not use this control. Each phase checks owned child absence, descriptor closure, and watcher behavior; the fault suite is registered. Source and tests remain finite N1 scope; no N2 pump or worker routing is included.

The final patch SHA-256 is `0c02825596f1f11782bd374e62cfc1e89792b1bc5cdeef496dbb3d844a5ee39d`, with the lock bound to that patch. The rebuilt macOS arm64 addon SHA-256 is `be3f28dfe92dc1c92d5ae4b1803fee40559da3e2e27d70dad47f1965222c9fe2`; the executable helper SHA-256 is `86120406ec4e928ff4955013b4bc338507c964e9f72c054e1803bf5595872c19`. The pinned author environment was Node 26.10.0, pnpm 12.6.0 and ABI 147. These generated binaries are local evidence, not committed artifacts.

The first broad attempt on `c78cf0b` passed source preparation and builds but stopped at the CI suite inventory because `test.each` exposed one declaration where four separately discovered reuse cases were required. A second broad attempt on `64c8507` reached lint and stopped because the explicit test bodies only called asserting helpers. Those failures remain in `/tmp/cove-m0-n1-final-check.log` and `/tmp/cove-m0-n1-final-check-64c.log`; neither is a passing gate. `fbd3ecc` changes only the reuse and rollback test bodies to assert their observed exit, closed-descriptor, and reaped-child results directly. It does not change the patch, lock, limits, registration, or cases from `c78cf0b`.

The final author `pnpm check` at exact `fbd3ecc` passed formatting, lint, TypeScript and browser builds, then **37/37 files and 301/301 tests** with all required suites discovered. The log is `/tmp/cove-m0-n1-final-check-fbd.log`, SHA-256 `12a1556c5c9cb762906013c3d8952c648ae077174ff7769c52499d65afcc14fb`; JSON/JUnit inventories are generated in this checkout's `.cache/ci`. No later source or test edit is included in that pass. This does not replace independent native delta verification, final source/test review, or hosted macOS/Linux gates.

The independent first-use observation remains cold **12→13** for one persistent `/dev/null` descriptor, while subsequent successful bounded and legacy PTY churn was **13→13**. Read-only source attribution in `/tmp/cove-m0-w1-devnull-attribution/report.md` (SHA-256 `b8c7f7ac82aa854ac1139a8f731006f67d4c21da40f0ef8dc0fcb731e8988c20`) identifies pinned Node 26.10.0/libuv 1.52.1's per-loop lazy `emfile_fd` reserve through `tty.ReadStream` initialization; `/tmp/cove-m0-w1-n1-native-verify/devnull-evidence-supplement.md` (SHA-256 `bf0b26c900696b4c6df392a6c12ccf5a86e145a62847f6e13b3adeea04724467`) checks that source chain against the observed descriptor type. The exact fd-13 open return was not syscall-traced, so this is high-confidence attribution rather than direct syscall proof. The original per-PTY leak and first-use observation stay visible; N1 acceptance still requires independent final-candidate assessment.

## Resource-owning test deadline correction

Independent corrected-source review `/tmp/cove-m0-w1-n1-corrected-review/report.md` (SHA-256 `662bec68939df3d26b77dbe6129ba87108b40aeb4f673baabf92b9568e900e23`) closed the production acquisition and callback findings, but held acceptance because a 15-second local fixture watchdog could race the same 15-second Vitest timeout and two direct owner-completion awaits had no local bound. The churn runner also had no outer watchdog. That review inspected `c78cf0b` source; its later `64c8507`/`fbd3ecc` changes only made the same test cases explicitly discoverable and lint-visible. These limitations were real test-safety gaps despite the author and hosted 301-test passes at the prior head.

Pre-code correction plan `51ceb1f` and test-only commit `7b3cda6` leave the native patch, lock, runtime bounds and public API unchanged. Resource-owning reuse and native-fault cases now have 15-second local watchdogs inside 35-second test deadlines; lifecycle owner completion is locally bounded to 5 seconds inside 35-second tests; the churn parent has a 10-second runner watchdog inside a 45-second test. The churn runner reports nonce-bound child PIDs, and its parent checks exact argv identity before any signal. Hung reuse and churn negative controls force early watchdog failures, then verify runner and owned-child absence before returning to Vitest. The parent also removes only the fixture-reported task-owned reuse scratch directory after an interrupted runner. CI minimums rise only for those two new cases (reuse 4→5, churn 1→2), with the matching tooling assertion.

At exact `7b3cda6`, scoped terminal-worker and tooling checks passed **5 files / 38 tests**; Prettier, Oxlint and `git diff --check` passed. One author `pnpm check` then passed formatting, lint, TypeScript/browser builds and **37/37 files, 303/303 tests** with every required suite discovered. The full log is `/tmp/cove-m0-n1-fixture-deadline-fullcheck.log`, SHA-256 `c942b5a0f4cbc07f79d05b4d887db0765830dcc111c7ee41efd59fa564f58537`. No new production code or N2 work is included. The earlier 301-test gate and hosted run belong to the prior head; independent review of this test delta, finite native cleanup verification, and new exact-head dual-OS CI remain required before N1 acceptance.

## Final owned-fixture cleanup and hang observation

Independent review of `7b3cda6` found that the native rollback test could skip remaining cleanup after its first process inspection error, and forced-hang reuse/churn tests could pass vacuously if their 250 ms timer fired before reporting an owned child. The precise report is `/tmp/cove-m0-w1-n1-final-timeout-review/report.md`, SHA-256 `f639159c1daa77673cc04cc180b1ebf0306e1017ed8bdcb6d389260fdfc57195`. Its production-source findings remained closed; these were test-safety findings. Pre-code plan commit `ecd25ca` allocated only the five package-local test and fixture files.

Test-only commit `b7e4cdcb0ea43f89a381cb756139409571c94dc0` (tree `54a311b21d3b1c61909e607dc3c03e9b423a49bd`) makes rollback runner/child discovery and each verified stop independent; exit wait and both absence checks also retain the first error while allowing other safe cleanup. An injected discovery failure and an injected stop failure verify this progression without signaling an unverifiable PID. The forced-hang reuse and churn fixtures now send nonce-bound `hang-ready` only after they report an actual owned child; reuse also reports its scratch directory. The short 250 ms watchdog begins only after that phase is acknowledged and observed resources are nonempty. With a deliberately withheld acknowledgement, both tests fail on their bounded phase deadline and still verify disappearance of the resources that were actually reported. This change does not alter the production patch, lock, native binary, CI floor or browser budget.

At exact `b7e4cdc`, the focused worker run passed **3 files / 9 tests** including the two withheld-ack negative controls, and the post-edit fault test passed **2/2**. Oxlint and `git diff --check` passed. One full author `pnpm check` passed formatting, lint, builds and the required inventory: **37/37 files, 304/304 tests**, zero failed/pending/todo. Log `/tmp/cove-m0-n1-final-hangphase-fullcheck.log` has SHA-256 `d0af8d90eb687fd0d731ebb6ded6f741c3f40c69ef1244a359ae52c7fbaee24d`. This is local macOS author evidence for the test-only correction. Independent cleanup-negative verification, source delta review, exact-head dual-OS hosted checks and final-main acceptance remain separate gates; the earlier native results retain their original source attribution.

## Hosted watcher callback observation

PR #42 run `36261530039` at exact head `34c76b1` passed Ubuntu **37/304** but failed macOS **36/37 files, 303/304 tests**. The only failed case was the registered native fault fixture: after-watcher checked `exitCallbacks === 1` after a fixed 100 ms and observed zero. Downloaded artifact provenance and the nonpassing result are preserved by the independent report `/tmp/cove-m0-w1-n1-final-hosted-verify/report.md`, SHA-256 `edf8978e91535543aab9256e36268699083a1a7adf455b03db7de299cf55a576`. At that head, timing versus a permanently missing native callback remained unproved; no retry or acceptance waiver followed.

Pre-code plan commit `9a37568` and test-only source commit `0dbffbbd1034f124fa286d084dbf8100418e77a7` (tree `f94b2d599282ba830205d7ecaa5cf0f375003832`) change only the native fault runner. The after-watcher case now awaits observation for at most 3 seconds within the unchanged 15-second runner and 35-second Vitest deadlines, then checks raw native callback count and delayed observed count are both exactly one after 100 ms of quiescence. The fixture deliberately delays its counted observation by 175 ms after the actual native callback, so the former fixed sleep is a deterministic negative control. A truly absent callback still fails at the local deadline; the other two fault phases still require zero callbacks, baseline descriptor count and no owned child. The original verified finally cleanup remains.

At exact `0dbffbb`, the real patched-addon fault suite passed **1 file / 2 tests**. One full author `pnpm check` passed formatting, lint, builds and required inventory: **37/37 files, 304/304 tests**, zero failed/pending/todo. Log `/tmp/cove-m0-n1-watcher-observation-fullcheck.log` has SHA-256 `60eb8f61d400c972117567c1a1acfce44870865dd7033221c68b7ca0470a69eb`. Independent source delta review approved `0dbffbb` (`/tmp/cove-m0-w1-n1-watcher-oracle-review/report.md`, SHA-256 `e192f61a6ba69946300f5ab123f4f183119815a5fac6cf944cd9a703ec0502e9`); independent native timing/missing-callback controls and new exact-head hosted macOS/Linux results remain required. The production patch, lock, binary, public contract, G0 browser work and N2 scope are unchanged.
