# M0 V1 shared test and build registration

Status: implementation plan and allocation entry. This file is committed before shared configuration edits; it is not V1 acceptance evidence.

Owner: `/root/m0_t1_impl`, GPT-6 Sol high. The coordinator transferred the sole root/shared registration writer role from completed T2 to this bounded V1 registration task. Assigned clean checkout `/Users/luchengxuan/WORKSPACE/cove-worktrees/m0-terminal-recovery`, preserved T2 branch `p/luchengxuan/m0-15-terminal-engine`, new branch `p/luchengxuan/m0-20-view-registration` from exact integrated V1 source `bedac3a91c1ed083f17f26e256b109fbce2415ae` (P1b/T2 main predecessor `caa2c65cf618aa9ba77df4739884cd25e8d1118b`). The external V1 author owns production/browser-test repairs separately and does not write shared registration. The registration request `/tmp/cove-m0-v1-traex/registration-request.md` was read with SHA-256 `4d95c5f26770f7544eb8fad088be59a2810565cd900487c1754faf69c136c619`.

Objective: make the existing built `@cove/terminal-web/xterm-view` and the three existing six-case V1 browser suites part of the ordinary macOS/Linux required `pnpm check` and artifact gate. Scope is only `vitest.config.ts`, `scripts/ci-test-gate.mjs`, `tsconfig.json` if the root solution omits the already package-local production/test projects, directly affected `tests/tooling/{ci-test-gate,package-boundaries,project-references}.test.*`, and this plan/results pair. A root script or workflow change requires a concrete discovered gap; the current `.github/workflows/check.yml` already runs `pnpm check` on both OSes and uploads `.cache/ci/`. No terminal-web source/tests/probes/package/plan/results, protocol, dependency, pin or lockfile edit is owned here. Existing historical T1/Q1 suites and classifications remain unchanged.

The existing Cove CI gate is executable authority for discovery, mandatory suite floors, actual JSON/JUnit execution and artifact output. The V1 plan already compares relevant Orca source at `5534462b50c660888487a2108700d4cf284270db`; Orca is not authority for Cove root registration. The package-local terminal-web tsconfig already references a DOM production project and test browser project, and its Vite outputs are separated. Check the root solution build from a clean output path so the public browser fixture never depends on stale `dist`. Add one `terminal-web` Vitest project, `maxWorkers:1`, bounded timeout, and three explicit suite floors of six without decreasing any existing floor. Extend the inventory's source scan and missing/empty-suite controls. Strengthen the isolated compiled consumer boundary for the public subpath while keeping protocol declarations ES-only and DOM types local to terminal-web.

Validation sequence: verify the base/clean checkout and installed package output; commit this entry; make one coherent shared-registration change against the existing six-case suites, then run the affected tooling and V1/probe scoped tests with pinned Node 26.10.0/pnpm 12.6.0. Once the external author freezes any additional test-only delta and root integrates it, run one full `pnpm check` at the coherent combined source and inspect the required suite inventory and `.cache/ci` artifact contents. Preserve errors, browser cleanup/process evidence and exact revision/digests. Record author results in this task's results file; independent V1 verification/review, hosted dual-OS CI and final-main evidence remain coordinator gates. No push, PR operation, nested agent or unrelated runtime work is delegated by this plan.

## PR #38 tooling correction allocation

Owner remains `/root/m0_t1_impl`, GPT-6 Sol high, temporarily transferred from the parked W1 qualification. The clean W1 branch `p/luchengxuan/m0-16-worker-execution` is preserved at `a5f874f06ff02f5ad5771d26a2618fad7f3e8233`. This correction uses the same checkout on `p/luchengxuan/m0-20-view-tooling` from exact integrated base `7f96fbf361124ba67b1cdfc549ee5829e8168d0b`. The coordinator retains GitHub and handoff ownership; this branch owns only `packages/terminal-web/package.json`, `scripts/ci-test-gate.mjs`, directly affected gate/tooling tests, and this registration plan/results pair. There is no production view, browser-suite, corpus, pin, lockfile or W1 implementation edit.

Objective: make the package-local `test` script execute both registered terminal-web probe and production-view projects, and derive view evidence counts from the validated inventory so a new passing case above the three six-case floors does not fail CI. Keep exact three-suite identity, all-discovered execution, and missing/short/pending fail-closed behavior. Add a script-behavior assertion and finite positive growth and negative counterfactual controls. Run scoped tooling and the actual package script, then one pinned full `pnpm check` at the frozen candidate because the root gate changed; record exact source and results separately. Independent review, verification, CI and PR completion remain coordinator gates.
