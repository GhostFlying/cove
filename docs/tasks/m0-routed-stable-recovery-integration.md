# M0 R1 routed stable-recovery integration

Status: pre-code integration allocation, 2026-09-27. The integration owner is `/root/m0_t1_impl` in `/Users/luchengxuan/WORKSPACE/cove-worktrees/m0-terminal-recovery`, branch `p/luchengxuan/m0-19-routed-protocol-integration`, based on accepted main `77de10a2f91fb3eb0fc42c7fc27dd5270ce56704`. The completed author branch `p/luchengxuan/m0-19-routed-protocol` remains at `7e7cbeb6496b8eff6d86afe6bf1f86045fadd864` and must not be rewritten.

Objective: replay the completed R1 schema, test, fixture, task-plan and results commits onto the accepted main without introducing N1's unaccepted native changes. Preserve each author's commit content and full message where conflict-free; document any conflict resolution and compare each original-to-integrated source tree. Correct the task-local original candidate tree typo identified by independent source review, then reconcile shared current-contract documents with same-ref recover and the user's decision that pre-formal-release experimental versions are not maintained compatibility lines. Formal-release and same-version compatibility rules remain intact.

The original independent R1 functional source review is `/tmp/cove-m0-p3-r1-corrected-review/report.md`, SHA-256 `dfcceee5a155be3814505bdb536462239f6fe37d0d29ba39e809c0fafb38c16b`; the independent compiled correction verification is `/tmp/cove-m0-p3-r1-corrected-verify/report.md`, SHA-256 `5daf764f3708f080c3ba4ac8cba525a4988970360f2ac104beee16c3bf4a7bb0`. Neither report proves the rebased combination. Keep the historical old-reader `[2]` rejection as a disclosed negative control, without a v1 backport.

Writable paths: the reviewed R1 author's `packages/protocol/src/**`, `packages/protocol/tests/**`, `tests/fixtures/protocol/m0/**`, task-specific R1 plan/results, this integration document, and integrator-owned `docs/tasks/m0-supported-protocol-plan.md`, `docs/tasks/m0-client-controller-plan.md`, `docs/relay-protocol.md`, `docs/handoff.md` and directly relevant execution record. No package pin, lock, root registration, N1 native patch, worker/server/view implementation, or runtime P3 client work belongs here. Resolve collisions only after inspecting both sides and explaining the chosen semantics.

Verify the integrated protocol public build and its real suites, affected compiled terminal-engine/web consumers and the complete root `pnpm check` in this checkout's own installation. Record original-to-integrated commit/tree/message mapping, exact source/fixture diff, test counts and held runtime obligations. Freeze the source for separate independent review/test and hosted macOS/Linux CI. Open a draft PR and attach it, but do not merge without the independent gates and exact-head CI. R1 does not release W2/P2 runtime ordering by itself.
