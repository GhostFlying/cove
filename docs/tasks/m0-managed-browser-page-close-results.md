# M0 managed-browser page-close phase repair results

Status: frozen author candidate for issue #40. Independent verification, source review, hosted dual-OS CI and final-main acceptance are separate gates. W1 N1 remains held.

The primary checkout `/Users/luchengxuan/WORKSPACE/cove` used branch `p/luchengxuan/m0-40-browser-cleanup` from merged main `b1af3f2510c52b2d03b37d02613b6dcbc5fccf5b`. The pre-code allocation plan is `f340b15`; its separate native Sol approval was recorded in `3fdcd8e`. Functional source is `031f5cb43370720cf29e6b02bfefe24ad43370c5`, tree `0d2af87129822e0c1d8338a05bffce79252e972b`. No production view, terminal engine, worker, protocol, package pin or lockfile changed.

The query browser cleanup now bounds each page close by its remaining fair share of the existing 3.5-second page phase, after its disposal reserve. The environment/B0 500 ms page-operation limit and overall seven-second query cleanup envelope remain unchanged. An expired phase starts no new page operation. A close is issued once; timeout remains a failure even if its promise settles later. Final finite evidence records each page's disposal/close budgets, elapsed time and outcome alongside the owned browser and listener state. The required Q1 probe floor increased from ten to eleven with the added controlled close case.

The pinned Playwright 1.63.0 installed implementation was inspected before editing: `browser.newPage` creates a browser-owned context; `Page.close` awaits that context's close; and `BrowserContext.close` awaits request disposal, instrumentation, HAR export, channel close and its closed promise. This shows that the prior fixed 500 ms cap can cut short more than a page operation. It does not establish which stage caused either hosted timeout. The historical PR #39 first-attempt V1-L1 timeout and final-main V1-L3 timeout, plus the single unchanged-head passing retry, remain in `/tmp/cove-m0-qi-browser-cleanup-incident.md` and [issue #40](https://github.com/GhostFlying/cove/issues/40).

Pinned macOS arm64 author validation used Node 26.10.0, pnpm 12.6.0, Playwright 1.63.0 and managed Chromium revision 1243. Source-scoped Prettier, oxlint and terminal-web build passed. The focused query suite passed **11/11**; the affected V1 lifecycle suite passed **6/6**; B0 environment and tooling gate suites passed **32/32**. The new query case exercised two 700 ms close delays within their shares, a never-settling first close with later page cleanup, late settlement after timeout, an expired phase with zero close attempts, and simultaneous primary and cleanup errors. Each controlled subprocess reported its owned browser exited and fixture listener closed. The initial `pnpm exec` invocation stopped before tests while pnpm tried the default internal registry for its already pinned self binary; adding `npm_config_manage_package_manager_versions=false` used the installed pinned binary without repository changes.

At the exact functional source commit, one full author `pnpm check` passed native preparation, formatting, lint, TypeScript, both browser bundles and **31 required files / 276 tests** with zero failed, pending or todo. The generated inventory contains the query suite with eleven discovered/passed cases and V1 lifecycle with six. `view-evidence.json` readback names all eighteen passing production-view cases and records the exact source commit/tree and pinned runtime; its cleanup basis is awaited `withManagedBrowser` completion, not a per-case process record. A targeted post-run process scan found no process under this checkout's terminal-web or Playwright installation path.

Artifact SHA-256 at the functional source: `inventory.json` `43884690b68c17254a5820ffd8aeb05ffd3a7ed9caa70167494eea96ad0d00ae`; `view-evidence.json` `1489f6abbb61d4c7ddebd22b3204d01ac7663acaed477d757856dd236ceb14ee`; `vitest-results.json` `b9148b3ab198bd482b4ae5934ee5694e55c96172ccdac0771126600e41bf2ce9`; `execution.json` `02320969532bc4bf49d13aa7f0338bf89c6d582ab7aed98e292b162d3cc2971c`. The inventory reports 31/31 passing files and 276 passing cases; the runner reports zero failure/pending/todo and numeric exit zero for toolchain, discovery, Vitest and source-revision stages.

This author result proves the bounded deadline/accounting correction and its local controlled oracles. It is not a reproduction of the hosted timeout cause, independent acceptance, Linux execution or a released W1 dependency. The separate reviewer and verifier received the immutable functional source while this results record was prepared.
