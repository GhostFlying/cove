# M0 W1 native qualification integration

Status: pre-code integration allocation; the historical qualification does not establish a bounded native input writer.

Owner: `/root/m0_t1_impl`, GPT-6 Sol high, is the sole primary-checkout integration and shared-registration writer. The assigned checkout is `/Users/luchengxuan/WORKSPACE/cove`, branch `p/luchengxuan/m0-16-native-input-decision`, clean pre-code head `b612b8124cd1d26e61bfecc2b28fc96529470191` on V1-merged main predecessor `489f3466d55009b2730a8fa70b86c54e44abbb3c`. The source to preserve is the committed W1 qualification branch `a5f874f06ff02f5ad5771d26a2618fad7f3e8233`; its package files and qualification result are historical inputs, not authority to overwrite newer shared tooling. The accepted corrected W1 sequence is QI → N1 → N2. N1 production patch work cannot start until QI is accepted and the registrar grants its boundary.

Objective: integrate only the exact committed `@cove/terminal-worker` qualification package and bounded native tests into the current workspace so the source can be built, discovered, and independently reviewed without V1 regression. Preserve the qualification's diagnostic failure: public unpatched `node-pty` 1.1.0 cannot prove the required ongoing 64-KiB native input bound. Do not turn the private diagnostic read into production behavior or claim W1 worker acceptance.

Writable scope: `packages/terminal-worker/**`, its workspace importer in `pnpm-lock.yaml`, `tsconfig.json`, `vitest.config.ts`, `scripts/ci-test-gate.mjs`, directly affected `tests/tooling/ci-test-gate.test.mjs`, and this task-specific integration record. The original W1 plan/results are read-only provenance. Do not edit protocol (R1 has exclusive ownership), T2/V1 source, pinned versions, worker production writer, P3 contract, or unrelated root configuration. No new worktree, agent or devbox retry belongs to this allocation.

Method: copy the six committed worker-package files byte-for-byte from `a5f874f`; add only their actual TypeScript reference and native Vitest project; register the three real qualification cases without lowering the existing tooling floor or removing V1 evidence checks; regenerate the lockfile through the pinned pnpm after adding the existing catalog dependency. First run package build and compiled native tests, then affected tooling and a single full `pnpm check` on the coherent source. Record macOS-only native identity and cleanup separately from Linux CI. Freeze exact source SHA and results for independent TraeX xhigh review plus independently authored verification before any N1 dispatch or merge.
