# M0 managed-browser page-close phase repair (issue #40)

Status: Astra HIGH plan only, 2026-09-26. Base: Cove main b1af3f2510c52b2d03b37d02613b6dcbc5fccf5b. Implementation checkout/task identity assigned by root before dispatch. No source edits, probes, test execution, review or Git/GH mutation performed here. QI is merged but final-main acceptance is blocked; N1 stays held.

## Evidence and bounded diagnosis

Read-only incident packet: /tmp/cove-m0-qi-browser-cleanup-incident.md; tracked issue https://github.com/GhostFlying/cove/issues/40. PR39 exact head 8c3aa2e101fa562a819a199f7a8cffde9ec408ff failed macOS V1-L1 in run36249529823 attempt1; one authorized same-head failed-job rerun passed. Same-tree merged main then failed macOS V1-L3 in run36250196472 (274/275), again `Browser page close timed out`; Ubuntu275 and worker4 passed. These failures remain evidence. No further blind retry.

Pinned source: packages/terminal-web/probes/node/managed-browser.ts:57–73 (Promise.race bounds without cancellation), :285–405 (cleanup), :418 onward (page acquisition); packages/terminal-web/tests/view-browser-runner.mjs:9–21 selects query profile; tests/browser/main.ts:819–842 exposes the actual fixture.dispose through coveQuery. Disposal is wired; there is no evidence it was skipped. Each page is created with browser.newPage; do not assume explicit context ownership or independent context-close completion without inspecting the pinned Playwright implementation in the implementation checkout.

The source proves a narrower invariant defect: cleanup reserves a 3.5-second page phase inside seven seconds, yet after successful disposal page.close is capped at500ms even when that page's allocated share remains. Thus the harness can reject a close that completes inside its already accepted phase. Promise.race leaves a timed-out close running while browser-server teardown proceeds. The logs do not establish what delayed the real close (target/context teardown, transport, scheduling, or another resource); none is asserted as root cause. Fix this demonstrable deadline/accounting defect and collect bounded close-stage evidence; do not claim a reproduced hosted cause or a proven V1 product bug.

Prior contract: docs/tasks/m0-browser-cleanup-plan.md and -results.md repaired Q1 disposal allocation after PR33, retaining500ms page close; docs/tasks/m0-client-query-plan.md:69–71 and m0-terminal-view-plan.md:177–179 require bounded page disposal/close, graceful browser close, owned kill, observed process/listener closure and primary+cleanup errors. Retain all of those and seven seconds total. B0/environment profile behavior remains unchanged.

## One bounded implementation slice

Owner: interactive native GPT-6 Sol HIGH, browser environment work. Writable source: packages/terminal-web/probes/node/managed-browser.ts. Test scope: probes/node/query-input.ts (existing cleanup-only scenario), probes/query-input.test.mjs, tests/view-lifecycle.test.mjs and tests/view-browser-runner.mjs only for targeted cleanup evidence/failure coverage. No production terminal/view change. Task plan/results docs may be updated by the assigned author; shared handoff, root CI/config/manifest/lock and registration stay with the sole integration owner. Existing discovery should cover tests; any registration change requires that owner and the same complete atomic commit.

1. Before editing, inspect installed pinned Playwright1.63.0 newPage/page.close ownership implementation; record exact package/source path. Add small per-page lifecycle records to existing optional cleanup evidence: page index, dispose/close start/end, allocated/remaining budget, settled/timeout/error, and final owned browser/listener result. Evidence is finite (existing max2 query pages), no DOM/terminal/private content. V1 subprocess failures must retain this evidence, not only the timer stack. Do not turn this into a logger/framework.
2. For query profile retain existing pageDeadline=start+3500ms and fair per-page pageEnd, disposal cap1500ms and its reserved500ms close minimum. After disposal, issue page.close exactly once and bound its await by the remainder of that pageEnd (also clipped to absolute cleanupDeadline), rather than a fresh fixed500ms maximum. Unused time carries forward through existing phases; no enlarged constant, aggregate budget, suite timeout, profile work budget or retry. Do not start a new page operation after its phase expires; record that failure and continue owned browser cleanup.
3. Track that single close promise through success, error and timeout; attach rejection handling immediately. If its await times out, retain the timeout permanently and let the reserved browser close/kill path clean the resource. A later successful settlement must not erase the error, increment success twice or schedule another close. Snapshot finite evidence at finalization; do not let late callbacks mutate emitted results or throw unhandled rejections. No requirement to wait unboundedly for page closure before killing the owned process.
4. Preserve graceful-close deadline=start+4500ms, kill deadline=start+6000ms and listener/evidence tail up to7000ms; preserve exit observation and all late page errors. A successful server close is not substitute evidence for a failed page stage. Preserve AggregateError primary cause/order, do not suppress timeout/target-closed errors wholesale. Synchronous call throws remain cleanup errors and do not bypass later cleanup.
5. If instrumentation instead reveals an independent acquisition/ownership defect, report the precise phase before changing scope. Do not preemptively replace launchServer/connect, introduce context pooling, serialize the whole suite or rewrite B0. This slice is complete only with evidence and tests for the specific deadline invariant, not merely changed timeout text.

## Meaningful finite acceptance

| Variant                                | Required oracle                                                                                                                                                                                                                                                                                                                 |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Delayed close fits existing page share | Test-only gate delays close completion beyond500ms but below allocated pageEnd; still performs real page.close once. Success, correct disposedPages, observed browser exit/listener closure, cleanup within unchanged envelope. The old fixed500ms candidate fails this case. Record allocated and actual close duration.       |
| Two owned pages                        | Existing cleanup-two-pages scenario with delayed close on each; both close, no page starves its fair share, unused time is available to later cleanup. Keep prior700ms disposal success case.                                                                                                                                   |
| Never-settling close                   | First page close completion gate never resolves: fail at its phase, retain close timeout, attempt later owned pages, close/kill owned browser, observe exit/listener closed; no second page.close for the same owner and no detached rejection. Do not assert two successful page closes when one is deliberately unobservable. |
| Close reject/late settle               | Rejected close plus primary work error preserves both; a close that settles after timeout/browser teardown neither converts failure to success nor double-counts. Unit-level controlled seam may complement real subprocess checks; no global Playwright/fs monkeypatch.                                                        |
| Existing fallback/cause checks         | Keep hung disposal, hung browser graceful close with real owned kill, late page error, and simultaneous primary+cleanup failure. Retain Q1 two-page and V1-L6 lifecycle oracles and B0 negative/version/external-request checks.                                                                                                |
| Real V1 lifecycle                      | V1-L1 and V1-L3 plus existing V1 suite use ordinary real page.close; success/evidence must show resources closed. Controlled delay proves allocation, not hosted-load reproduction.                                                                                                                                             |

Use a narrow test-only close-operation seam in the existing fixture harness, disabled normally, with fixed finite scenarios. Gate injection must not skip actual resource cleanup: browser owner still closes/kills; subprocess finally/watchdog may terminate only its verified owned process. No live user's browser/process/port touched. Retain existing wrapper ceilings and truthful timeout errors. Avoid elapsed-time tests that expect exact milliseconds: assert stage budgets/order and bounded completion, with existing scheduler tolerance only; never weaken the seven-second algorithmic deadline.

Validation sequence: source-scoped formatting/lint/build; focused deterministic cleanup subprocess cases and affected B0/Q1/V1 suite; freeze the coherent source; separate independent verification on exact Node26.10.0/pnpm12.6.0/Playwright1.63.0/Chromium1243 and required macOS/Linux, plus separate Sol-only review. Bounded review prefers TraeX GPT-5.6 Sol XHIGH; interactive native verification uses a separately assigned Sol HIGH (not the implementer/reviewer). Integration owner runs registered full gates, reads hosted artifacts and final-main dual-OS CI before releasing N1. No count-only acceptance: verify test inventory, page/process/listener evidence and preserved negative cases. Each meaningful source revision receives current evidence; no repeat-until-green policy.

## Orca comparison and exit

Read pinned Orca5534462b50c660888487a2108700d4cf284270db only. Targeted tests/config search found no launchServer/browserServer.kill/cleanupDeadline counterpart. tests/e2e/helpers/browser-link-server.ts has an awaited HTTP-server close callback, but no Cove-style seven-second managed Chromium process/page cleanup contract; it is not a suitable repair to copy. This is a bounded inspected-scope conclusion, not a claim that Orca contains no browser teardown code.

DAG: issue40 plan → interactive repair → frozen source → independent verification + independent Sol review → PR dual-OS CI → serial integration/final-main dual-OS evidence → QI accepted → N1. No product decision, M0 expansion or devbox retry is needed. Failure of the bounded candidate yields its exact stage evidence for further diagnosis, not a timeout waiver.

## Implementation allocation (prepared before source edits)

Owner: `/root/m0_t1_impl`, native GPT-6 Sol high, in the primary checkout `/Users/luchengxuan/WORKSPACE/cove`, branch `p/luchengxuan/m0-40-browser-cleanup` from exact main `b1af3f2510c52b2d03b37d02613b6dcbc5fccf5b`. The copied Astra plan source is `/tmp/cove-m0-browser-cleanup-plan.md`, SHA-256 `d5a6e519ad8f7da530c761d0ab504fa5052ce8d948823c67a131c5ae078d535d`. Its separate bounded plan review is pending; source edits wait for that verdict and root release.

Writable implementation paths are `packages/terminal-web/probes/node/managed-browser.ts`, the existing cleanup-only scenario in `packages/terminal-web/probes/node/query-input.ts`, `packages/terminal-web/probes/query-input.test.mjs`, `packages/terminal-web/tests/view-lifecycle.test.mjs`, and `packages/terminal-web/tests/view-browser-runner.mjs` only for the finite cleanup cases and evidence. This task plan and its results record are task-owned. Shared handoff/CI registration remains with the designated integration writer; this owner is also that writer if a genuinely necessary root gate change arises, but no registration change is planned. Production V1 renderer, protocol, engine, native worker, package manifests, pins, lockfile, unrelated probes and other checkouts are excluded.

The current required macOS/Linux main gate is red on issue #40 and W1 N1 remains held. The implementation must retain the accepted seven-second cleanup envelope, owned resource checks, fail-closed errors, and distinct work/cleanup deadlines. Before coding, inspect the installed pinned Playwright ownership path. After a coherent correction, run scoped formatting/lint/build and deterministic cleanup plus affected B0/Q1/V1 tests, then freeze a reviewable source commit for independent verification and separate review. A complete registered gate, hosted artifacts and final-main checks remain integration gates; no green rerun is claimed by this allocation.
