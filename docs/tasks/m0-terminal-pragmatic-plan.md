# Revised T1: pragmatic logical-grid recovery

Planning base: Cove `ca3006a`. User approved proceeding with the pragmatic Orca-like decision; this document implements that changed acceptance, not a request to approve it again. Coordinator must commit the revised contract/plan before implementation. Prepared by `/root/m0_p1a_plan` (GPT-6 Astra high), integrated by the coordinator before implementation. No implementation or tests were performed by the planner.

The confirmed first-release agent matrix is **codex, traex, claude code**. Independent Sol-high read-only inventory observed Codex 0.155.1, TraeX 0.205.1 internal, and Claude Code 2.1.220 on this host. Runnable authenticated sessions remain unverified; inventory is not live-agent qualification. No further agent-list question is pending. Real agent workflows are primary product qualification; headless corpus success is necessary implementation evidence, not a substitute for them.

## Revised outcome and limits

Implement a small source-derived recovery candidate at the source's **current logical grid** using serialized VT plus necessary current modes, saved position and bounded parser tail. Pause the inverse-reflow generator and maintained installation API. No xterm patches, source resize/mutation, private cell installation, permanent mirror, unlimited raw log or production protocol changes.

Require practical current-grid content and common continuation in normal and alternate, including budgeted normal content while alternate is active. Do not copy mobile's normal-prefix deletion. Preserve current effective pen/charset, cursor, current pending wrap and supported modes needed by ordinary output. Saved SGR/charset, exact saved pending-wrap semantics beyond position restoration, off-grid retained cells and mixed physical row-capacity equality become explicitly documented **non-gating diagnostics**. This is a user-approved change of fidelity contract; old failures remain failures under the old contract.

A genuine ordered authority resize may rely on application repaint and/or a new authoritative baseline. A spectator must not resize the source. Same-grid recovery must not manufacture a PTY resize, size pulse or SIGWINCH to hide loss. Application repaint is not permission to show an incorrect same-grid baseline indefinitely. The baseline must first meet the revised current-grid oracle; resize/repaint readiness is a later integration obligation.

## R1–R8 disposition

| Old row                    | Required T1 headless evidence now                                                                                                                                                                                                                                                           | Diagnostic / later-stage boundary                                                                                                                                                                                                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1 saved state             | Restore saved position for each buffer where the supported engine retains it, current position/pen/charset, and ordinary save→move→restore→print positioning. Clamp evicted saved rows according to the pinned engine's behavior. Test saved home too.                                      | Saved SGR/charset equality and suffixes whose only divergence follows their omitted restoration are diagnostic. Saved wrap-pending versus addressable saved position is explicitly a position-only limitation. Do not apply that exemption to current pending wrap or ordinary positioning errors. |
| R2 modes/margins/tabs/wrap | Current DECSTBM/DECOM, insert/wrap/reverse-wrap, visibility and agreed input modes; current-grid custom tabs; current pending wrap; ordinary print, scroll, erase, in-grid edits and CPR/mode reports.                                                                                      | Retained off-grid tab-map identity and mixed row capacities are diagnostic. If their omission can affect resize, mark local resize context incomplete and require authority handling; no tab-setter API. Saved-state-only exceptions follow R1.                                                    |
| R3 both buffers            | Source logical-grid image of active alternate plus necessary normal screen and budgeted history; ordinary 47/1047/1049 switching and exit continuation; normal-active recovery too. Preserve actual source clearing semantics.                                                              | Exact hidden off-grid capacities/cells and topology-dependent DCH/ICH differences are diagnostic. Visible missing normal text at installation or after ordinary alternate exit is still required failure. Desktop and later mobile share the preservation contract; no deletion of normal prefix.  |
| R4 partial parser          | Existing byte cuts and chunk partitions for UTF-8/ESC/CSI/OSC/DCS, cancellation and split terminators; checkpoint before unfinished work plus raw tail exactly once.                                                                                                                        | No downgrade. Real browser parsing parity comes later in V1/C3.                                                                                                                                                                                                                                    |
| R5 effects/final print     | LF/CR/BS/HT/SI/SO/BEL effects exactly once, no external replay actions; source-derived ordinary checkpoint refresh; common combining and REP, including existing joined/IRM/rejected-wide controls and 67-byte cluster.                                                                     | Do not compare private join scalar for its own sake. Common observable continuation remains required. Geometry-dependent continuation and saved-style-only divergence are separately labeled diagnostics, not reasons to drop all R5.                                                              |
| R6 history/resize          | 1000- and 3-line budget, source-grid replay and explicit coverage; Unicode/margin cases; ordinary same-size continuation. Simulate ordered true resize with source at new grid and fresh baseline if context is insufficient.                                                               | Exact inverse reflow and retained off-grid/mixed topology no longer gate. Reflow equivalence with full context remains informative. Actual app repaint/authority/observer behavior belongs to W2/P3/V1/H1/C3; no fake same-grid resize.                                                            |
| R7 queries/purity          | Existing supported query families and appearance fixture, exact live source responses, generation purity and separate inert receiver reply sink. No reply from reconstruction reaches source.                                                                                               | Q1 covers real client input/query separation; C3 proves integration with a real PTY and 0/1/2 views. A headless sink is not a browser or PTY proof.                                                                                                                                                |
| R8 bounds/negatives        | Existing limits, source purity, deterministic output, cap boundaries, pathological unfinished sequence handling, cleanup and malformed identity/chunk rejection. Negatives must catch visible text loss, wrong current cursor, duplicate C0/tail, input/query leakage and bound violations. | Old full-private oracle and strict source-derived G failures remain diagnostic evidence under their original contract. Ordinary valid required cases cannot pass as unavailable/skipped.                                                                                                           |

A suffix explicitly designed to expose omitted off-grid storage is diagnostic even if its eventual text differs. That does not excuse a current-grid content mismatch before that suffix. Conversely, a raw foreground difference on a visually inert null cell is not automatically a visible failure: compare effective rendering and the relevant continuation before classification. Keep exact observations as diagnostics, with narrowly documented equivalences. Never drop all hidden state or all styles from the oracle.

## Candidate and owned files

Continue using headless/browser 6.0.0 and addon-serialize 0.14.0 pins. Existing compiled probe export remains the entry. T1 implementation owner may edit:

- `packages/terminal-engine/probes/recovery-checkpoint.ts`, `recovery-boundaries.ts`: add/select the revised source-grid candidate, keep bounded checkpoint/tail and old entry behavior available for historical controls. Restore normal before alternate; supplemental VT restores saved positions and current registers without imposing saved style fidelity. No temporary wider receiver geometry in this candidate.
- A single concrete `packages/terminal-engine/probes/logical-grid-recovery.ts`, if isolation is clearer: source-grid generation and source-derived final-print composition. Reuse tested bounded primitives; do not import inverse row packing or receiver-diff repair. Do not rename or silently rewrite `source-derived-recovery.ts` into the new contract.
- `packages/terminal-engine/probes/recovery-observation.ts`: add a practical oracle alongside exact diagnostics. Compare logical-column cells/effective styles, necessary normal/history content, active/current cursor/modes and behavior; retain source/receiver full observations for bounded first-difference diagnostics. Document null/palette equivalences explicitly.
- Existing `recovery-state.test.mjs`, `recovery-parser.test.mjs`, `recovery-query.test.mjs`, `recovery-boundaries.test.mjs`, `recovery-join.test.mjs`: run required classifications against the new candidate. Keep existing strict/negative evidence rather than turning its expected mismatch into recovery success.
- New `packages/terminal-engine/probes/recovery-pragmatic.test.mjs`: finite cross-cutting matrix below. `recovery-geometry.test.mjs` and `recovery-source-derived.test.mjs` remain the historical/strict diagnostic suites; no further inverse-algorithm work.
- `tests/fixtures/terminal/engine/recovery-cases.mjs`, `recovery-evidence.mjs`: explicit contract, assertion class and bounded result aggregation; `docs/tasks/m0-terminal-recovery-plan.md` and `m0-terminal-spike.md`: changed acceptance and fresh evidence, while preserving old conclusions with their dated contract.

If stock serialization visits retained columns outside the logical grid and pollutes the visible replay, a bounded adapter repair may emit only the necessary logical-grid rows/cells using existing VT/style primitives. It must be derived from source, preserve Unicode/null/background/wrap behavior required here, and receive a minimized regression. This permits an ordinary writer repair, not a generic inverse solver, fixture literals, dependency patch or endless serializer rewrite. Reuse the source-derived J witness where independently qualified, separating it from geometry construction; keep all restoration before its final print. Charge complete clusters to the baseline budget rather than resurrecting the arbitrary 64-byte cap.

## Finite implementation and proof

Use two coherent, buildable commits after coordinator lands the plan:

1. Add the revised candidate and practical oracle with real compiled acceptance; adapt state/parser/query/boundary suites and evidence classifications. Preserve diagnostic exports/tests. A saved-position/current-pen case, both-buffer baseline+exit and split-tail query case must run in this first change; no empty registration-only commit.
2. Complete source-derived refresh/J composition, the fixed pragmatic matrix, independent verification and documentation/CI inventory. If the first change is already small and complete, combining is acceptable; avoid one large speculative rewrite.

Freeze **16 practical scenario families** before implementation, each with byte/coalesced partitions where relevant: normal current pen; alternate current pen; saved position normal; saved position alternate; active current wrap-pending; origin/margins+scroll; visible custom tabs; ordinary in-grid ICH/DCH/erase; normal→alternate→exit; source-normal snapshot after actual alternate clearing; three-line retained history; full-budget history; Unicode wide/combining at margins; incomplete UTF-8/CSI tail; OSC/DCS+C0 cancellation/tail; ordered real source resize then new-grid baseline. Reuse existing exhaustive parser cut/query cases rather than replace them with these 16 summaries.

Additionally retain the existing 131,073-ASCII-byte workload with **20 fresh checkpoint replacements**, J common suffix cases, 67-byte cluster and print→resize checkpoint. A fresh independent receiver validates before allowing the test journal to reset; production must not gain a permanent mirror. Separate historical off-grid/edit and saved-style cases are diagnostic by name, not counted among required passes. Test all mandatory cases on macOS and Linux, preserving existing parser/query cut counts and CI suite minima. No synthetic corpus can establish a real agent's interactive behavior.

Limits stay 120×40, 1000 history plus forced 3-line case, 8 MiB baseline, 64 KiB tail, 30-second per-case watchdog and bounded diagnostics. Account source capture/UTF-16/encoded overlap and operation counts; do not call payload bounds a measured heap guarantee. For long unfinished pathological sequences, live source parsing continues; bounded recovery retention may become unavailable until a new proved checkpoint. Ordinary printable output must refresh rather than hitting this exception.

Allow one implementation pass and one minimized **general repair per demonstrated required defect class**, followed by the whole required matrix. No fixture-specific patches, exact-private matching campaign or repeated expansion of exempt semantics. Stop and freeze a remaining required failure with its observable trigger, minimal source, budget impact and proposed next scope. A diagnostic-only divergence does not block this revised gate or justify more algorithm work. If a fix requires a dependency patch, altered source semantics or a new fidelity reduction, return that concrete decision to the coordinator rather than implementing it under this plan.

## Result vocabulary and evidence preservation

Evidence for each result links the exact tested SHA/pins/platform, `contract` (`strict-retained-v1` or `pragmatic-logical-grid-v1`), case ID, observation/continuation, `classification`, and `outcome`. Completed case IDs, committed assertions, bounded CI failure logs and the shared environment/pin artifact may jointly carry these fields; a duplicate per-cut report schema is not required:

- `required`: `pass` or `fail`; any generation/install/bounds/harness error or missing observation is fail.
- `diagnostic`: `match` or `divergence`, with precise approved limitation or semantic equivalence. Neither is a recovery pass.
- `integration-obligation`: `not-run`, `pass`, `fail` or `blocked-external`; these do not enter headless required-pass counts.

Report required passed/failed/total separately from diagnostic matches/divergences and later obligations. Preserve old strict trial commit/results/report hashes (including frozen `ab32c555` independent report) and their failure verdicts. New acceptance succeeds only through new tests under the explicitly changed contract, never relabeling the old G failure. A CI test that successfully asserts a diagnostic mismatch remains “diagnostic recorded,” not “restoration passed.”

Coordinator's registration writer owns `scripts/ci-test-gate.mjs` and associated tooling tests; register the new suite atomically with its actual first-step cases (minimum 3), then raise to the final minimum 16 scenario tests when all families land; assert actual variant counts inside. Preserve existing minima and package exports/pins; manifest/config changes only if concretely needed, serially assigned. Existing `.cache/ci/smoke/terminal-recovery/` artifact path is reused. Independent tester/reviewer verify the frozen candidate, oracle equivalences, required counts, historical evidence and compiled public probe consumption. Run normal required checks on that SHA, not repeated broad tests without a change.

## Real-agent qualification without a circular dependency

T1 gates P1b using the finite headless corpus, revised capability/limitation record and Q1 intersection. It **does not wait for H1/C3**, which depend on P1b. P1b freezes an initial pragmatic profile with explicit later validation obligations, not a statement of real-agent qualification. T2/W1/W2 implement authority and recovery; P3/V1 consume the contract; H1 supplies CLI plus two browser views; C3 exercises actual integration; X1 requires the final evidence. GUI mobile is deferred, not a prerequisite inserted into M0.

Explicit ownership follows the existing DAG; coordinator names the concrete agents/checkouts before each allocation:

| Owner                                        | Deliverable / boundary                                                                                                                                                                                                                       |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1 implementer + independent tester/reviewer | Compiled practical candidate, deterministic corpus, classified evidence; owns only assigned probe/fixture/task-doc paths. Does not implement PTY/client infrastructure.                                                                      |
| Coordinator / P1b protocol writer            | Commit accepted architecture and plan first; reconcile Q1/T1 profile; record superseded strict obligations and later real-agent gates. Registration writer alone edits shared inventory/config.                                              |
| T2 engine owner                              | Production logical-grid capture and bounded tail under frozen profile, with diagnostic limitations represented honestly.                                                                                                                     |
| W1/W2 execution owners                       | Real PTY lifecycle, single authority, ordered dimensions/output/recovery and no client-generated query reply or fake recovery resize.                                                                                                        |
| P3 controller + V1 browser owners            | Input/readiness/recovery ordering, observer restrictions, fresh browser installation and reply suppression.                                                                                                                                  |
| H1 harness owner                             | CLI/server wiring and two browser views; isolated agent launch/scenario controls and cleanup, consuming actual compiled public packages.                                                                                                     |
| C3 independent integration owner             | Actual three-agent workflow qualification on that chain, including resize/repaint and reconnect; record exact versions and real versus replay provenance. Coordinate benign task definitions with the user workflow, without making T1 wait. |
| X1 coordinator                               | Aggregate final-SHA M0 results and unresolved external blockers before user exit review. Small real-agent workflows belong to M0; 100 real-agent capacity remains M4. Existing C5/X1 100 synthetic-output real-PTY load is unchanged.        |

At C3/H1, execute **codex, traex and claude code** separately on isolated benign synthetic tasks and record binary/version, launch method, PTY dimensions and session provenance. Use the established TraeX launch workflow. Exercise output and streaming, prompt editing/submission, scroll/history, alternate entry/exit when the agent actually uses it, same-grid disconnect/reconnect, operator switch, observer attachment, true authority resize and app repaint, and 0/1/2-client query/input isolation. Record actions unsupported by a particular agent honestly; synthetic controls still cover the engine feature, but cannot be described as that agent's coverage. Authentication/service failures are external blockers, not successful product qualification. Do not reuse personal sessions or publish credentials/task data.

T1 may optionally capture bounded output from an actual local agent session on a synthetic task, once version/auth and session isolation are established. Label evidence separately as **live capture**, **trace replay**, or **live Cove end-to-end**. Only the last uses the actual server/worker/controller/browser chain. Such capture must not expand T1 into building that chain or create a circular gate. Actual agent findings remain the first-release go/no-go evidence at C3/X1; known user agents cannot be dropped because synthetic tests pass.

## Orca precedent and deliberate differences

Pinned Orca stable `5534462b50c660888487a2108700d4cf284270db` supplies useful precedent: logical capture-grid replay, supplemental modes/saved position, parsed-write ordering and source-documented saved SGR/charset trade (`src/shared/terminal-serialize-absolute-cursor.ts:43–68,109–115`). Desktop frame skipping/repaint at actual width mismatch is in `terminal-snapshot-replay-paint.ts:55–79,112–125`. These choices motivate the approved pragmatic profile, not wholesale code copying or beta dependency adoption.

Retain Cove's budgeted normal content across all clients; reject mobile initial-prefix deletion (`mobile/src/terminal/document/mouse-mode-decset-scan.ts:14–22`). Do not borrow desktop size pulses as a same-grid recovery repair. Borrow effective-style normalization as an oracle technique only with continuation evidence. Engine patches, exact off-grid topology and inverse-reflow construction remain paused. The new contract is practical and explicit about its limits, with real user-agent workflows deciding first-release readiness.

## Implementation allocation

After this plan is committed, `/root/m0_t1_impl` (GPT-6 Sol high) reuses its clean `cove-worktrees/m0-terminal-recovery` checkout with a new `p/luchengxuan/m0-13-pragmatic-recovery` branch at the coordinator-provided integrated plan SHA. Its older source branch is retained. It owns the probe/fixture/task-result paths enumerated above and remains sole writer for shared suite registration, tooling expectations and development instructions. No dependency pin/lock change is currently needed. Coordinator owns architecture/parent plans/handoff/execution and GitHub; independent verifier owns its separate checkout and temporary reports, separate Sol-high reviewer writes no implementation/tests. Exact base and writable entry are recorded before code.

## Implementer entry and execution scope

Entry verified clean at `03c4889fa2953530b994cf85e1ae21894c85c230` on the retained `p/luchengxuan/m0-13-recovery` branch. This checkout `/Users/luchengxuan/WORKSPACE/cove-worktrees/m0-terminal-recovery` now uses the new branch `p/luchengxuan/m0-13-pragmatic-recovery` created from exact integrated plan commit `04520a9a54e195fe1fa9f29da6980534297b4c4b` (parent revised contract `9c46ee568b62372976f820aa82b65181eec6c9fb`). `/root/m0_t1_impl` owns implementation and remains the sole shared registration writer; `/root` coordinates review, integration and GitHub. The objective is a compiled current-logical-grid candidate and practical oracle for the revised R1–R8 acceptance, with the old strict probes left intact as diagnostics.

Writable implementation paths are `packages/terminal-engine/probes/**`, `tests/fixtures/terminal/engine/**`, `docs/tasks/m0-terminal-recovery-plan.md`, `docs/tasks/m0-terminal-spike.md`, and this execution section. Shared registration paths are `scripts/ci-test-gate.mjs`, directly affected tooling tests and `docs/development.md`; package manifest/config may change only if a concrete new export or dependency requires it, and package pins/lockfile remain fixed. The source contract is [terminal architecture §3.2.1](../terminal-architecture.md), this plan, and the existing compiled `@cove/terminal-engine/probes/recovery-boundaries` export. The candidate must use source logical geometry, current-grid VT plus bounded tail, separated reply sinks and source purity; it may not resize/mutate the source, patch xterm, reuse the frozen inverse-reflow algorithm, add production wire/worker/client code, launch real agents or weaken the newly required common cases.

Validation starts with compiled package/scoped probe and inventory tests, preserves the exhaustive parser/query and strict diagnostic suites, then runs `pnpm check` once at the final registered source on macOS. Independent macOS/Linux verification and review are coordinator-owned after the frozen handoff; author success alone does not accept T1 or release P1b.
