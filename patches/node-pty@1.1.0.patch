diff --git a/lib/unixTerminal.js b/lib/unixTerminal.js
index 1ec12f796a822c78fba9ad7f6448c3987e325c23..9f35d15513cdfbe41b06391946c442aab48f09dc 100644
--- a/lib/unixTerminal.js
+++ b/lib/unixTerminal.js
@@ -13,7 +13,7 @@ var __extends = (this && this.__extends) || (function () {
     };
 })();
 Object.defineProperty(exports, "__esModule", { value: true });
-exports.UnixTerminal = void 0;
+exports.__createBoundedWriteStreamForTest = exports.UnixTerminal = void 0;
 /**
  * Copyright (c) 2012-2015, Christopher Jeffrey (MIT License)
  * Copyright (c) 2016, Daniel Imms (MIT License).
@@ -33,6 +33,9 @@ helperPath = helperPath.replace('node_modules.asar', 'node_modules.asar.unpacked
 var DEFAULT_FILE = 'sh';
 var DEFAULT_NAME = 'xterm';
 var DESTROY_SOCKET_TIMEOUT_MS = 200;
+var MAX_BOUNDED_WRITE_BYTES = 64 * 1024;
+var MAX_BOUNDED_WRITE_TASKS = 256;
+var BOUNDED_WRITE_RETRY_DELAY_MS = 1;
 var UnixTerminal = /** @class */ (function (_super) {
     __extends(UnixTerminal, _super);
     function UnixTerminal(file, args, opt) {
@@ -47,6 +50,7 @@ var UnixTerminal = /** @class */ (function (_super) {
         args = args || [];
         file = file || DEFAULT_FILE;
         opt = opt || {};
+        var boundedWrite = validateBoundedWriteOptions(opt.boundedWrite);
         opt.env = opt.env || process.env;
         _this._cols = opt.cols || terminal_1.DEFAULT_COLS;
         _this._rows = opt.rows || terminal_1.DEFAULT_ROWS;
@@ -62,7 +66,13 @@ var UnixTerminal = /** @class */ (function (_super) {
         env.TERM = name;
         var parsedEnv = _this._parseEnv(env);
         var encoding = (opt.encoding === undefined ? 'utf8' : opt.encoding);
+        var adoptionFailed = false;
         var onexit = function (code, signal) {
+            if (adoptionFailed)
+                return;
+            if (_this._writeStream) {
+                _this._writeStream.dispose('closed');
+            }
             // XXX Sometimes a data event is emitted after exit. Wait til socket is
             // destroyed.
             if (!_this._emittedClose) {
@@ -88,58 +98,131 @@ var UnixTerminal = /** @class */ (function (_super) {
             }
             _this.emit('exit', code, signal);
         };
-        // fork
-        var term = pty.fork(file, args, parsedEnv, cwd, _this._cols, _this._rows, uid, gid, (encoding === 'utf8'), helperPath, onexit);
-        _this._socket = new tty.ReadStream(term.fd);
-        if (encoding !== null) {
-            _this._socket.setEncoding(encoding);
-        }
-        _this._writeStream = new CustomWriteStream(term.fd, (encoding || undefined));
-        // setup
-        _this._socket.on('error', function (err) {
-            // NOTE: fs.ReadStream gets EAGAIN twice at first:
-            if (err.code) {
-                if (~err.code.indexOf('EAGAIN')) {
+        if (boundedWrite && pty.coveBoundedWriterVersion !== 1) {
+            throw new Error('Bounded writer native capability is unavailable');
+        }
+        var term = boundedWrite
+            ? pty.fork(file, args, parsedEnv, cwd, _this._cols, _this._rows, uid, gid, encoding === 'utf8', helperPath, onexit, true)
+            : pty.fork(file, args, parsedEnv, cwd, _this._cols, _this._rows, uid, gid, encoding === 'utf8', helperPath, onexit);
+        var readOwner;
+        var writerOwner;
+        var readAdopted = false;
+        try {
+            if (boundedWrite && (!Number.isSafeInteger(term.fd) || term.fd < 0 ||
+                !Number.isSafeInteger(term.writeFd) || term.writeFd < 0 ||
+                term.writeFd === term.fd || typeof term.stopOwnedChild !== 'function'))
+                throw new Error('Invalid bounded writer native ownership result');
+            if (boundedWrite)
+                _this._boundedOwnedStop = term.stopOwnedChild;
+            // Retain the partial owner if Node's ReadStream constructor fails after adoption.
+            var socket = Object.create(tty.ReadStream.prototype);
+            try {
+                Reflect.apply(tty.ReadStream, socket, [term.fd]);
+                readAdopted = true;
+            }
+            catch (error) {
+                if (socket._handle)
+                    readAdopted = true;
+                if (readAdopted)
+                    readOwner = socket;
+                throw error;
+            }
+            readOwner = socket;
+            _this._socket = socket;
+            if (encoding !== null) {
+                _this._socket.setEncoding(encoding);
+            }
+            writerOwner = new CustomWriteStream(boundedWrite ? term.writeFd : term.fd, (encoding || undefined), boundedWrite);
+            _this._writeStream = writerOwner;
+            // setup
+            _this._socket.on('error', function (err) {
+                // NOTE: fs.ReadStream gets EAGAIN twice at first:
+                if (err.code) {
+                    if (~err.code.indexOf('EAGAIN')) {
+                        return;
+                    }
+                }
+                var isExitEio = !!err.code && (err.code.indexOf('errno 5') !== -1 || err.code.indexOf('EIO') !== -1);
+                // The distinct writer fd remains owned through any issued fs.write.
+                if (_this._writeStream) {
+                    _this._writeStream.dispose(isExitEio ? 'closed' : 'error', isExitEio ? undefined : err);
+                }
+                // close
+                _this._close();
+                // EIO on exit from fs.ReadStream:
+                if (!_this._emittedClose) {
+                    _this._emittedClose = true;
+                    _this.emit('close');
+                }
+                // EIO, happens when someone closes our child process: the only process in
+                // the terminal.
+                // node < 0.6.14: errno 5
+                // node >= 0.6.14: read EIO
+                if (isExitEio) {
+                    return;
+                }
+                // throw anything else
+                if (_this.listeners('error').length < 2) {
+                    throw err;
+                }
+            });
+            _this._pid = term.pid;
+            _this._fd = term.fd;
+            _this._pty = term.pty;
+            _this._file = file;
+            _this._name = name;
+            _this._readable = true;
+            _this._writable = true;
+            _this._socket.on('close', function () {
+                if (_this._emittedClose) {
                     return;
                 }
-            }
-            // close
-            _this._close();
-            // EIO on exit from fs.ReadStream:
-            if (!_this._emittedClose) {
                 _this._emittedClose = true;
+                if (_this._writeStream) {
+                    _this._writeStream.dispose('closed');
+                }
+                _this._close();
                 _this.emit('close');
-            }
-            // EIO, happens when someone closes our child process: the only process in
-            // the terminal.
-            // node < 0.6.14: errno 5
-            // node >= 0.6.14: read EIO
-            if (err.code) {
-                if (~err.code.indexOf('errno 5') || ~err.code.indexOf('EIO')) {
-                    return;
+            });
+            _this._forwardEvents();
+        }
+        catch (error) {
+            if (boundedWrite) {
+                adoptionFailed = true;
+                var cleanupErrors = [];
+                try {
+                    if (writerOwner)
+                        writerOwner.dispose('closed');
+                    else if (Number.isSafeInteger(term.writeFd) && term.writeFd >= 0 && term.writeFd !== term.fd)
+                        fs.closeSync(term.writeFd);
                 }
+                catch (cleanupError) {
+                    cleanupErrors.push(cleanupError);
+                }
+                try {
+                    if (readOwner)
+                        readOwner.destroy();
+                    else if (!readAdopted && Number.isSafeInteger(term.fd) && term.fd >= 0)
+                        fs.closeSync(term.fd);
+                }
+                catch (cleanupError) {
+                    cleanupErrors.push(cleanupError);
+                }
+                try {
+                    if (!term.stopOwnedChild || !term.stopOwnedChild(9)) {
+                        // A recorded reap is safe; absence of the stop closure is not.
+                        if (!term.stopOwnedChild)
+                            cleanupErrors.push(new Error('Owned child stop unavailable'));
+                    }
+                }
+                catch (cleanupError) {
+                    cleanupErrors.push(cleanupError);
+                }
+                if (cleanupErrors.length)
+                    throw new Error("Bounded PTY adoption failed with " + cleanupErrors.length + " cleanup error(s): " + String(error));
             }
-            // throw anything else
-            if (_this.listeners('error').length < 2) {
-                throw err;
-            }
-        });
-        _this._pid = term.pid;
-        _this._fd = term.fd;
-        _this._pty = term.pty;
-        _this._file = file;
-        _this._name = name;
-        _this._readable = true;
-        _this._writable = true;
-        _this._socket.on('close', function () {
-            if (_this._emittedClose) {
-                return;
-            }
-            _this._emittedClose = true;
-            _this._close();
-            _this.emit('close');
-        });
-        _this._forwardEvents();
+            throw error;
+        }
         return _this;
     }
     Object.defineProperty(UnixTerminal.prototype, "master", {
@@ -155,6 +238,39 @@ var UnixTerminal = /** @class */ (function (_super) {
     UnixTerminal.prototype._write = function (data) {
         this._writeStream.write(data);
     };
+    UnixTerminal.prototype._close = function () {
+        var keepBoundedWriteRejection = !!this._writeStream && this._writeStream.isBounded;
+        _super.prototype._close.call(this);
+        if (keepBoundedWriteRejection) {
+            // Terminal._close normally turns legacy writes into silent no-ops. On an
+            // opted-in bounded terminal they must still reach the retired writer and
+            // synchronously report that no bytes were admitted.
+            this.write = terminal_1.Terminal.prototype.write;
+        }
+    };
+    UnixTerminal.prototype.writeBounded = function (data, onSettled) {
+        return this._writeStream
+            ? this._writeStream.writeBounded(data, onSettled)
+            : { accepted: false, reason: 'not-enabled' };
+    };
+    UnixTerminal.prototype.getBoundedWriteState = function () {
+        return this._writeStream ? this._writeStream.getBoundedState() : undefined;
+    };
+    UnixTerminal.prototype.disposeBoundedWrite = function () {
+        if (!this._writeStream || !this._writeStream.isBounded) {
+            return false;
+        }
+        this._writeStream.dispose('closed');
+        return true;
+    };
+    Object.defineProperty(UnixTerminal.prototype, "boundedWriteCompletion", {
+        get: function () {
+            var _a;
+            return (_a = this._writeStream) === null || _a === void 0 ? void 0 : _a.boundedWriteCompletion;
+        },
+        enumerable: false,
+        configurable: true
+    });
     Object.defineProperty(UnixTerminal.prototype, "fd", {
         /* Accessors */
         get: function () { return this._fd; },
@@ -214,6 +330,9 @@ var UnixTerminal = /** @class */ (function (_super) {
     };
     UnixTerminal.prototype.destroy = function () {
         var _this = this;
+        if (this._writeStream) {
+            this._writeStream.dispose('closed');
+        }
         this._close();
         // Need to close the read stream so node stops reading a dead file
         // descriptor. Then we can safely SIGHUP the shell.
@@ -221,7 +340,6 @@ var UnixTerminal = /** @class */ (function (_super) {
             _this.kill('SIGHUP');
         });
         this._socket.destroy();
-        this._writeStream.dispose();
     };
     UnixTerminal.prototype.kill = function (signal) {
         try {
@@ -273,74 +391,361 @@ var UnixTerminal = /** @class */ (function (_super) {
     return UnixTerminal;
 }(terminal_1.Terminal));
 exports.UnixTerminal = UnixTerminal;
+var DEFAULT_WRITE_IO = {
+    write: function (fd, buffer, offset, callback) { return fs.write(fd, buffer, offset, callback); },
+    setImmediate: function (callback) { return setImmediate(callback); },
+    clearImmediate: function (handle) { return clearImmediate(handle); },
+    setTimeout: function (callback, delay) { return setTimeout(callback, delay); },
+    clearTimeout: function (handle) { return clearTimeout(handle); },
+    close: function (fd) { return fs.closeSync(fd); }
+};
+var NOOP_SETTLEMENT = function (_settlement) { };
+function validateBoundedWriteOptions(options) {
+    if (options === undefined) {
+        return undefined;
+    }
+    if (typeof options !== 'object' || options === null) {
+        throw new Error('boundedWrite must be an object');
+    }
+    if (!Number.isSafeInteger(options.maxAllocatedBytes) || options.maxAllocatedBytes <= 0 || options.maxAllocatedBytes > MAX_BOUNDED_WRITE_BYTES) {
+        throw new Error("boundedWrite.maxAllocatedBytes must be a positive safe integer no greater than " + MAX_BOUNDED_WRITE_BYTES);
+    }
+    if (!Number.isSafeInteger(options.maxTasks) || options.maxTasks <= 0 || options.maxTasks > MAX_BOUNDED_WRITE_TASKS) {
+        throw new Error("boundedWrite.maxTasks must be a positive safe integer no greater than " + MAX_BOUNDED_WRITE_TASKS);
+    }
+    return {
+        maxAllocatedBytes: options.maxAllocatedBytes,
+        maxTasks: options.maxTasks
+    };
+}
 /**
  * A custom write stream that writes directly to a file descriptor with proper
  * handling of backpressure and errors. This avoids some event loop exhaustion
  * issues that can occur when using the standard APIs in Node.
  */
 var CustomWriteStream = /** @class */ (function () {
-    function CustomWriteStream(_fd, _encoding) {
+    function CustomWriteStream(_fd, _encoding, _boundedOptions, _io) {
+        var _this = this;
+        if (_io === void 0) { _io = DEFAULT_WRITE_IO; }
         this._fd = _fd;
         this._encoding = _encoding;
+        this._boundedOptions = _boundedOptions;
+        this._io = _io;
         this._writeQueue = [];
+        this._state = 'open';
+        this._writeInFlight = false;
+        this._allocatedBytes = 0;
+        this._remainingBytes = 0;
+        this._retainedTasks = 0;
+        this._nextTicket = 1;
+        this._settlementDepth = 0;
+        this._closingOut = false;
+        if (_boundedOptions) {
+            this.boundedWriteCompletion = new Promise(function (resolve) { _this._resolveCompletion = resolve; });
+        }
     }
-    CustomWriteStream.prototype.dispose = function () {
-        clearImmediate(this._writeImmediate);
-        this._writeImmediate = undefined;
+    Object.defineProperty(CustomWriteStream.prototype, "isBounded", {
+        get: function () {
+            return this._boundedOptions !== undefined;
+        },
+        enumerable: false,
+        configurable: true
+    });
+    CustomWriteStream.prototype.dispose = function (status, error) {
+        if (status === void 0) { status = 'closed'; }
+        if (this._state !== 'open')
+            return;
+        this._state = status;
+        this._retirementError = error;
+        this._io.clearImmediate(this._turn);
+        this._turn = undefined;
+        this._io.clearTimeout(this._retryTimer);
+        this._retryTimer = undefined;
+        if (!this._boundedOptions) {
+            this._writeQueue.length = 0;
+            return;
+        }
+        this._finishRetirement();
     };
     CustomWriteStream.prototype.write = function (data) {
-        // Writes are put in a queue and processed asynchronously in order to handle
-        // backpressure from the kernel buffer.
-        var buffer = typeof data === 'string'
-            ? Buffer.from(data, this._encoding)
-            : Buffer.from(data);
-        if (buffer.byteLength !== 0) {
+        if (this._boundedOptions) {
+            var admission = this.writeBounded(data, NOOP_SETTLEMENT);
+            if (!admission.accepted) {
+                var error = new Error("Bounded pty write rejected: " + admission.reason);
+                error.code = "PTY_WRITE_" + String(admission.reason).toUpperCase().replace('-', '_');
+                throw error;
+            }
+            return;
+        }
+        var buffer = typeof data === 'string' ? Buffer.from(data, this._encoding) : Buffer.from(data);
+        if (buffer.byteLength) {
             this._writeQueue.push({ buffer: buffer, offset: 0 });
-            if (this._writeQueue.length === 1) {
+            if (this._writeQueue.length === 1)
                 this._processWriteQueue();
-            }
         }
     };
+    CustomWriteStream.prototype.writeBounded = function (data, onSettled) {
+        if (!this._boundedOptions)
+            return { accepted: false, reason: 'not-enabled' };
+        if (this._state !== 'open')
+            return { accepted: false, reason: this._state };
+        if ((typeof data !== 'string' && !Buffer.isBuffer(data)) || typeof onSettled !== 'function') {
+            return { accepted: false, reason: 'invalid-data' };
+        }
+        var byteLength = typeof data === 'string' ? Buffer.byteLength(data, this._encoding) : data.byteLength;
+        if (!byteLength)
+            return { accepted: false, reason: 'empty' };
+        if (this._retainedTasks >= this._boundedOptions.maxTasks)
+            return { accepted: false, reason: 'task-limit' };
+        if (byteLength > this._boundedOptions.maxAllocatedBytes - this._allocatedBytes)
+            return { accepted: false, reason: 'byte-limit' };
+        if (!Number.isSafeInteger(this._nextTicket))
+            return { accepted: false, reason: 'ticket-exhausted' };
+        // Admission accounts for the detached copy and ticket before any drain.
+        var buffer = typeof data === 'string' ? Buffer.from(data, this._encoding) : Buffer.from(data);
+        var ticket = this._nextTicket++;
+        this._allocatedBytes += buffer.byteLength;
+        this._remainingBytes += buffer.byteLength;
+        this._retainedTasks++;
+        this._writeQueue.push({ buffer: buffer, offset: 0, ticket: ticket, onSettled: onSettled });
+        this._scheduleTurn();
+        return { accepted: true, ticket: ticket, byteLength: buffer.byteLength };
+    };
+    CustomWriteStream.prototype.getBoundedState = function () {
+        if (!this._boundedOptions)
+            return undefined;
+        return {
+            state: this._state,
+            allocatedBytes: this._allocatedBytes,
+            remainingBytes: this._remainingBytes,
+            tasks: this._retainedTasks,
+            queuedTasks: this._state === 'open' ? this._writeQueue.length : 0,
+            writeInFlight: this._writeInFlight,
+            retryScheduled: this._retryTimer !== undefined,
+            turnScheduled: this._turn !== undefined
+        };
+    };
+    CustomWriteStream.prototype._scheduleTurn = function () {
+        var _this = this;
+        if (this._state !== 'open' || this._writeInFlight || this._turn !== undefined ||
+            this._retryTimer !== undefined || this._writeQueue.length === 0)
+            return;
+        this._turn = this._io.setImmediate(function () { return _this._processWriteQueue(); });
+    };
+    CustomWriteStream.prototype._scheduleRetry = function () {
+        var _this = this;
+        if (this._state !== 'open' || this._retryTimer !== undefined ||
+            this._turn !== undefined || this._writeQueue.length === 0)
+            return;
+        this._retryTimer = this._io.setTimeout(function () {
+            _this._retryTimer = undefined;
+            _this._scheduleTurn();
+        }, BOUNDED_WRITE_RETRY_DELAY_MS);
+    };
     CustomWriteStream.prototype._processWriteQueue = function () {
         var _this = this;
-        this._writeImmediate = undefined;
-        if (this._writeQueue.length === 0) {
+        this._turn = undefined;
+        if (this._state !== 'open' || this._writeInFlight || !this._writeQueue.length)
             return;
-        }
         var task = this._writeQueue[0];
-        // Write to the underlying file descriptor and handle it directly, rather
-        // than using the `net.Socket`/`tty.WriteStream` wrappers which swallow and
-        // mask errors like EAGAIN and can cause the thread to block indefinitely.
-        fs.write(this._fd, task.buffer, task.offset, function (err, written) {
-            if (err) {
-                if ('code' in err && err.code === 'EAGAIN') {
-                    // `setImmediate` is used to yield to the event loop and re-attempt
-                    // the write later.
-                    _this._writeImmediate = setImmediate(function () { return _this._processWriteQueue(); });
+        var issue = {};
+        this._activeIssue = issue;
+        this._writeInFlight = true;
+        var submitted = false;
+        try {
+            this._io.write(this._fd, task.buffer, task.offset, function (err, written) {
+                // A stale callback cannot retire a newer issued write.
+                if (_this._activeIssue !== issue) {
+                    if (_this._boundedOptions) {
+                        var invalid = new Error('Stale fs.write callback');
+                        invalid.code = 'EIO';
+                        _this.dispose('error', invalid);
+                    }
+                    return;
+                }
+                _this._activeIssue = undefined;
+                _this._writeInFlight = false;
+                _this._onWriteComplete(task, err, written);
+            });
+            submitted = true;
+        }
+        catch (error) {
+            // The seam, like fs.write, throws only before handing off a callback.
+            if (!submitted && this._activeIssue === issue) {
+                this._activeIssue = undefined;
+                this._writeInFlight = false;
+                var writeError = error instanceof Error ? error : new Error(String(error));
+                if (this._boundedOptions)
+                    this.dispose('error', writeError);
+                else {
+                    this._writeQueue.length = 0;
+                    console.error('Unhandled pty write error', writeError);
                 }
+            }
+            else {
+                throw error;
+            }
+        }
+    };
+    CustomWriteStream.prototype._onWriteComplete = function (task, err, written) {
+        if (!this._boundedOptions) {
+            if (err) {
+                if (err.code === 'EAGAIN')
+                    this._scheduleTurn();
                 else {
-                    // Stop processing immediately on unexpected error and log
-                    _this._writeQueue.length = 0;
+                    this._writeQueue.length = 0;
                     console.error('Unhandled pty write error', err);
                 }
                 return;
             }
+            if (written > 0) {
+                task.offset += written;
+                if (task.offset >= task.buffer.byteLength)
+                    this._writeQueue.shift();
+            }
+            this._scheduleTurn();
+            return;
+        }
+        if (err && Number.isSafeInteger(written) && written > 0 &&
+            written <= task.buffer.byteLength - task.offset) {
+            task.offset += written;
+            this._remainingBytes -= written;
+        }
+        if (this._state !== 'open' && err && err.code !== 'EAGAIN') {
+            this._state = 'error';
+            this._retirementError = err;
+            this._finishRetirement();
+            return;
+        }
+        if (err && err.code !== 'EAGAIN') {
+            this.dispose('error', err);
+            return;
+        }
+        if (!err) {
+            if (!Number.isSafeInteger(written) || written < 0 || written > task.buffer.byteLength - task.offset) {
+                var invalid = new Error("Invalid fs.write byte count: " + written);
+                invalid.code = 'EIO';
+                if (this._state === 'open')
+                    this.dispose('error', invalid);
+                else {
+                    this._state = 'error';
+                    this._retirementError = invalid;
+                    this._finishRetirement();
+                }
+                return;
+            }
             task.offset += written;
-            if (task.offset >= task.buffer.byteLength) {
-                _this._writeQueue.shift();
+            this._remainingBytes -= written;
+        }
+        if (this._state !== 'open') {
+            this._finishRetirement();
+            return;
+        }
+        if (task.offset === task.buffer.byteLength) {
+            this._writeQueue.shift();
+            try {
+                this._settleAndRelease(task, 'written');
             }
-            // Since there is more room in the kernel buffer, we can continue to write
-            // until we hit EAGAIN or exhaust the queue.
-            //
-            // Note that old versions of bash, like v3.2 which ships in macOS, appears
-            // to have a bug in its readline implementation that causes data
-            // corruption when writes to the pty happens too quickly. Instead of
-            // trying to workaround that we just accept it so that large pastes are as
-            // fast as possible.
-            // Context: https://github.com/microsoft/node-pty/issues/833
-            _this._processWriteQueue();
-        });
+            catch (error) {
+                var callbackError = error instanceof Error ? error : new Error(String(error));
+                try {
+                    this.dispose('error', callbackError);
+                }
+                finally {
+                    throw error;
+                }
+            }
+        }
+        if (this._state === 'open') {
+            if (err || written === 0)
+                this._scheduleRetry();
+            else
+                this._scheduleTurn();
+        }
+    };
+    CustomWriteStream.prototype._finishRetirement = function () {
+        if (this._state === 'open' || this._writeInFlight || this._closingOut || this._ownerResult)
+            return;
+        this._closingOut = true;
+        try {
+            // Surrender ownership before close: EINTR/unknown close state cannot be
+            // retried on a numeric fd that another owner may already have reused.
+            var fd = this._fd;
+            this._fd = -1;
+            try {
+                this._io.close(fd);
+                this._ownerResult = { kind: 'closed' };
+            }
+            catch (error) {
+                this._ownerResult = {
+                    kind: 'close-uncertain',
+                    error: error instanceof Error ? error.message : String(error)
+                };
+            }
+            var tasks = this._writeQueue.splice(0);
+            var firstError = void 0;
+            for (var _i = 0, tasks_1 = tasks; _i < tasks_1.length; _i++) {
+                var task = tasks_1[_i];
+                var status = task.offset === task.buffer.byteLength ? 'written' : this._state;
+                try {
+                    this._settleAndRelease(task, status, this._retirementError);
+                }
+                catch (error) {
+                    if (firstError === undefined)
+                        firstError = error;
+                }
+            }
+            if (firstError !== undefined)
+                throw firstError;
+        }
+        finally {
+            this._closingOut = false;
+            this._maybeComplete();
+        }
+    };
+    CustomWriteStream.prototype._settleAndRelease = function (task, status, error) {
+        if (task.ticket === undefined || task.settled)
+            return;
+        task.settled = true;
+        var settlement = {
+            ticket: task.ticket,
+            status: status,
+            originalBytes: task.buffer.byteLength,
+            writtenBytes: task.offset,
+            remainingBytes: task.buffer.byteLength - task.offset
+        };
+        if (error) {
+            settlement.errorCode = error.code;
+            settlement.errorMessage = error.message;
+        }
+        this._settlementDepth++;
+        try {
+            task.onSettled(settlement);
+        }
+        finally {
+            this._allocatedBytes -= task.buffer.byteLength;
+            this._remainingBytes -= task.buffer.byteLength - task.offset;
+            this._retainedTasks--;
+            task.ticket = undefined;
+            task.onSettled = undefined;
+            this._settlementDepth--;
+            this._maybeComplete();
+        }
+    };
+    CustomWriteStream.prototype._maybeComplete = function () {
+        if (this._ownerResult && !this._closingOut && !this._writeInFlight &&
+            this._settlementDepth === 0 && this._retainedTasks === 0) {
+            var resolve = this._resolveCompletion;
+            this._resolveCompletion = undefined;
+            if (resolve)
+                resolve(this._ownerResult);
+        }
     };
     return CustomWriteStream;
 }());
+/** @internal Test seam for deterministic package-local writer fault tests. */
+function __createBoundedWriteStreamForTest(fd, encoding, options, io) {
+    return new CustomWriteStream(fd, encoding, validateBoundedWriteOptions(options), io);
+}
+exports.__createBoundedWriteStreamForTest = __createBoundedWriteStreamForTest;
 //# sourceMappingURL=unixTerminal.js.map
\ No newline at end of file
diff --git a/lib/unixTerminal.js.map b/lib/unixTerminal.js.map
index 6e4bdc12dc4c1d2f113f608f4aa3e215e3f746a4..0565fe481b976bf374a579464e8882489380a326 100644
--- a/lib/unixTerminal.js.map
+++ b/lib/unixTerminal.js.map
@@ -1 +1 @@
-{"version":3,"file":"unixTerminal.js","sourceRoot":"","sources":["../src/unixTerminal.ts"],"names":[],"mappings":";;;;;;;;;;;;;;;;AAAA;;;;GAIG;AACH,uBAAyB;AAEzB,2BAA6B;AAC7B,yBAA2B;AAC3B,uCAAkE;AAGlE,iCAAmD;AAEnD,IAAM,MAAM,GAAG,wBAAgB,CAAC,KAAK,CAAC,CAAC;AACvC,IAAM,GAAG,GAAgB,MAAM,CAAC,MAAM,CAAC;AACvC,IAAI,UAAU,GAAG,MAAM,CAAC,GAAG,GAAG,eAAe,CAAC;AAC9C,UAAU,GAAG,IAAI,CAAC,OAAO,CAAC,SAAS,EAAE,UAAU,CAAC,CAAC;AACjD,UAAU,GAAG,UAAU,CAAC,OAAO,CAAC,UAAU,EAAE,mBAAmB,CAAC,CAAC;AACjE,UAAU,GAAG,UAAU,CAAC,OAAO,CAAC,mBAAmB,EAAE,4BAA4B,CAAC,CAAC;AAEnF,IAAM,YAAY,GAAG,IAAI,CAAC;AAC1B,IAAM,YAAY,GAAG,OAAO,CAAC;AAC7B,IAAM,yBAAyB,GAAG,GAAG,CAAC;AAEtC;IAAkC,gCAAQ;IAqBxC,sBAAY,IAAa,EAAE,IAAwB,EAAE,GAAqB;;QAA1E,YACE,kBAAM,GAAG,CAAC,SAuHX;QAnIO,iBAAW,GAAY,KAAK,CAAC;QAC7B,mBAAa,GAAY,KAAK,CAAC;QAarC,IAAI,OAAO,IAAI,KAAK,QAAQ,EAAE;YAC5B,MAAM,IAAI,KAAK,CAAC,4CAA4C,CAAC,CAAC;SAC/D;QAED,uBAAuB;QACvB,IAAI,GAAG,IAAI,IAAI,EAAE,CAAC;QAClB,IAAI,GAAG,IAAI,IAAI,YAAY,CAAC;QAC5B,GAAG,GAAG,GAAG,IAAI,EAAE,CAAC;QAChB,GAAG,CAAC,GAAG,GAAG,GAAG,CAAC,GAAG,IAAI,OAAO,CAAC,GAAG,CAAC;QAEjC,KAAI,CAAC,KAAK,GAAG,GAAG,CAAC,IAAI,IAAI,uBAAY,CAAC;QACtC,KAAI,CAAC,KAAK,GAAG,GAAG,CAAC,IAAI,IAAI,uBAAY,CAAC;QACtC,IAAM,GAAG,SAAG,GAAG,CAAC,GAAG,mCAAI,CAAC,CAAC,
QACxB,IAAI;YACF,IAAI,CAAC,SAAU,CAAC,UAAU,CAAC,CAAC;SAC7B;gBAAS;YACR,IAAI,CAAC,eAAe,IAAI,IAAI,CAAC,MAAM,CAAC,UAAU,CAAC;YAC/C,IAAI,CAAC,eAAe,IAAI,IAAI,CAAC,MAAM,CAAC,UAAU,GAAG,IAAI,CAAC,MAAM,CAAC;YAC7D,IAAI,CAAC,cAAc,EAAE,CAAC;YACtB,IAAI,CAAC,MAAM,GAAG,SAAS,CAAC;YACxB,IAAI,CAAC,SAAS,GAAG,SAAS,CAAC;YAC3B,IAAI,CAAC,gBAAgB,EAAE,CAAC;YACxB,IAAI,CAAC,cAAc,EAAE,CAAC;SACvB;IACH,CAAC;IAEO,0CAAc,GAAtB;QACE,IAAI,IAAI,CAAC,YAAY,IAAI,CAAC,IAAI,CAAC,WAAW,IAAI,CAAC,IAAI,CAAC,cAAc;YAC9D,IAAI,CAAC,gBAAgB,KAAK,CAAC,IAAI,IAAI,CAAC,cAAc,KAAK,CAAC,EAAE;YAC5D,IAAM,OAAO,GAAG,IAAI,CAAC,kBAAkB,CAAC;YACxC,IAAI,CAAC,kBAAkB,GAAG,SAAS,CAAC;YACpC,IAAI,OAAO;gBAAE,OAAO,CAAC,IAAI,CAAC,YAAY,CAAC,CAAC;SACzC;IACH,CAAC;IACH,wBAAC;AAAD,CAAC,AAtSD,IAsSC;AAED,8EAA8E;AAC9E,SAAgB,iCAAiC,CAC/C,EAAU,EACV,QAAwB,EACxB,OAA6B,EAC7B,EAAY;IAEZ,OAAO,IAAI,iBAAiB,CAAC,EAAE,EAAE,QAAQ,EAAE,2BAA2B,CAAC,OAAO,CAAC,EAAE,EAAE,CAAC,CAAC;AACvF,CAAC;AAPD,8EAOC"}
\ No newline at end of file
diff --git a/scripts/prebuild.js b/scripts/prebuild.js
index 17f1d980e62e017d77058627a7a15c4c29f9470c..0646e37b0e30323f1983da9b799246f35648e98d 100644
--- a/scripts/prebuild.js
+++ b/scripts/prebuild.js
@@ -17,6 +17,12 @@ const path = require('path');
 const PREBUILDS_ROOT = path.join(__dirname, '..', 'prebuilds');
 const PREBUILD_DIR = path.join(__dirname, '..', 'prebuilds', `${process.platform}-${process.arch}`);
 
+// Cove's Unix addon carries the bounded writer fd owner; stock prebuilds do not.
+if (process.platform === 'darwin' || process.platform === 'linux') {
+  fs.rmSync(PREBUILDS_ROOT, { recursive: true, force: true });
+  process.exit(1);
+}
+
 // Do not use prebuilds when npm_config_build_from_source is set
 if (process.env.npm_config_build_from_source === 'true') {
   console.log('\x1b[33m> Removing prebuilds and rebuilding because npm_config_build_from_source is set\x1b[0m');
diff --git a/src/interfaces.ts b/src/interfaces.ts
index a269e77bd2acf31c762b0fd8e13e6bf435fd7d07..e62dd82e030f580b5ee4af60e79c65c4db4cec03 100644
--- a/src/interfaces.ts
+++ b/src/interfaces.ts
@@ -7,6 +7,50 @@ export interface IProcessEnv {
   [key: string]: string | undefined;
 }
 
+export interface IBoundedWriteOptions {
+  maxAllocatedBytes: number;
+  maxTasks: number;
+}
+
+export type BoundedWriteRejectionReason =
+  | 'not-enabled'
+  | 'closed'
+  | 'error'
+  | 'empty'
+  | 'invalid-data'
+  | 'byte-limit'
+  | 'task-limit'
+  | 'ticket-exhausted';
+
+export type IBoundedWriteAdmission =
+  | { accepted: true; ticket: number; byteLength: number }
+  | { accepted: false; reason: BoundedWriteRejectionReason };
+
+export interface IBoundedWriteSettlement {
+  ticket: number;
+  status: 'written' | 'error' | 'closed';
+  originalBytes: number;
+  writtenBytes: number;
+  remainingBytes: number;
+  errorCode?: string;
+  errorMessage?: string;
+}
+
+export type IBoundedWriteOwnerResult =
+  | { kind: 'closed' }
+  | { kind: 'close-uncertain'; error: string };
+
+export interface IBoundedWriteState {
+  state: 'open' | 'error' | 'closed';
+  allocatedBytes: number;
+  remainingBytes: number;
+  tasks: number;
+  queuedTasks: number;
+  writeInFlight: boolean;
+  retryScheduled: boolean;
+  turnScheduled: boolean;
+}
+
 export interface ITerminal {
   /**
    * Gets the name of the process.
@@ -24,6 +68,18 @@ export interface ITerminal {
    */
   write(data: string | Buffer): void;
 
+  /** Available only for Unix terminals spawned with boundedWrite enabled. */
+  writeBounded?(data: string | Buffer, onSettled: (settlement: IBoundedWriteSettlement) => void): IBoundedWriteAdmission;
+
+  /** Available only for Unix terminals. */
+  getBoundedWriteState?(): IBoundedWriteState | undefined;
+
+  /** Available only for Unix terminals. */
+  disposeBoundedWrite?(): boolean;
+
+  /** Settles after the bounded writer closes and all owned callbacks unwind. */
+  readonly boundedWriteCompletion?: Promise<IBoundedWriteOwnerResult>;
+
   /**
    * Resize the pty.
    * @param cols The number of columns.
@@ -115,6 +171,7 @@ interface IBasePtyForkOptions {
 export interface IPtyForkOptions extends IBasePtyForkOptions {
   uid?: number;
   gid?: number;
+  boundedWrite?: IBoundedWriteOptions;
 }
 
 export interface IWindowsPtyForkOptions extends IBasePtyForkOptions {
diff --git a/src/native.d.ts b/src/native.d.ts
index c53e086b4968d6fb0bf601ace3a813809433fedb..4088e4b509648694a7f075aea9663c1005eccd65 100644
--- a/src/native.d.ts
+++ b/src/native.d.ts
@@ -19,7 +19,8 @@ interface IWinptyNative {
 }
 
 interface IUnixNative {
-  fork(file: string, args: string[], parsedEnv: string[], cwd: string, cols: number, rows: number, uid: number, gid: number, useUtf8: boolean, helperPath: string, onExitCallback: (code: number, signal: number) => void): IUnixProcess;
+  coveBoundedWriterVersion?: number;
+  fork(file: string, args: string[], parsedEnv: string[], cwd: string, cols: number, rows: number, uid: number, gid: number, useUtf8: boolean, helperPath: string, onExitCallback: (code: number, signal: number) => void, boundedWriter?: boolean): IUnixProcess;
   open(cols: number, rows: number): IUnixOpenProcess;
   process(fd: number, pty?: string): string;
   resize(fd: number, cols: number, rows: number): void;
@@ -43,6 +44,8 @@ interface IWinptyProcess {
 
 interface IUnixProcess {
   fd: number;
+  writeFd?: number;
+  stopOwnedChild?: (signal: number) => boolean;
   pid: number;
   pty: string;
 }
diff --git a/src/unix/pty.cc b/src/unix/pty.cc
index 7b4b9e1f990fbf95b51528bb56dc9717f5b87532..ebae7526886c3b95cf24782de107570ce2b271e2 100644
--- a/src/unix/pty.cc
+++ b/src/unix/pty.cc
@@ -24,6 +24,9 @@
 #include <string.h>
 #include <stdlib.h>
 #include <unistd.h>
+#include <chrono>
+#include <memory>
+#include <mutex>
 #include <thread>
 
 #include <sys/types.h>
@@ -110,6 +113,60 @@ struct ExitEvent {
   int exit_code = 0, signal_code = 0;
 };
 
+struct CoveOwnedChild {
+  explicit CoveOwnedChild(pid_t child_pid) : pid(child_pid) {}
+  std::mutex mutex;
+  pid_t pid;
+  bool reaped = false;
+};
+
+void SetupCoveOwnedExitCallback(Napi::Env env, Napi::Function cb,
+                                std::shared_ptr<CoveOwnedChild> child) {
+  auto thread = std::make_unique<std::thread>();
+  auto tsfn = std::make_shared<Napi::ThreadSafeFunction>(
+      Napi::ThreadSafeFunction::New(
+          env, cb, "CoveOwnedExitCallback", 0, 1,
+          [raw = thread.get()](Napi::Env) {
+            if (raw->joinable()) raw->join();
+            delete raw;
+          }));
+  std::thread* raw_thread = thread.release();
+  try {
+    *raw_thread = std::thread([tsfn, child]() {
+      int status = 0;
+      while (true) {
+        int result;
+        int saved_errno;
+        {
+          std::lock_guard<std::mutex> lock(child->mutex);
+          result = waitpid(child->pid, &status, WNOHANG);
+          saved_errno = errno;
+          if (result == child->pid) child->reaped = true;
+        }
+        if (result == child->pid) break;
+        if (result == -1 && saved_errno != EINTR) {
+          Napi::Error::Fatal("CoveOwnedExitCallback", "Owned child waitpid failed");
+        }
+        std::this_thread::sleep_for(std::chrono::milliseconds(20));
+      }
+      auto* event = new ExitEvent;
+      if (WIFEXITED(status)) event->exit_code = WEXITSTATUS(status);
+      if (WIFSIGNALED(status)) event->signal_code = WTERMSIG(status);
+      const auto result = tsfn->BlockingCall(
+          event, [](Napi::Env env, Napi::Function callback, ExitEvent* completed) {
+            callback.Call({Napi::Number::New(env, completed->exit_code),
+                           Napi::Number::New(env, completed->signal_code)});
+            delete completed;
+          });
+      if (result != napi_ok) delete event;
+      tsfn->Release();
+    });
+  } catch (...) {
+    tsfn->Release();
+    throw;
+  }
+}
+
 void SetupExitCallback(Napi::Env env, Napi::Function cb, pid_t pid) {
   std::thread *th = new std::thread;
   // Don't use Napi::AsyncWorker which is limited by UV_THREADPOOL_SIZE.
@@ -167,6 +224,8 @@ void SetupExitCallback(Napi::Env env, Napi::Function cb, pid_t pid) {
         }
       }
     }
+    // The wait thread owns the kqueue descriptor after its final event.
+    if (kq >= 0) close(kq);
 #else
     while (true) {
       errno = 0;
@@ -262,7 +321,7 @@ Napi::Value PtyFork(const Napi::CallbackInfo& info) {
   Napi::Env napiEnv(info.Env());
   Napi::HandleScope scope(napiEnv);
 
-  if (info.Length() != 11 ||
+  if ((info.Length() != 11 && info.Length() != 12) ||
       !info[0].IsString() ||
       !info[1].IsArray() ||
       !info[2].IsArray() ||
@@ -273,9 +332,11 @@ Napi::Value PtyFork(const Napi::CallbackInfo& info) {
       !info[7].IsNumber() ||
       !info[8].IsBoolean() ||
       !info[9].IsString() ||
-      !info[10].IsFunction()) {
+      !info[10].IsFunction() ||
+      (info.Length() == 12 && !info[11].IsBoolean())) {
     throw Napi::Error::New(napiEnv, "Usage: pty.fork(file, args, env, cwd, cols, rows, uid, gid, utf8, helperPath, onexit)");
   }
+  const bool bounded_writer = info.Length() == 12 && info[11].As<Napi::Boolean>().Value();
 
   // file
   std::string file = info[0].As<Napi::String>();
@@ -373,6 +434,12 @@ Napi::Value PtyFork(const Napi::CallbackInfo& info) {
     throw Napi::Error::New(napiEnv, "posix_spawnp failed.");
   }
   if (pty_nonblock(master) == -1) {
+    if (bounded_writer) {
+      close(master);
+      kill(pid, SIGKILL);
+      int ignored_status;
+      while (waitpid(pid, &ignored_status, 0) == -1 && errno == EINTR) {}
+    }
     throw Napi::Error::New(napiEnv, "Could not set master fd to nonblocking.");
   }
 #else
@@ -443,11 +510,79 @@ Napi::Value PtyFork(const Napi::CallbackInfo& info) {
       }
     default:
       if (pty_nonblock(master) == -1) {
+        if (bounded_writer) {
+          close(master);
+          kill(pid, SIGKILL);
+          int ignored_status;
+          while (waitpid(pid, &ignored_status, 0) == -1 && errno == EINTR) {}
+        }
         throw Napi::Error::New(napiEnv, "Could not set master fd to nonblocking.");
       }
   }
 #endif
 
+  if (bounded_writer) {
+#if !defined(__APPLE__) && !defined(__linux__)
+    close(master);
+    kill(pid, SIGKILL);
+    int ignored_status;
+    while (waitpid(pid, &ignored_status, 0) == -1 && errno == EINTR) {}
+    throw Napi::Error::New(napiEnv, "Bounded writer is unsupported on this platform.");
+#else
+    const int write_fd = fcntl(master, F_DUPFD_CLOEXEC, 3);
+    if (write_fd == -1) {
+      close(master);
+      kill(pid, SIGKILL);
+      int ignored_status;
+      while (waitpid(pid, &ignored_status, 0) == -1 && errno == EINTR) {}
+      throw Napi::Error::New(napiEnv, "Could not duplicate bounded writer fd.");
+    }
+    std::shared_ptr<CoveOwnedChild> child;
+    bool watcher_started = false;
+    try {
+      child = std::make_shared<CoveOwnedChild>(pid);
+      const char* pty_name = ptsname(master);
+      if (pty_name == nullptr) throw Napi::Error::New(napiEnv, "Could not name pty.");
+      Napi::Object obj = Napi::Object::New(napiEnv);
+      obj.Set("fd", Napi::Number::New(napiEnv, master));
+      obj.Set("writeFd", Napi::Number::New(napiEnv, write_fd));
+      obj.Set("pid", Napi::Number::New(napiEnv, pid));
+      obj.Set("pty", Napi::String::New(napiEnv, pty_name));
+      obj.Set("stopOwnedChild", Napi::Function::New(
+          napiEnv, [child](const Napi::CallbackInfo& call) {
+            Napi::Env env = call.Env();
+            if (call.Length() != 1 || !call[0].IsNumber()) {
+              throw Napi::TypeError::New(env, "Owned stop requires a signal number");
+            }
+            const int signal = call[0].As<Napi::Number>().Int32Value();
+            if (signal != SIGHUP && signal != SIGTERM && signal != SIGKILL) {
+              throw Napi::TypeError::New(env, "Unsupported owned stop signal");
+            }
+            std::lock_guard<std::mutex> lock(child->mutex);
+            if (child->reaped) return Napi::Boolean::New(env, false);
+            if (kill(child->pid, signal) == 0) return Napi::Boolean::New(env, true);
+            if (errno == ESRCH) return Napi::Boolean::New(env, false);
+            throw Napi::Error::New(env, "Owned child stop failed");
+          }));
+      SetupCoveOwnedExitCallback(napiEnv, info[10].As<Napi::Function>(), child);
+      watcher_started = true;
+      return obj;
+    } catch (...) {
+      close(write_fd);
+      close(master);
+      if (watcher_started && child) {
+        std::lock_guard<std::mutex> lock(child->mutex);
+        if (!child->reaped) kill(child->pid, SIGKILL);
+      } else {
+        kill(pid, SIGKILL);
+        int ignored_status;
+        while (waitpid(pid, &ignored_status, 0) == -1 && errno == EINTR) {}
+      }
+      throw;
+    }
+#endif
+  }
+
   Napi::Object obj = Napi::Object::New(napiEnv);
   obj.Set("fd", Napi::Number::New(napiEnv, master));
   obj.Set("pid", Napi::Number::New(napiEnv, pid));
@@ -691,95 +826,105 @@ pty_posix_spawn(char** argv, char** env,
                 int* master,
                 pid_t* pid,
                 int* err) {
-  int low_fds[3];
-  size_t count = 0;
-
-  for (; count < 3; count++) {
-    low_fds[count] = posix_openpt(O_RDWR);
-    if (low_fds[count] >= STDERR_FILENO)
-      break;
-  }
-
-  int flags = POSIX_SPAWN_CLOEXEC_DEFAULT |
-              POSIX_SPAWN_SETSIGDEF |
-              POSIX_SPAWN_SETSIGMASK |
-              POSIX_SPAWN_SETSID;
-  *master = posix_openpt(O_RDWR);
-  if (*master == -1) {
-    return;
-  }
-
-  int res = grantpt(*master) || unlockpt(*master);
-  if (res == -1) {
-    return;
-  }
-
-  // Use TIOCPTYGNAME instead of ptsname() to avoid threading problems.
-  int slave;
-  char slave_pty_name[128];
-  res = ioctl(*master, TIOCPTYGNAME, slave_pty_name);
-  if (res == -1) {
-    return;
-  }
-
-  slave = open(slave_pty_name, O_RDWR | O_NOCTTY);
-  if (slave == -1) {
-    return;
-  }
-
-  if (termp) {
-    res = tcsetattr(slave, TCSANOW, termp);
-    if (res == -1) {
-      return;
-    };
-  }
-
-  if (winp) {
-    res = ioctl(slave, TIOCSWINSZ, winp);
-    if (res == -1) {
-      return;
-    }
-  }
-
+  int low_fds[3] = {-1, -1, -1};
+  size_t low_count = 0;
+  int slave = -1;
+  bool actions_initialized = false;
+  bool attrs_initialized = false;
   posix_spawn_file_actions_t acts;
-  posix_spawn_file_actions_init(&acts);
-  posix_spawn_file_actions_adddup2(&acts, slave, STDIN_FILENO);
-  posix_spawn_file_actions_adddup2(&acts, slave, STDOUT_FILENO);
-  posix_spawn_file_actions_adddup2(&acts, slave, STDERR_FILENO);
-  posix_spawn_file_actions_addclose(&acts, slave);
-  posix_spawn_file_actions_addclose(&acts, *master);
-
   posix_spawnattr_t attrs;
-  posix_spawnattr_init(&attrs);
-  *err = posix_spawnattr_setflags(&attrs, flags);
-  if (*err != 0) {
-    goto done;
-  }
-
-  sigset_t signal_set;
-  /* Reset all signal the child to their default behavior */
-  sigfillset(&signal_set);
-  *err = posix_spawnattr_setsigdefault(&attrs, &signal_set);
-  if (*err != 0) {
-    goto done;
-  }
+  *master = -1;
+  *pid = -1;
+  *err = 0;
+
+  do {
+    // Reserve closed stdio numbers so PTY master/slave never become 0, 1 or 2.
+    while (low_count < 3) {
+      const int fd = posix_openpt(O_RDWR);
+      if (fd == -1) {
+        *err = errno;
+        break;
+      }
+      low_fds[low_count++] = fd;
+      if (fd >= STDERR_FILENO) break;
+    }
+    if (*err != 0) break;
 
-  /* Reset the signal mask for all signals */
-  sigemptyset(&signal_set);
-  *err = posix_spawnattr_setsigmask(&attrs, &signal_set);
-  if (*err != 0) {
-    goto done;
-  }
+    *master = posix_openpt(O_RDWR);
+    if (*master == -1) {
+      *err = errno;
+      break;
+    }
+    if (grantpt(*master) == -1 || unlockpt(*master) == -1) {
+      *err = errno;
+      break;
+    }
 
-  do
-    *err = posix_spawn(pid, argv[0], &acts, &attrs, argv, env);
-  while (*err == EINTR);
-done:
-  posix_spawn_file_actions_destroy(&acts);
-  posix_spawnattr_destroy(&attrs);
+    // Use TIOCPTYGNAME instead of ptsname() to avoid threading problems.
+    char slave_pty_name[128];
+    if (ioctl(*master, TIOCPTYGNAME, slave_pty_name) == -1) {
+      *err = errno;
+      break;
+    }
+    slave = open(slave_pty_name, O_RDWR | O_NOCTTY);
+    if (slave == -1) {
+      *err = errno;
+      break;
+    }
+    if (termp && tcsetattr(slave, TCSANOW, termp) == -1) {
+      *err = errno;
+      break;
+    }
+    if (winp && ioctl(slave, TIOCSWINSZ, winp) == -1) {
+      *err = errno;
+      break;
+    }
 
-  for (; count > 0; count--) {
-    close(low_fds[count]);
+    *err = posix_spawn_file_actions_init(&acts);
+    if (*err != 0) break;
+    actions_initialized = true;
+    *err = posix_spawn_file_actions_adddup2(&acts, slave, STDIN_FILENO);
+    if (*err != 0) break;
+    *err = posix_spawn_file_actions_adddup2(&acts, slave, STDOUT_FILENO);
+    if (*err != 0) break;
+    *err = posix_spawn_file_actions_adddup2(&acts, slave, STDERR_FILENO);
+    if (*err != 0) break;
+    *err = posix_spawn_file_actions_addclose(&acts, slave);
+    if (*err != 0) break;
+    *err = posix_spawn_file_actions_addclose(&acts, *master);
+    if (*err != 0) break;
+
+    *err = posix_spawnattr_init(&attrs);
+    if (*err != 0) break;
+    attrs_initialized = true;
+    const int flags = POSIX_SPAWN_CLOEXEC_DEFAULT |
+                      POSIX_SPAWN_SETSIGDEF |
+                      POSIX_SPAWN_SETSIGMASK |
+                      POSIX_SPAWN_SETSID;
+    *err = posix_spawnattr_setflags(&attrs, flags);
+    if (*err != 0) break;
+
+    sigset_t signal_set;
+    sigfillset(&signal_set);
+    *err = posix_spawnattr_setsigdefault(&attrs, &signal_set);
+    if (*err != 0) break;
+    sigemptyset(&signal_set);
+    *err = posix_spawnattr_setsigmask(&attrs, &signal_set);
+    if (*err != 0) break;
+
+    do {
+      *err = posix_spawn(pid, argv[0], &acts, &attrs, argv, env);
+    } while (*err == EINTR);
+  } while (false);
+
+  if (attrs_initialized) posix_spawnattr_destroy(&attrs);
+  if (actions_initialized) posix_spawn_file_actions_destroy(&acts);
+  // Child file actions do not release descriptors retained by this parent.
+  if (slave >= 0) close(slave);
+  for (size_t index = 0; index < low_count; index++) close(low_fds[index]);
+  if (*err != 0 && *master >= 0) {
+    close(*master);
+    *master = -1;
   }
 }
 #endif
@@ -789,6 +934,7 @@ done:
  */
 
 Napi::Object init(Napi::Env env, Napi::Object exports) {
+  exports.Set("coveBoundedWriterVersion", Napi::Number::New(env, 1));
   exports.Set("fork",    Napi::Function::New(env, PtyFork));
   exports.Set("open",    Napi::Function::New(env, PtyOpen));
   exports.Set("resize",  Napi::Function::New(env, PtyResize));
diff --git a/src/unixTerminal.ts b/src/unixTerminal.ts
index 98733dc0cd752b554bd94e45904ca341ad141bba..2ef698f511218941d0ba8f682bae132b7e3bc7e5 100644
--- a/src/unixTerminal.ts
+++ b/src/unixTerminal.ts
@@ -8,7 +8,17 @@ import * as net from 'net';
 import * as path from 'path';
 import * as tty from 'tty';
 import { Terminal, DEFAULT_COLS, DEFAULT_ROWS } from './terminal';
-import { IProcessEnv, IPtyForkOptions, IPtyOpenOptions } from './interfaces';
+import {
+  BoundedWriteRejectionReason,
+  IBoundedWriteAdmission,
+  IBoundedWriteOptions,
+  IBoundedWriteOwnerResult,
+  IBoundedWriteSettlement,
+  IBoundedWriteState,
+  IProcessEnv,
+  IPtyForkOptions,
+  IPtyOpenOptions
+} from './interfaces';
 import { ArgvOrCommandLine, IDisposable } from './types';
 import { assign, loadNativeModule } from './utils';
 
@@ -22,6 +32,10 @@ helperPath = helperPath.replace('node_modules.asar', 'node_modules.asar.unpacked
 const DEFAULT_FILE = 'sh';
 const DEFAULT_NAME = 'xterm';
 const DESTROY_SOCKET_TIMEOUT_MS = 200;
+const MAX_BOUNDED_WRITE_BYTES = 64 * 1024;
+const MAX_BOUNDED_WRITE_TASKS = 256;
+const BOUNDED_WRITE_RETRY_DELAY_MS = 1;
+
 
 export class UnixTerminal extends Terminal {
   protected _fd: number;
@@ -36,7 +50,8 @@ export class UnixTerminal extends Terminal {
   private _boundClose: boolean = false;
   private _emittedClose: boolean = false;
 
-  private _writeStream: CustomWriteStream;
+  private _writeStream: CustomWriteStream | undefined;
+  private _boundedOwnedStop: ((signal: number) => boolean) | undefined;
 
   private _master: net.Socket | undefined;
   private _slave: net.Socket | undefined;
@@ -55,6 +70,7 @@ export class UnixTerminal extends Terminal {
     args = args || [];
     file = file || DEFAULT_FILE;
     opt = opt || {};
+    const boundedWrite = validateBoundedWriteOptions(opt.boundedWrite);
     opt.env = opt.env || process.env;
 
     this._cols = opt.cols || DEFAULT_COLS;
@@ -75,7 +91,12 @@ export class UnixTerminal extends Terminal {
 
     const encoding = (opt.encoding === undefined ? 'utf8' : opt.encoding);
 
+    let adoptionFailed = false;
     const onexit = (code: number, signal: number): void => {
+      if (adoptionFailed) return;
+      if (this._writeStream) {
+        this._writeStream.dispose('closed');
+      }
       // XXX Sometimes a data event is emitted after exit. Wait til socket is
       // destroyed.
       if (!this._emittedClose) {
@@ -102,14 +123,44 @@ export class UnixTerminal extends Terminal {
       this.emit('exit', code, signal);
     };
 
-    // fork
-    const term = pty.fork(file, args, parsedEnv, cwd, this._cols, this._rows, uid, gid, (encoding === 'utf8'), helperPath, onexit);
-
-    this._socket = new tty.ReadStream(term.fd);
-    if (encoding !== null) {
-      this._socket.setEncoding(encoding);
+    if (boundedWrite && pty.coveBoundedWriterVersion !== 1) {
+      throw new Error('Bounded writer native capability is unavailable');
     }
-    this._writeStream = new CustomWriteStream(term.fd, (encoding || undefined) as BufferEncoding);
+    const term = boundedWrite
+      ? pty.fork(file, args, parsedEnv, cwd, this._cols, this._rows, uid, gid, encoding === 'utf8', helperPath, onexit, true)
+      : pty.fork(file, args, parsedEnv, cwd, this._cols, this._rows, uid, gid, encoding === 'utf8', helperPath, onexit);
+    let readOwner: tty.ReadStream | undefined;
+    let writerOwner: CustomWriteStream | undefined;
+    let readAdopted = false;
+    try {
+      if (boundedWrite && (
+        !Number.isSafeInteger(term.fd) || term.fd < 0 ||
+        !Number.isSafeInteger(term.writeFd) || term.writeFd! < 0 ||
+        term.writeFd === term.fd || typeof term.stopOwnedChild !== 'function'
+      )) throw new Error('Invalid bounded writer native ownership result');
+      if (boundedWrite) this._boundedOwnedStop = term.stopOwnedChild;
+
+      // Retain the partial owner if Node's ReadStream constructor fails after adoption.
+      const socket = Object.create(tty.ReadStream.prototype) as tty.ReadStream;
+      try {
+        Reflect.apply(tty.ReadStream, socket, [term.fd]);
+        readAdopted = true;
+      } catch (error) {
+        if ((socket as any)._handle) readAdopted = true;
+        if (readAdopted) readOwner = socket;
+        throw error;
+      }
+      readOwner = socket;
+      this._socket = socket;
+      if (encoding !== null) {
+        this._socket.setEncoding(encoding);
+      }
+      writerOwner = new CustomWriteStream(
+        boundedWrite ? term.writeFd! : term.fd,
+        (encoding || undefined) as BufferEncoding,
+        boundedWrite
+      );
+      this._writeStream = writerOwner;
 
     // setup
     this._socket.on('error', (err: any) => {
@@ -120,6 +171,11 @@ export class UnixTerminal extends Terminal {
         }
       }
 
+      const isExitEio = !!err.code && (err.code.indexOf('errno 5') !== -1 || err.code.indexOf('EIO') !== -1);
+      // The distinct writer fd remains owned through any issued fs.write.
+      if (this._writeStream) {
+        this._writeStream.dispose(isExitEio ? 'closed' : 'error', isExitEio ? undefined : err);
+      }
       // close
       this._close();
       // EIO on exit from fs.ReadStream:
@@ -132,10 +188,8 @@ export class UnixTerminal extends Terminal {
       // the terminal.
       // node < 0.6.14: errno 5
       // node >= 0.6.14: read EIO
-      if (err.code) {
-        if (~err.code.indexOf('errno 5') || ~err.code.indexOf('EIO')) {
-          return;
-        }
+      if (isExitEio) {
+        return;
       }
 
       // throw anything else
@@ -159,15 +213,73 @@ export class UnixTerminal extends Terminal {
         return;
       }
       this._emittedClose = true;
+      if (this._writeStream) {
+        this._writeStream.dispose('closed');
+      }
       this._close();
       this.emit('close');
     });
 
-    this._forwardEvents();
+      this._forwardEvents();
+    } catch (error) {
+      if (boundedWrite) {
+        adoptionFailed = true;
+        const cleanupErrors: unknown[] = [];
+        try {
+          if (writerOwner) writerOwner.dispose('closed');
+          else if (Number.isSafeInteger(term.writeFd) && term.writeFd! >= 0 && term.writeFd !== term.fd) fs.closeSync(term.writeFd!);
+        } catch (cleanupError) { cleanupErrors.push(cleanupError); }
+        try {
+          if (readOwner) readOwner.destroy();
+          else if (!readAdopted && Number.isSafeInteger(term.fd) && term.fd >= 0) fs.closeSync(term.fd);
+        } catch (cleanupError) { cleanupErrors.push(cleanupError); }
+        try {
+          if (!term.stopOwnedChild || !term.stopOwnedChild(9)) {
+            // A recorded reap is safe; absence of the stop closure is not.
+            if (!term.stopOwnedChild) cleanupErrors.push(new Error('Owned child stop unavailable'));
+          }
+        } catch (cleanupError) { cleanupErrors.push(cleanupError); }
+        if (cleanupErrors.length) throw new Error(`Bounded PTY adoption failed with ${cleanupErrors.length} cleanup error(s): ${String(error)}`);
+      }
+      throw error;
+    }
   }
 
   protected _write(data: string | Buffer): void {
-    this._writeStream.write(data);
+    this._writeStream!.write(data);
+  }
+
+  protected _close(): void {
+    const keepBoundedWriteRejection = !!this._writeStream && this._writeStream.isBounded;
+    super._close();
+    if (keepBoundedWriteRejection) {
+      // Terminal._close normally turns legacy writes into silent no-ops. On an
+      // opted-in bounded terminal they must still reach the retired writer and
+      // synchronously report that no bytes were admitted.
+      this.write = Terminal.prototype.write;
+    }
+  }
+
+  public writeBounded(data: string | Buffer, onSettled: (settlement: IBoundedWriteSettlement) => void): IBoundedWriteAdmission {
+    return this._writeStream
+      ? this._writeStream.writeBounded(data, onSettled)
+      : { accepted: false, reason: 'not-enabled' };
+  }
+
+  public getBoundedWriteState(): IBoundedWriteState | undefined {
+    return this._writeStream ? this._writeStream.getBoundedState() : undefined;
+  }
+
+  public disposeBoundedWrite(): boolean {
+    if (!this._writeStream || !this._writeStream.isBounded) {
+      return false;
+    }
+    this._writeStream.dispose('closed');
+    return true;
+  }
+
+  public get boundedWriteCompletion(): Promise<IBoundedWriteOwnerResult> | undefined {
+    return this._writeStream?.boundedWriteCompletion;
   }
 
   /* Accessors */
@@ -234,6 +346,9 @@ export class UnixTerminal extends Terminal {
   }
 
   public destroy(): void {
+    if (this._writeStream) {
+      this._writeStream.dispose('closed');
+    }
     this._close();
 
     // Need to close the read stream so node stops reading a dead file
@@ -243,7 +358,6 @@ export class UnixTerminal extends Terminal {
     });
 
     this._socket.destroy();
-    this._writeStream.dispose();
   }
 
   public kill(signal?: string): void {
@@ -304,6 +418,50 @@ interface IWriteTask {
   buffer: Buffer;
   /** The current offset of not yet written data. */
   offset: number;
+  /** Present only for admitted bounded writes. */
+  ticket?: number;
+  /** Present only for admitted bounded writes. */
+  onSettled?: (settlement: IBoundedWriteSettlement) => void;
+  settled?: boolean;
+}
+
+interface IWriteIo {
+  write(fd: number, buffer: Buffer, offset: number, callback: (err: NodeJS.ErrnoException | null, written: number) => void): void;
+  setImmediate(callback: () => void): NodeJS.Immediate;
+  clearImmediate(handle: NodeJS.Immediate | undefined): void;
+  setTimeout(callback: () => void, delay: number): NodeJS.Timeout;
+  clearTimeout(handle: NodeJS.Timeout | undefined): void;
+  close(fd: number): void;
+}
+
+const DEFAULT_WRITE_IO: IWriteIo = {
+  write: (fd, buffer, offset, callback) => fs.write(fd, buffer, offset, callback),
+  setImmediate: callback => setImmediate(callback),
+  clearImmediate: handle => clearImmediate(handle),
+  setTimeout: (callback, delay) => setTimeout(callback, delay),
+  clearTimeout: handle => clearTimeout(handle),
+  close: fd => fs.closeSync(fd)
+};
+
+const NOOP_SETTLEMENT = (_settlement: IBoundedWriteSettlement): void => {};
+
+function validateBoundedWriteOptions(options: IBoundedWriteOptions | undefined): IBoundedWriteOptions | undefined {
+  if (options === undefined) {
+    return undefined;
+  }
+  if (typeof options !== 'object' || options === null) {
+    throw new Error('boundedWrite must be an object');
+  }
+  if (!Number.isSafeInteger(options.maxAllocatedBytes) || options.maxAllocatedBytes <= 0 || options.maxAllocatedBytes > MAX_BOUNDED_WRITE_BYTES) {
+    throw new Error(`boundedWrite.maxAllocatedBytes must be a positive safe integer no greater than ${MAX_BOUNDED_WRITE_BYTES}`);
+  }
+  if (!Number.isSafeInteger(options.maxTasks) || options.maxTasks <= 0 || options.maxTasks > MAX_BOUNDED_WRITE_TASKS) {
+    throw new Error(`boundedWrite.maxTasks must be a positive safe integer no greater than ${MAX_BOUNDED_WRITE_TASKS}`);
+  }
+  return {
+    maxAllocatedBytes: options.maxAllocatedBytes,
+    maxTasks: options.maxTasks
+  };
 }
 
 /**
@@ -312,77 +470,308 @@ interface IWriteTask {
  * issues that can occur when using the standard APIs in Node.
  */
 class CustomWriteStream implements IDisposable {
-
   private readonly _writeQueue: IWriteTask[] = [];
-  private _writeImmediate: NodeJS.Immediate | undefined;
+  private _turn: NodeJS.Immediate | undefined;
+  private _retryTimer: NodeJS.Timeout | undefined;
+  private _state: 'open' | 'error' | 'closed' = 'open';
+  private _retirementError: NodeJS.ErrnoException | undefined;
+  private _writeInFlight = false;
+  private _activeIssue?: object;
+  private _allocatedBytes = 0;
+  private _remainingBytes = 0;
+  private _retainedTasks = 0;
+  private _nextTicket = 1;
+  private _settlementDepth = 0;
+  private _closingOut = false;
+  private _ownerResult: IBoundedWriteOwnerResult | undefined;
+  private _resolveCompletion: ((result: IBoundedWriteOwnerResult) => void) | undefined;
+  public readonly boundedWriteCompletion: Promise<IBoundedWriteOwnerResult> | undefined;
 
   constructor(
-    private readonly _fd: number,
-    private readonly _encoding: BufferEncoding
+    private _fd: number,
+    private readonly _encoding: BufferEncoding,
+    private readonly _boundedOptions?: IBoundedWriteOptions,
+    private readonly _io: IWriteIo = DEFAULT_WRITE_IO
   ) {
+    if (_boundedOptions) {
+      this.boundedWriteCompletion = new Promise(resolve => { this._resolveCompletion = resolve; });
+    }
   }
 
-  dispose(): void {
-    clearImmediate(this._writeImmediate);
-    this._writeImmediate = undefined;
+  public get isBounded(): boolean {
+    return this._boundedOptions !== undefined;
   }
 
-  write(data: string | Buffer): void {
-    // Writes are put in a queue and processed asynchronously in order to handle
-    // backpressure from the kernel buffer.
-    const buffer = typeof data === 'string'
-      ? Buffer.from(data, this._encoding)
-      : Buffer.from(data);
+  dispose(status: 'error' | 'closed' = 'closed', error?: NodeJS.ErrnoException): void {
+    if (this._state !== 'open') return;
+    this._state = status;
+    this._retirementError = error;
+    this._io.clearImmediate(this._turn);
+    this._turn = undefined;
+    this._io.clearTimeout(this._retryTimer);
+    this._retryTimer = undefined;
+    if (!this._boundedOptions) {
+      this._writeQueue.length = 0;
+      return;
+    }
+    this._finishRetirement();
+  }
 
-    if (buffer.byteLength !== 0) {
-      this._writeQueue.push({ buffer, offset: 0 });
-      if (this._writeQueue.length === 1) {
-        this._processWriteQueue();
+  write(data: string | Buffer): void {
+    if (this._boundedOptions) {
+      const admission = this.writeBounded(data, NOOP_SETTLEMENT);
+      if (!admission.accepted) {
+        const error: NodeJS.ErrnoException = new Error(`Bounded pty write rejected: ${admission.reason}`);
+        error.code = `PTY_WRITE_${String(admission.reason).toUpperCase().replace('-', '_')}`;
+        throw error;
       }
+      return;
+    }
+    const buffer = typeof data === 'string' ? Buffer.from(data, this._encoding) : Buffer.from(data);
+    if (buffer.byteLength) {
+      this._writeQueue.push({ buffer, offset: 0 });
+      if (this._writeQueue.length === 1) this._processWriteQueue();
     }
   }
 
-  private _processWriteQueue(): void {
-    this._writeImmediate = undefined;
-
-    if (this._writeQueue.length === 0) {
-      return;
+  writeBounded(data: string | Buffer, onSettled: (settlement: IBoundedWriteSettlement) => void): IBoundedWriteAdmission {
+    if (!this._boundedOptions) return { accepted: false, reason: 'not-enabled' };
+    if (this._state !== 'open') return { accepted: false, reason: this._state };
+    if ((typeof data !== 'string' && !Buffer.isBuffer(data)) || typeof onSettled !== 'function') {
+      return { accepted: false, reason: 'invalid-data' };
     }
+    const byteLength = typeof data === 'string' ? Buffer.byteLength(data, this._encoding) : data.byteLength;
+    if (!byteLength) return { accepted: false, reason: 'empty' };
+    if (this._retainedTasks >= this._boundedOptions.maxTasks) return { accepted: false, reason: 'task-limit' };
+    if (byteLength > this._boundedOptions.maxAllocatedBytes - this._allocatedBytes) return { accepted: false, reason: 'byte-limit' };
+    if (!Number.isSafeInteger(this._nextTicket)) return { accepted: false, reason: 'ticket-exhausted' };
+
+    // Admission accounts for the detached copy and ticket before any drain.
+    const buffer = typeof data === 'string' ? Buffer.from(data, this._encoding) : Buffer.from(data);
+    const ticket = this._nextTicket++;
+    this._allocatedBytes += buffer.byteLength;
+    this._remainingBytes += buffer.byteLength;
+    this._retainedTasks++;
+    this._writeQueue.push({ buffer, offset: 0, ticket, onSettled });
+    this._scheduleTurn();
+    return { accepted: true, ticket, byteLength: buffer.byteLength };
+  }
 
+  getBoundedState(): IBoundedWriteState | undefined {
+    if (!this._boundedOptions) return undefined;
+    return {
+      state: this._state,
+      allocatedBytes: this._allocatedBytes,
+      remainingBytes: this._remainingBytes,
+      tasks: this._retainedTasks,
+      queuedTasks: this._state === 'open' ? this._writeQueue.length : 0,
+      writeInFlight: this._writeInFlight,
+      retryScheduled: this._retryTimer !== undefined,
+      turnScheduled: this._turn !== undefined
+    };
+  }
+
+  private _scheduleTurn(): void {
+    if (this._state !== 'open' || this._writeInFlight || this._turn !== undefined ||
+        this._retryTimer !== undefined || this._writeQueue.length === 0) return;
+    this._turn = this._io.setImmediate(() => this._processWriteQueue());
+  }
+
+  private _scheduleRetry(): void {
+    if (this._state !== 'open' || this._retryTimer !== undefined ||
+        this._turn !== undefined || this._writeQueue.length === 0) return;
+    this._retryTimer = this._io.setTimeout(() => {
+      this._retryTimer = undefined;
+      this._scheduleTurn();
+    }, BOUNDED_WRITE_RETRY_DELAY_MS);
+  }
+
+  private _processWriteQueue(): void {
+    this._turn = undefined;
+    if (this._state !== 'open' || this._writeInFlight || !this._writeQueue.length) return;
     const task = this._writeQueue[0];
+    const issue = {};
+    this._activeIssue = issue;
+    this._writeInFlight = true;
+    let submitted = false;
+    try {
+      this._io.write(this._fd, task.buffer, task.offset, (err, written) => {
+        // A stale callback cannot retire a newer issued write.
+        if (this._activeIssue !== issue) {
+          if (this._boundedOptions) {
+            const invalid: NodeJS.ErrnoException = new Error('Stale fs.write callback');
+            invalid.code = 'EIO';
+            this.dispose('error', invalid);
+          }
+          return;
+        }
+        this._activeIssue = undefined;
+        this._writeInFlight = false;
+        this._onWriteComplete(task, err, written);
+      });
+      submitted = true;
+    } catch (error) {
+      // The seam, like fs.write, throws only before handing off a callback.
+      if (!submitted && this._activeIssue === issue) {
+        this._activeIssue = undefined;
+        this._writeInFlight = false;
+        const writeError = error instanceof Error ? error as NodeJS.ErrnoException : new Error(String(error));
+        if (this._boundedOptions) this.dispose('error', writeError);
+        else {
+          this._writeQueue.length = 0;
+          console.error('Unhandled pty write error', writeError);
+        }
+      } else {
+        throw error;
+      }
+    }
+  }
 
-    // Write to the underlying file descriptor and handle it directly, rather
-    // than using the `net.Socket`/`tty.WriteStream` wrappers which swallow and
-    // mask errors like EAGAIN and can cause the thread to block indefinitely.
-    fs.write(this._fd, task.buffer, task.offset, (err, written) => {
+  private _onWriteComplete(task: IWriteTask, err: NodeJS.ErrnoException | null, written: number): void {
+    if (!this._boundedOptions) {
       if (err) {
-        if ('code' in err && err.code === 'EAGAIN') {
-          // `setImmediate` is used to yield to the event loop and re-attempt
-          // the write later.
-          this._writeImmediate = setImmediate(() => this._processWriteQueue());
-        } else {
-          // Stop processing immediately on unexpected error and log
+        if (err.code === 'EAGAIN') this._scheduleTurn();
+        else {
           this._writeQueue.length = 0;
           console.error('Unhandled pty write error', err);
         }
         return;
       }
-
+      if (written > 0) {
+        task.offset += written;
+        if (task.offset >= task.buffer.byteLength) this._writeQueue.shift();
+      }
+      this._scheduleTurn();
+      return;
+    }
+    if (err && Number.isSafeInteger(written) && written > 0 &&
+        written <= task.buffer.byteLength - task.offset) {
       task.offset += written;
-      if (task.offset >= task.buffer.byteLength) {
-        this._writeQueue.shift();
+      this._remainingBytes -= written;
+    }
+    if (this._state !== 'open' && err && err.code !== 'EAGAIN') {
+      this._state = 'error';
+      this._retirementError = err;
+      this._finishRetirement();
+      return;
+    }
+    if (err && err.code !== 'EAGAIN') {
+      this.dispose('error', err);
+      return;
+    }
+    if (!err) {
+      if (!Number.isSafeInteger(written) || written < 0 || written > task.buffer.byteLength - task.offset) {
+        const invalid: NodeJS.ErrnoException = new Error(`Invalid fs.write byte count: ${written}`);
+        invalid.code = 'EIO';
+        if (this._state === 'open') this.dispose('error', invalid);
+        else {
+          this._state = 'error';
+          this._retirementError = invalid;
+          this._finishRetirement();
+        }
+        return;
       }
+      task.offset += written;
+      this._remainingBytes -= written;
+    }
+    if (this._state !== 'open') {
+      this._finishRetirement();
+      return;
+    }
+    if (task.offset === task.buffer.byteLength) {
+      this._writeQueue.shift();
+      try {
+        this._settleAndRelease(task, 'written');
+      } catch (error) {
+        const callbackError = error instanceof Error ? error as NodeJS.ErrnoException : new Error(String(error));
+        try { this.dispose('error', callbackError); }
+        finally { throw error; }
+      }
+    }
+    if (this._state === 'open') {
+      if (err || written === 0) this._scheduleRetry();
+      else this._scheduleTurn();
+    }
+  }
 
-      // Since there is more room in the kernel buffer, we can continue to write
-      // until we hit EAGAIN or exhaust the queue.
-      //
-      // Note that old versions of bash, like v3.2 which ships in macOS, appears
-      // to have a bug in its readline implementation that causes data
-      // corruption when writes to the pty happens too quickly. Instead of
-      // trying to workaround that we just accept it so that large pastes are as
-      // fast as possible.
-      // Context: https://github.com/microsoft/node-pty/issues/833
-      this._processWriteQueue();
-    });
+  private _finishRetirement(): void {
+    if (this._state === 'open' || this._writeInFlight || this._closingOut || this._ownerResult) return;
+    this._closingOut = true;
+    try {
+      // Surrender ownership before close: EINTR/unknown close state cannot be
+      // retried on a numeric fd that another owner may already have reused.
+      const fd = this._fd;
+      this._fd = -1;
+      try {
+        this._io.close(fd);
+        this._ownerResult = { kind: 'closed' };
+      } catch (error) {
+        this._ownerResult = {
+          kind: 'close-uncertain',
+          error: error instanceof Error ? error.message : String(error)
+        };
+      }
+      const tasks = this._writeQueue.splice(0);
+      let firstError: unknown;
+      for (const task of tasks) {
+        const status = task.offset === task.buffer.byteLength ? 'written' : this._state;
+        try {
+          this._settleAndRelease(task, status, this._retirementError);
+        } catch (error) {
+          if (firstError === undefined) firstError = error;
+        }
+      }
+      if (firstError !== undefined) throw firstError;
+    } finally {
+      this._closingOut = false;
+      this._maybeComplete();
+    }
   }
+
+  private _settleAndRelease(task: IWriteTask, status: 'written' | 'error' | 'closed', error?: NodeJS.ErrnoException): void {
+    if (task.ticket === undefined || task.settled) return;
+    task.settled = true;
+    const settlement: IBoundedWriteSettlement = {
+      ticket: task.ticket,
+      status,
+      originalBytes: task.buffer.byteLength,
+      writtenBytes: task.offset,
+      remainingBytes: task.buffer.byteLength - task.offset
+    };
+    if (error) {
+      settlement.errorCode = error.code;
+      settlement.errorMessage = error.message;
+    }
+    this._settlementDepth++;
+    try {
+      task.onSettled!(settlement);
+    } finally {
+      this._allocatedBytes -= task.buffer.byteLength;
+      this._remainingBytes -= task.buffer.byteLength - task.offset;
+      this._retainedTasks--;
+      task.ticket = undefined;
+      task.onSettled = undefined;
+      this._settlementDepth--;
+      this._maybeComplete();
+    }
+  }
+
+  private _maybeComplete(): void {
+    if (this._ownerResult && !this._closingOut && !this._writeInFlight &&
+        this._settlementDepth === 0 && this._retainedTasks === 0) {
+      const resolve = this._resolveCompletion;
+      this._resolveCompletion = undefined;
+      if (resolve) resolve(this._ownerResult);
+    }
+  }
+}
+
+/** @internal Test seam for deterministic package-local writer fault tests. */
+export function __createBoundedWriteStreamForTest(
+  fd: number,
+  encoding: BufferEncoding,
+  options: IBoundedWriteOptions,
+  io: IWriteIo
+): CustomWriteStream {
+  return new CustomWriteStream(fd, encoding, validateBoundedWriteOptions(options), io);
 }
diff --git a/typings/node-pty.d.ts b/typings/node-pty.d.ts
index 6f050ff1d97b8d3477be7ee95d2af516bd9517b7..f4a8799e2d288cf9ce320f8b9f81931fc7f0dda4 100644
--- a/typings/node-pty.d.ts
+++ b/typings/node-pty.d.ts
@@ -76,6 +76,53 @@ declare module 'node-pty' {
     flowControlResume?: string;
   }
 
+  export interface IBoundedWriteOptions {
+    /** Maximum retained copied bytes for this PTY; must be 1..65536. */
+    maxAllocatedBytes: number;
+    /** Maximum retained accepted tasks for this PTY; must be 1..256. */
+    maxTasks: number;
+  }
+
+  export type BoundedWriteRejectionReason =
+    | 'not-enabled'
+    | 'closed'
+    | 'error'
+    | 'empty'
+    | 'invalid-data'
+    | 'byte-limit'
+    | 'task-limit'
+    | 'ticket-exhausted';
+
+  export type IBoundedWriteAdmission =
+    | { accepted: true; ticket: number; byteLength: number }
+    | { accepted: false; reason: BoundedWriteRejectionReason };
+
+  export interface IBoundedWriteSettlement {
+    ticket: number;
+    status: 'written' | 'error' | 'closed';
+    originalBytes: number;
+    /** Bytes definitely accepted by fs.write, not necessarily consumed by the child. */
+    writtenBytes: number;
+    remainingBytes: number;
+    errorCode?: string;
+    errorMessage?: string;
+  }
+
+  export type IBoundedWriteOwnerResult =
+    | { kind: 'closed' }
+    | { kind: 'close-uncertain'; error: string };
+
+  export interface IBoundedWriteState {
+    state: 'open' | 'error' | 'closed';
+    allocatedBytes: number;
+    remainingBytes: number;
+    tasks: number;
+    queuedTasks: number;
+    writeInFlight: boolean;
+    retryScheduled: boolean;
+    turnScheduled: boolean;
+  }
+
   export interface IPtyForkOptions extends IBasePtyForkOptions {
     /**
      * Security warning: use this option with great caution,
@@ -83,6 +130,9 @@ declare module 'node-pty' {
      */
     uid?: number;
     gid?: number;
+
+    /** Opts this Unix PTY into whole-intent bounded writes. */
+    boundedWrite?: IBoundedWriteOptions;
   }
 
   export interface IWindowsPtyForkOptions extends IBasePtyForkOptions {
@@ -175,6 +225,21 @@ declare module 'node-pty' {
      */
     write(data: string | Buffer): void;
 
+    /**
+     * Synchronously admits a complete write without copying rejected data.
+     * Available on Unix; returns not-enabled unless boundedWrite was configured.
+     */
+    writeBounded?(data: string | Buffer, onSettled: (settlement: IBoundedWriteSettlement) => void): IBoundedWriteAdmission;
+
+    /** Returns retained bounded-write diagnostics on Unix. */
+    getBoundedWriteState?(): IBoundedWriteState | undefined;
+
+    /** Idempotently retires an enabled bounded Unix writer. */
+    disposeBoundedWrite?(): boolean;
+
+    /** Settles after the bounded writer closes and all owned callbacks unwind. */
+    readonly boundedWriteCompletion?: Promise<IBoundedWriteOwnerResult>;
+
     /**
      * Kills the pty.
      * @param signal The signal to use, defaults to SIGHUP. This parameter is not supported on
